# 项目分层与审阅约定

按用户提供的参考统一目录。正式 Python 包为 `backend/app/`，API 文件直接放在 `app/api/`，不建立版本号文件夹。现有接口的 `/api/v1` URL 前缀保留；URL 与文件夹没有必须同名的关系。

## 1. 当前实际目录

```text
TravelMindAI/
├── backend/
│   ├── app/
│   │   ├── api/              # 认证、会话、资料及流式HTTP接口
│   │   ├── llm/              # 聊天模型与Embedding客户端
│   │   ├── models/           # SQLAlchemy数据库模型
│   │   ├── schemas/          # 请求、响应、工具和保存格式
│   │   ├── services/         # 对话、需求、资料、地图、行程及事务
│   │   ├── config.py         # 配置校验
│   │   ├── database.py       # 数据库连接
│   │   └── main.py           # 应用工厂与生命周期
│   ├── scripts/              # 检查、评测、管理命令
│   ├── evals/                # 固定评测输入JSON，提交Git
│   ├── migrations/           # 数据库迁移，提交Git
│   ├── tests/                # 自动化测试
│   ├── .env / .env.example   # 本机配置 / 可公开模板
│   └── pyproject.toml、uv.lock
├── frontend/
│   ├── src/                  # 页面、组件、HTTP与SSE客户端
│   ├── tests/                # 流式对话回归
│   └── package.json、package-lock.json、vite.config.ts、tsconfig.json
├── data/
│   ├── chinatravel/          # 本地研究数据集
│   └── lvbangpt/             # 本地补充数据集
├── temp/
│   ├── uploads/              # 上传原文，必须保留
│   ├── knowledge/            # 补充资料，必须保留
│   ├── logs/                 # 服务日志
│   ├── reports/              # 评测生成的报告
│   └── runtime/              # 验收临时文件
├── deploy/milvus/            # Milvus部署配置，持久数据由Docker卷管理
├── docs/                     # 文档、图表及实施计划
└── README.md、AGENTS.md、.gitignore
```

源码核对：2026-09-17。树中省略工具缓存、依赖和临时兼容入口。`data/uploads`、`backend/data/uploads`仍是指向`temp/uploads`的兼容联接；`backend/data/runtime`仍有前端占用的两份日志，暂未停服清理。空`assets`及旧根目录缓存的删除尚未执行。学习示例已移到项目外`E:/TravelMindAI-learning`。

`__init__.py` 使用双下划线，是 Python 包声明文件。目录名、数据库名和环境变量不必相同：包名改为 `app`，安装项目仍叫 `travelmind-backend`，数据库仍叫 `travelmind`，配置仍使用 `TRAVELMIND_` 前缀。

## 2. 后续模块按同一层次增加

以下展示完整产品的规划位置；其中frontend基础及对话页已提前到M2实现，实际存在文件以上节为准，其他目录按需扩展，不先放空业务类。

```text
TravelMindAI/
├── backend/
│   ├── app/
│   │   ├── models/                 # 后续按业务增加 user.py、spot.py 等
│   │   ├── schemas/                # 后续增加 agent.py、user.py 等契约
│   │   ├── api/                    # 后续增加 agent.py、spots.py、auth.py 等路由
│   │   ├── rag/
│   │   │   ├── __init__.py
│   │   │   ├── embeddings.py        # 嵌入模型适配
│   │   │   ├── vectorstore.py       # Milvus 存取
│   │   │   └── chain.py             # 检索与引用组装
│   │   ├── agent/
│   │   │   ├── __init__.py
│   │   │   ├── state.py             # 工作流状态
│   │   │   ├── nodes.py             # 节点
│   │   │   ├── graph.py             # LangGraph 图装配
│   │   │   ├── memory.py            # 记忆与恢复
│   │   │   └── tools/              # 景点、天气、路线等工具，按实际接入增加
│   │   ├── services/               # 后续增加需求抽取、景点等业务服务
│   │   └── utils/                  # 确有共用的无业务辅助函数时再建立
│   ├── scripts/                    # 后续增加 seed_data.py 等显式数据导入命令
│   └── Dockerfile
├── frontend/
│   ├── src/
│   │   ├── api/                    # trips.ts、agent.ts 等请求封装
│   │   ├── components/             # common/、trip/、map/ 可复用组件
│   │   ├── views/                  # Home、TripCreate、TripDetail、TripPlan 等页面
│   │   ├── stores/                 # Pinia 状态
│   │   ├── router/                 # 页面路由
│   │   ├── types/                  # TypeScript 类型
│   │   ├── App.vue
│   │   └── main.ts
│   ├── public/
│   ├── package.json
│   ├── tsconfig.json
│   ├── vite.config.ts
│   ├── Dockerfile
│   └── nginx.conf
├── docker-compose.yml
└── docker-compose.prod.yml
```

这份参考规定代码放在哪里，不表示一次实现所有功能。预订模块等参考示例中的功能，仍需按项目需求范围决定是否实现；不会因为目录示例中出现过就直接新增业务。

保留 `pyproject.toml + uv.lock` 作为唯一依赖来源，不再手工维护重复的 `requirements.txt`。数据库初始化继续使用 Alembic，不增加绕开迁移的 `init_db.py/create_all()` 命令。

## 3. 审阅时按这个顺序

1. `schemas/`：请求字段是否合理，哪些输入应被拒绝，返回内容是什么。
2. `api/`：HTTP 地址、参数接收、依赖注入、状态码和返回值。
3. `services/`：预算怎么算，事务如何提交/回滚，版本怎么分配。
4. `models/` 和 `database.py`：字段与约束、数据库连接方式。
5. `tests/`：正常输入、非法输入、故障与持久化是否有验证。

运行调用主线为 **api → services → models / database → PostgreSQL**。纯预算服务只做计算，因此不需要经过数据库。`main.py` 负责把这些组件组装起来；`schemas/` 提供契约，不是额外的网络服务。

M2主线：**Vue对话页 → api/requirements → requirement_service → llm/client → DeepSeek → requirement_merge与schema校验 → Vue需求卡片**。保留命令行抽取入口；对话消息尚未入库。逐文件讲解统一见 [各阶段开发说明](./07-各阶段开发说明.md)。

保持详细中文说明。函数说明使用 `def` 上方的 `"""..."""`，中间空一行；有装饰器时说明放在装饰器上方。新增或修改两三个主要文件后，说明文件职责与调用顺序。

## 4. 这次迁移的对应关系

下表左侧仅用于辨认旧代码位置，不是继续保留的目录。

| 原模块 | 当前模块 |
|---|---|
| `travelmind.main` | `app.main` |
| `travelmind.settings` | `app.config` |
| `travelmind.persistence.database` | 连接函数在 `app.database`，命令入口在 `scripts.check_db` |
| `travelmind.persistence.models` | `app.models.trip` |
| `travelmind.schemas` | `app.schemas.budget` 和 `app.schemas.common` |
| `travelmind.session_schemas` | `app.schemas.trip` |
| 原预算路由 | `app.api.budget` |
| 原会话路由 | `app.api.trips` |
| `travelmind.domain.budget` | `app.services.budget_service` |
| `travelmind.persistence.store.TravelStore` | `app.services.trip_service.TripService` |
| `travelmind.persistence.demo` | `scripts.demo_trip` |

PyCharm 打开项目根目录时，将 `backend` 标记为 Sources Root，并选用 `backend/.venv/Scripts/python.exe`。已安装的项目在其他工作目录也能导入 `app`，不需要在业务代码中修改 `sys.path`。

在 `backend` 下运行：

```powershell
python -m uv sync --locked --link-mode copy
.\.venv\Scripts\python.exe -m uvicorn app.main:create_app --factory --env-file .env --reload --host 127.0.0.1 --port 8000
```

旧包导入和旧启动命令不再使用。计算公式、请求/响应结构、接口 URL、环境变量及数据库表名不变。
