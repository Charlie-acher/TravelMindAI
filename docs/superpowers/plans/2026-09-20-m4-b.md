# M4-B 附件参与行程（2026-09-20）

范围沿用用户已接受的09第12.1节及本轮“继续下一阶段”：附件用途按轮次解释，复用M4-A的同一规划图、预算规则、事务、摘要和撤销，不新增规划器或数据库表。

## 执行顺序与验收

1. 用途合同与统一理解：read/reference/required/replace/unclear、是否要求生成草稿、目标日；只依据用户消息，不执行附件指令。验证缺省兼容、参考不变硬约束、用途不明不改草稿。
2. 证据与规划：将附件地点原文作为私有来源送入现有PlanTools，必经/替换地点逐一通过同城地图核对；程序检查必经覆盖、已知顺序、指定日范围，任何缺口不提交半份草稿。未知顺序按必经清单处理并保留提示，不猜测原路线。
3. 聊天与历史：显式附件消息和紧接的用途补问复用本会话快照；保存原始请求附件编号，保证补问重试仍幂等。参考/读取不改旅行条件；规划未成功时保留旧条件和草稿。新建行程条件缺失时先登记用户明确给出的条件，供下一轮补齐。
4. 页面与集成：保留用户附件在气泡上方，识别证据与规划结果分开说明；历史恢复用途，不展示过时“本轮不改行程”提示。验证指定日修改、预算、冲突、撤销、刷新。
5. 跑相关规则、真实隔离PostgreSQL、前端构建，尝试已配置模型/地图的小样本验收；如外部服务受阻，精确记录而不冒充实测。

## 实施裁定

- 当前codex/m4-a已提交且干净，继续当前开发分支，不移动正在使用的服务或另开缺少本地配置的工作树。
- 用户已接受参考/必经/替换及复用既有规划图，本轮是执行既定阶段，不重复索要该范围授权。先测试后实现，最后整体审查；不自动提交或推送。
- 附件来源保存在行程JSON和轮次JSON，仅当前会话可访问，不进入共享documents/Milvus。JSON增加可选字段，旧历史可读。

## 进度

- [x] 合同与失败用例
- [x] 同图规划与规则
- [x] 聊天、历史和页面
- [x] 验证、审查与文档

结果：相关28项实库集成通过；全量725通过、2项Milvus实连失败（容器启动leader changed，保留数据），Ruff/mypy 93文件及前端回归/构建通过。真实文本模型、百度地图和PostgreSQL完成读取/参考/替换、预算保留、幂等和撤销，浏览器历史、来源及刷新恢复通过。审查两项覆盖风险已修复并复验。源码合同与限制见08第12.12节；真实图片到新行程、Milvus恢复及M4-C不标记完成。

## 验收修复追加（2026-09-20）

已按用户选择落实集中追问人数预算、暂无票务服务时待查询；发送即清空、PDF完整视觉读取、RAG城市限定与规划失败提示、单点替换和补问恢复已修复。替换模式可从整篇攻略选点，只有required强制全点必去。Milvus保留原卷恢复，完整后端754通过，前端回归/构建及类型检查通过。真实16页PDF识别结果分段接入正式链生成长沙三日草稿，浏览器核对发送中清空和查询入口；边界见08第12.12。本轮不提交、不推送，M4-C仍未开发。


## 首次图片全链补充验收（2026-09-20）

已补齐清晰四景点PNG的浏览器首次上传、MinerU/Qwen识别、地图核对、两日草稿、指定日修改、撤销及刷新；只读、城市冲突、消息/撤销幂等、跨用户/会话及损坏图片边界通过。保留白堤同名歧义拒绝样例；修复一致路线顺序丢失及精确原名与别名碰撞，均有先失败再通过回归。最终后端801通过、0跳过，mypy 94文件、范围Ruff及前端回归/构建通过。完整证据与限制见08第12.16节，不外推复杂图/长PDF稳定率，M4-C仍未实现。
