# M5-B Implementation Plan

**Goal:** 文本业务接入有限重试、并发限制、熔断与降级网关。
**Architecture:** 请求持有适配器/HTTP连接，应用持有共享健康状态；GatewayClient适配原业务协议。
**Tech Stack:** Python 3.11、标准库锁/deque、现有Pydantic/HTTPX/LangChain。
**Spec:** ../specs/2026-09-21-m5-b-design.md

当前会话直接执行；保留M4/M5-A工作区变更，不自动提交推送。

- [x] 先写失败测试：503→重试→备用、401不重试、拒绝/400不切换、流中断不切换、并发/半开限制。
- [x] contracts/config补能力与路由配置，providers保留Retry-After；gateway实现共享状态、权重选择和有限执行。
- [x] GatewayClient接入API三个调用入口；文档回答/审查选择对应能力，原生工具model继续DeepSeek。
- [x] 管理员状态接口验证鉴权与脱敏；新增业务适配与真实HTTP契约回归。
- [x] 独立审查、类型/静态/后端全量验证；正式.env已配置提供方低成本smoke。
- [x] 同步03/04/07/08与.env.example，记录实测边界，清理本阶段临时文件。

最终954项后端通过、0跳过，102文件mypy、修改Ruff通过。半开并发P2先复现后修复并复查。真实flash与Qwen三种调用通过，独立进程注入主失败后真实Qwen接续成功；正式路由保持DeepSeek。用户确认修正.env型号为deepseek-flash，服务已更新，真实Cookie/SSE/PG历史与幂等通过，专用数据已清理。详见07/08本轮记录；M5-C及其后的全阶段验收仍待继续。
