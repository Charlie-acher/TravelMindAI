# M5-A Implementation Plan

**Goal:** 完成四提供方统一文本适配器，保持已验收M4的调用行为。
**Architecture:** 使用现有ChatOpenAI、显式独立配置与公共契约；保留DeepSeek业务封装。
**Tech Stack:** Python 3.11、Pydantic、httpx、LangChain、pytest。
**Spec:** ../specs/2026-09-20-m5-a-design.md

按用户已授权的下一步在当前工作区直接实施，保留未提交M4改动；不自动提交或推送。

## 任务1：适配契约、配置和公共实现

- [x] 新增tests/test_provider_adapters.py，共用四家参数化HTTP模拟；先运行确认缺少接口而失败。
- [x] config.py定义ProviderSettings与model_providers嵌套配置；llm/contracts.py定义请求、结果、事件与错误；llm/providers.py实现complete、stream、配置池和公共ChatOpenAI构造。
- [x] 验证参数差异、JSON对象与截断、usage、未配置排除、错误脱敏、流中断已有正文标记；修至通过。

## 任务2：兼容接入与验收

- [x] DeepSeekClient只切换模型构造，保留业务方法及错误类型；公开.env.example说明独立配置。
- [x] 运行新旧模型相关测试、mypy和修改范围Ruff；全量后端测试含现有数据库隔离夹具。
- [x] 只读取正式.env配置状态，对已配置适配器执行公开文本/JSON/流式smoke；记录真实模型与结果，未配置者标记未验证。
- [x] 同步03/04/07/08，记录后续网关范围及真实验收边界；清理本阶段停止使用的临时日志。

完成证据：107项适配器测试、全量925项后端通过且0跳过；mypy 100文件及修改范围Ruff通过。真实DeepSeek文本/JSON/SSE通过，另外三家未配置。独立审查两处P2先复现后修复并复查通过；详见07本轮记录。当前服务保持M4运行版本，未提交推送。

## 审查重点

不复用视觉/向量密钥；不泄漏提供方错误正文；流已输出不可安全切换；未配置提供方不影响启动；现有DeepSeek JSON修复和原生工具绑定不被改变。
