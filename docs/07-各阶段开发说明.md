# 各阶段开发说明

## 历史计划：三模型选择与主从四智能体（2026-09-21，已被替代）

> 用户随后批准体验优先方向；当前实现、逐文件说明与验收见本文末尾同日记录和08第12.20节。以下仅记录实施前的旧计划。

本次按用户要求修改计划，未修改程序、数据库或backend/.env，未重启服务。此前M5-B的954项通过是历史实现证据，不覆盖本次待开发功能。

核对现有调用链后确认：当前是持久工作流＋行程/周边两个原生工具循环＋单次模型审核，尚无Coordinator委派三个独立子Agent。generate/review/wait/publish不能计作四个独立Agent。M4-C原恢复验收保留，新增M4-D落实主从协作。

逐文件调整：

1. `superpowers/specs/2026-09-21-model-choice-and-four-agents.md`：记录用户目标、源码差距、四角色、输入框位置、会话选择、故障/取消/幂等和验收条件。
2. `superpowers/plans/2026-09-21-model-choice-and-four-agents.md`：拆为M5-C三模型全调用路径、M5-D选择器与快速切换、M4-D独立委派、联合验收，列出文件和测试；实现项均未勾选。
3. `02-技术设计文档.md`：目标改为Coordinator委派Research/Planner/Reviewer，模型改为DeepSeek/Kimi/Qwen；首选优先替代加权随机目标。
4. `03-功能模块实施文档.md`：更新阶段与完成标准，新增M4-D；原生工具路径必须随选择切换。
5. `04-项目进度文档.md`：纠正“M4完成”的适用范围，保留已有测试和实跑证据，明确新目标待实现。
6. `08-架构设计说明.md`：列出现有实现、缺口及待实现模型契约；本次无字段/迁移变化。
7. `09-双角色与对话附件改造方案.md`：同步M4-D及三模型阶段关系。

目标调用关系：输入框选择 → 后端冻结本轮选择 → 主智能体按需委派研究/规划/核对 → 程序校验并幂等保存。四角色共用该选择，故障才切备用并显示实际切换；视觉与Embedding仍用各自专用配置。

本轮检查文档链接、阶段名称和目标/现状边界；未执行新功能测试或浏览器选择器验收。MiniMax历史源码尚在，按新计划实施时退出产品入口及专用分支。

## M5-B：文本业务网关、并发与熔断降级（2026-09-21）

本步承接已完成M5-A，交付extract/research/review/chat四类文本业务接入。原生行程与周边工具仍通过GatewayClient.model取得原DeepSeek模型，不能把文本降级计为工具降级。设计和实施清单位于`superpowers/specs/2026-09-21-m5-b-design.md`、`superpowers/plans/2026-09-21-m5-b.md`。

### 开发顺序与逐文件说明

1. `backend/tests/test_model_gateway.py`先定义失败测试，再实现重试、备用切换、拒绝/参数错误禁止切换、流已输出禁止拼接、共享并发和半开探测。时钟与抽签可注入，测试不用睡30秒或依赖抽签碰运气。
2. `backend/app/llm/contracts.py`新增Capability、线程/异步上下文范围、请求能力与安全Retry-After值；`backend/app/config.py`新增GatewaySettings：路由、权重、并发、窗口、阈值、冷却与等待上限。默认仍只DeepSeek，其他提供方必须明确列入能力路由。
3. `backend/app/llm/gateway.py`中GatewayState属于应用进程，锁内获取名额并计算权重/成功率/延迟分数；ModelGateway属于请求，持有适配器与HTTP连接，每家最多两次尝试。释放名额放finally，用户关闭迭代器不会泄漏名额。统计窗口按“一次提供方执行，含重试”计量，不保存提示词或错误正文。
4. `backend/app/llm/providers.py`保留Retry-After数字或HTTP日期，过滤未授权提供方；`backend/app/llm/client.py`只抽出既有JSON历史整理函数共用，旧DeepSeek原生工具行为保留。
5. `backend/app/llm/gateway_client.py`维持generate_json/generate_text/model协议：JSON按当前能力走网关，自然回答走chat；SSE正文继续使用reset/draft，提供方错误转为既有业务异常。新配置的未授权提供方不会构造客户端；.model明确仍是原生DeepSeek。
6. `backend/app/main.py`创建唯一app.state.model_gateway；`api/requirement/routes.py`和`history.py`让普通HTTP、已存会话HTTP、SSE工作线程以及附件长文本抽取共用该状态。状态接口反映已授权提取模型，新增`GET /api/v1/requirements/gateway-status`仅管理员可访问。
7. `services/document/answer.py`在资料JSON调用中标research，`services/itinerary/review.py`独立审查标review，退出自动恢复上下文；其余需求/附件JSON默认extract。业务字段、引用和旅行规则校验仍在原业务层。
8. `tests/test_gateway_client.py`验证真实SDK＋模拟HTTP降级、能力隔离、管理员边界及错误脱敏；原test_chat_streaming改为替换新的客户端入口，保留断线后工作线程继续的断言。test_provider_adapters增加Retry-After契约。`.env.example`公开路由和并发参数，不提交正式.env或密钥。

顺序是“HTTP入口创建客户端 → 业务标记能力 → 文本网关申请共享名额 → 提供方适配器 → 原业务schema/引用校验 → M4持久化”。数据库、会话和前端结构不变。fallback事件仅携带原别名、目标别名与错误类别；当前页面不新增专门切换提示卡。

### 修复与真实验证

独立审查发现旧并发请求释放可能清掉半开探测状态：已先复现失败，再改为旧名额全部结束后才允许唯一探测；旧请求结果只记指标，不提前关闭熔断。复查确认旧请求成功、失败、取消均不破坏新状态，鉴权错误仍禁用提供方。

09:00最初正式配置deepseek-v4-pro通过网关文本/JSON/SSE。随后用户更新.env为`DeepSeek-V4.1-Flash`并加入Qwen，真实HTTP返回400，网关不重试；服务端明确拒绝该型号。经用户选择，仅把DeepSeek型号改为`deepseek-flash`，保留其他配置。真实FastAPI TestClient调用`/requirements/messages`返回200、杭州、needs_clarification，管理员统计记录一次成功；该小样本用真实模型、替换身份依赖、禁用数据库，不冒充浏览器或真实Cookie验收。

09:04:51起使用正式.env做公开短提示：deepseek-flash文本0.80秒/10tokens、JSON0.79秒/62tokens、SSE0.71秒/10tokens；qwen3.8-max文本0.63秒/18tokens、JSON0.85秒/51tokens、SSE0.66秒/18tokens，内容正确。独立进程临时授权chat路由两家并注入DeepSeek unavailable：主提供方尝试2次后切到真实Qwen成功，fallback_count=1。没有制造真实供应商故障，也没有发送用户附件；不是P95生产指标或降级率评测。

正式路由未改，依然只DeepSeek；新Qwen配置已可用但未自动加入正式候选。Kimi/MiniMax尚未配置与真实验证。M5-C原生工具路由、schema统一修复、生产隔离的演示开关及正式故障成功率评测仍待继续。

### 最终验收与运行状态

最终后端全量 **954 passed、0 skipped、44 warnings，117.90秒**，包含隔离PostgreSQL/Milvus、M4工作流与既有SSE回归；102个源码文件mypy及修改范围Ruff通过。警告仍为既有Starlette弃用、MCP beta与内层无checkpointer时durability提示，不称无警告。原生工具范围未改，不把本次文本试跑算成四提供方工具验收。

确认正式库无queued/running资料任务、无会话advisory锁、8000端口没有活动连接后，替换本地后端运行进程；当前加载新M5-B源码和deepseek-flash。本步无数据库迁移。真实HTTP注册专用账号、Cookie认证、创建会话后发送“想去杭州玩两天，请先问我出发城市和预算”：SSE依次progress→done，revision=1、needs_clarification、workflow=waiting；GET恢复一轮历史，同message_id普通HTTP重试返回完全相同结果，未重复保存。普通账号访问网关统计返回403。未重新做浏览器视觉验收，前端没有代码变化。

验收脚本第一次误读会话创建响应外层`id`而中断；实际接口是`session.id`，不是产品缺陷。该次仅创建的空会话和账号已按精确归属清理；修正脚本后真实链路通过，第二个专用会话经受认证删除接口清理、账号及登录令牌清除。未操作用户原有会话。

阶段清理：结果已写入本文后清理两轮M5-B测试日志及已停止M4-C服务日志；保留当前`temp/logs/m5b-backend.log`、`m5b-backend-error.log`、原上传和备份，不删除目录。代码仍未提交或推送。

## M5-A：统一文本模型适配层（2026-09-20）

本步按03第8节继续推进，先完成四家调用基础。共用现有LangChain/OpenAI兼容SDK，不新增依赖，不直接切换M4业务提供方。设计与实施清单见`superpowers/specs/2026-09-20-m5-a-design.md`、`superpowers/plans/2026-09-20-m5-a.md`。

### 开发顺序与文件职责

1. `backend/tests/test_provider_adapters.py`先写四家共用的HTTP契约测试，确认缺少ProviderSettings导致失败；补充用例又复现了响应模型名被配置别名覆盖、过滤流块先发正文的问题，再修至通过。
2. `backend/app/config.py`新增ProviderSettings，独立保存模型、端点、密钥和有限正数超时；`model_providers`只收Qwen/Kimi/MiniMax，DeepSeek沿用既有变量并新增可配端点。双下划线环境变量逐项读取，密钥为空者不入池；有密钥但配置不完整明确报错。
3. `backend/app/llm/contracts.py`定义ModelRequest、ModelResult、ModelStreamEvent、ProviderError；它规定调用的共同格式，不负责发送网络请求。错误仅包含提供方、类别、是否已输出正文，原始请求和服务端错误正文不进入展示消息。
4. `backend/app/llm/providers.py`读取配置，通过build_chat_model创建ChatOpenAI，再由ProviderAdapter.complete/stream发送一次请求。四家参数分支在这里集中处理：DeepSeek/Kimi关闭思考，Qwen使用enable_thinking=false，MiniMax分离推理；JSON统一检查为对象，MiniMax使用提示约束而非假设原生JSON mode。token缺失保留null，正常结束才有completed事件，中途出错携带started状态。新适配器启用CheckedChatOpenAI，在SDK转换丢字段前处理MiniMax业务错误/过滤和流式refusal；余额不足独立归quota_exceeded。旧业务构造默认不开此检查，保留既有错误类型。
5. `backend/app/llm/client.py`只复用公共模型构造，保留generate_json、generate_text、既有业务错误和原生工具调用接口；`.env.example`给出独立文本配置示例。网页仍通过DeepSeekClient执行业务，不会因填写另三家配置就自动发送聊天或附件。

调用顺序：配置 → 公共模型构造 → 原DeepSeek业务方法，或者新适配器complete/stream → 统一结果。当前新适配器的统一结果尚未替换业务服务的字符串返回值；后续ModelGateway负责连接两者，并覆盖原生工具调用的选择与故障策略。

### 真实验证与边界

2026-09-20 17:53（Asia/Shanghai），从正式`backend/.env`读取文本配置，仅DeepSeek可用。使用公开短提示验证`deepseek-v4-pro`：文本1.01秒/10 tokens、JSON 1.17秒/59 tokens、流式0.92秒/10 tokens，内容与流式拼接一致。没有上传用户附件或复用视觉/向量密钥。Qwen、Kimi、MiniMax未配置文本密钥，未做真实请求；契约通过不能代表任意型号都支持这些参数。

独立审查复现两处P2：MiniMax顶层业务错误被SDK丢弃、流式delta.refusal未保留。均先补真实SDK＋MockTransport失败测试，再在原始响应转换处修复；107项适配器测试通过，复查无新增P1/P2。17:59对最终适配器再次真实验证：文本0.94秒、JSON 1.18秒、流式0.75秒，型号仍为deepseek-v4-pro；此为短提示小样本，不是生产延迟统计。

最终自动验证：正式.env提供连接地址、测试使用隔离数据库schema及Milvus夹具，后端全量 **925 passed、0 skipped、44 warnings，111.81秒**；100个源码文件mypy通过，修改范围Ruff与git diff --check通过。44条警告为既有Starlette弃用、LangGraph内层无checkpointer时durability提示及MCP beta，不将它们写成无警告。此次无前端修改，未重复浏览器验收或前端构建。现有后端live/ready和前端页面均HTTP 200；运行服务未重启，因此不将新源码称为已部署。

阶段清理：两轮M5-A测试日志在摘要写入本文后按具体文件移除；不删除目录，不清空temp，保留M4运行日志、上传原件和备份。代码未提交或推送。

本步不改数据库/会话结构，不改前端，不重启现有运行服务。M5后续仍需能力registry、业务入口切换、负载/并发、重试、熔断、跨提供方降级、schema校验修复和开发故障演练。逐段路线、实时票价属于其他业务范围。

## M4-B 首次图片到新行程验收（2026-09-20）

本轮补齐真实浏览器首次上传图片、识别、生成新草稿的验收，使用专用账号与自制杭州路线图，没有复制已有识别缓存。先保留失败样例，再复现并修复两处问题。

1. `tests/test_attachment_analysis.py`先证明OCR与视觉对完整路线顺序一致时仍被合并函数清空；`services/attachment/analysis.py`仅在各份结果的完整地点及顺序一致时保留顺序。冲突、缺项或未知顺序继续不猜测，原输入不改写；四项新增回归覆盖这些分支。
2. `tests/test_baidu.py`复现“花港观鱼”精确原名与“花港公园”的同名别名被同等处理造成的误歧义；`services/baidu.py`先筛同城、主体类别与名称，再优先精确原名。多个精确同名仍补问，无精确原名才沿用已有别名及严格后缀规则。

调用关系不变：附件读取先调用识别结果合并，把私人地点依据交给同一规划图；规划工具调用百度地点核对，规则检查通过后由原短事务保存。没有新增表、迁移、依赖或规划器。

真实浏览器：全新会话上传四景点PNG，发送前HTTP核对`status=uploaded`、`analysis=null`及空历史；发送后输入与附件选择立即清空。实际MinerU、DeepSeek、百炼Qwen与百度MCP完成首次识别与规划，页面显示31秒完成，v1按顺序安排第一天断桥残雪→平湖秋月、第二天雷峰塔→花港观鱼，均9点开始，总预算4000元。第二天修改为11点生成v2，HTTP逐字段比较确认第一天及预算未变；原消息重试返回同一快照且revision不增加。浏览器撤销生成v3，其完整计划和旅行条件等于v1；刷新并重新进入规划页恢复三轮历史与v3。

边界检查：跨会话提交附件编号返回404，另一普通用户读取历史或原件返回404；损坏PNG返回422且未新增附件。成都附件只读请求保存识别结果，但不改杭州旅行条件或草稿；紧接要求用成都路线替换第二天时因城市冲突补问，数据库仍只有v1～v3三个草稿，完整旅行条件不变。撤销请求重放返回原v3，revision仍为5。上传原件回读SHA-256一致，四个活动均有该附件来源及百度BD-09坐标，完整解析正文只在私人附件缓存中，未生成共享documents记录。第一张六点路线因百度返回两个精确同名“白堤”（景点及道路）而补问，未生成半份计划；该样例保留为预期拒绝，不能计入成功规划。

自动验证：最终后端全量801通过、0跳过，真实隔离PostgreSQL/Milvus，保留两条既有依赖警告；mypy 94个源码文件、修改范围Ruff通过。前端三组Node回归及Vue类型检查/生产构建通过，测试启动时有既有HMR端口占用提示但退出码为0。两处修复均先见到断言失败再复验通过。

验收范围：补齐清晰小图片到新行程的小样本端到端证据，不外推任意图片、复杂攻略稳定率或16页大PDF一次浏览器请求成功率。原有PDF分段验收边界保留。M4-C独立审查、有限返工、interrupt及持久恢复仍未实现。当前合同见08第12.16节。

收尾：独立只读审查核对两处修补及调用链，无阻断发现。正式8000已加载修补，前端5173、后端PostgreSQL/Milvus及MinerU8010健康检查均通过。四份专用会话、两名专用账号及其附件副本已清理，本轮临时脚本、报告和已关闭的日志已删除；只留下空验收目录，未执行目录删除。当前运行日志保留在`temp/logs/m4b-e2e-backend-final-20260920.log`及同名前缀`-error.log`。用户原会话、上传原文、数据集、备份和运行模型未改动；本轮未提交或推送。

## 混合检索与同节原文展开（2026-09-20）

本步改善点名地点漏召回及片段上下文不足；保留Milvus，不重建索引，不增加数据库表、迁移或依赖。普通聊天和行程共用检索增强，只有行程Agent新增按需展开工具；普通知识问答的多轮补查和私人PDF按页读取不在本步实现。

1. `tests/test_document_hybrid.py`先复现名称漏召回和缺少展开工具，验证归属、城市、类别、停用资料和原文位置。随后增加两字中文问句、城市属于专名、标题附带记录键、父地点与子项目排序、重复同名章节及原生工具闭环回归。
2. `schemas/document/search.py`定义内部`DocumentContext`；`services/document/search.py`先用共同范围筛选资料，再从PostgreSQL按完整词、中文双字片段及名称召回，与Milvus候选按倒数排名合并；明确点名的完整名称优先。标题命中继续回查本节正文，编号和正文均去重。向量运行故障时已有词法证据仍可返回，无词法依据则保留原错误；缺失配置仍沿用原依赖层检查。
3. 同一`search.py`提供`read_context`：重新核对归属、parsed状态和城市，只在命中点附近读取最多五段连续同节正文。同名标题再次出现即停止；无标题只展开原文单元，不能把相邻PDF页或整份TXT当成同一节。保留原片段编号、页码和字符范围，超出范围明确`truncated=true`。
4. `schemas/itinerary.py`为`PlanSource`增加可空原文编号、片段编号、页码及默认空标题路径；旧草稿直接兼容。`itinerary/tools.py`用本轮来源编号调用原文展开，保留实际出处；`itinerary/graph.py`在首次共享检索后才开放`knowledge_read`，继续共用六轮模型决策、十二次查询限制。检索分配新来源编号，展开遇到已返回片段时复用编号。
5. `api/document/search.py`与`frontend/src/api/documents.ts`同步score含义：它是混合排序分，不再是余弦相似度，HTTP字段形状不变。`test_document_metadata.py`调整对应分数断言，其余原有隔离和排序回归保留。README与08第10.32节同步功能、调用图及JSON字典。

验证：最终后端全量795项通过、0跳过，使用真实隔离PostgreSQL及Milvus，模型按用例替换，保留两条既有依赖警告；mypy 94个源码文件和修改范围Ruff通过。只读审查后独立27项回归通过。真实共享库、阿里云Embedding与Milvus四个问题对比：苏州博物馆第4→第1，西湖游船与灵隐寺保持第1，老人轻松游前五候选不变。同查询共用真实向量，不修改资料或会话；检索阶段旧实现27～89ms、新实现156～238ms，不含Embedding，单次样例不是性能基线或总体准确率。真实章节展开返回同一文档两段、317字；模型主动选择新工具由原生框架替身闭环验证，尚未据此声称真实模型/浏览器完成验收。

运行交付：正式8000后端收到正常关闭信号并退出后，按README命令后台重启；live/ready均为200，PostgreSQL/Milvus均ready，运行OpenAPI已包含新增来源定位字段。当前日志为`temp/logs/retrieval-backend-20260920-152723.log`及同名前缀`.err.log`，保留运行日志。本轮未生成待清理的验收脚本或报告文件；不清理其他任务文件。

## M4-B 已识别景点在后续修改中丢失（2026-09-20）

用户反馈PDF第5页明确有岳麓山，追加“第二天还想顺便去岳麓山”却被告知资料没有。只读检查该轮数据确认：上一轮附件快照及原件成功缓存均含“岳麓山”，共84个候选；本轮`attachment_use=null`进入普通修改路径，仅旧活动来源被交给规划图。这是候选接续问题，不能据回复推断OCR漏识别。

1. `services/chat/attachments.py`新增`plan_attachment_candidates`，按当前草稿已引用的附件编号，从同会话历史回查最新有效完整快照。`chat/service.py`在普通规划修改路径传入这些候选，作为参考而非必经点；换目的地、新草稿及从未采用的附件不自动混入。跨一轮闲聊也能回查。没有新增数据库字段或迁移，不重新OCR，不改用户历史。
2. `tests/test_attachment_chat.py`先复现即时追问及隔轮追问的资料缺失，再验证完整候选传给规划器，以及无关附件、其他目的地和空草稿隔离。
3. 真实重跑发现第二处独立阻断：百度返回“岳麓山国家重点风景名胜区”，`services/baidu.py`原后缀规则不能匹配“国家重点”。扩展严格末尾景区称谓，继续要求同城、唯一主体并排除附属设施。`tests/test_baidu.py`复现旧匹配失败，并验证东门、停车场和码头不被认成岳麓山主体。
4. `services/itinerary/graph.py`区分已识别但地图未匹配与原文缺失，不能让用户重新上传已经读清的页面。PaddleOCR、PP-StructureV3、PaddleOCR-VL及MinerU属于识别增强候选，本次没有安装或替换识别引擎，也不承诺任意PDF百分百准确。

真实验证：读取用户该轮已存状态，仅在内存中用原话“第二天还想顺便去岳麓山”调用真实DeepSeek、百度地图及同一规划图；未向用户会话追加消息或修改草稿。地图返回found，主体为“岳麓山国家重点风景名胜区”；规划第二天为岳麓山、岳麓书院，第一天和第三天对象与旧稿完全一致。结束后再次读库，版本及完整旧稿不变。不重复OCR，不把该样例的通过说成整份PDF或任意PDF百分百识别。

自动验证：全量767项通过、0跳过，真实隔离PostgreSQL和Milvus，2条既有警告；修改范围Ruff和mypy 93文件通过。前端未改动。正式8000服务已重启，数据库及Milvus健康检查均ready，运行日志位于`temp/logs/m4-attachment-candidates-backend*.log`。

## M4-B 纯附件接续规划与回复精简（2026-09-20）

1. `schemas/requirement/history.py`允许有附件的消息保留空文字，文字与附件同时为空仍拒绝；`useRequirementConversation.ts`取消“请帮我读取这些附件”的自动补文，`App.vue`不渲染空用户气泡。发送即清空、失败恢复和幂等编号沿用原流程。
2. `services/chat/service.py`识别“已有规划条件、尚无草稿、上一轮待补充”的接续状态，传给`requirement/extract.py`。此时纯附件发送直接采用已确认条件、按参考资料继续规划，不再次调用用途理解模型；条件缺失仍由原规则集中补问。其他上传结合历史理解用途，提示规则写在`requirement/prompt.py`，明确仅读取仍保留。
3. `services/chat/context.py`仅在模型历史中用附件事件说明补足空原话；`requirement/history.py`首次纯附件消息以文件名生成标题。用户原话、恢复历史与幂等比较始终保留空字符串，不新增表或迁移。
4. `services/attachment/reader.py`将非规划附件回复收敛为简短状态；`AttachmentCards.vue`只展示原件和已采用状态，不再展示长概述、证据及待核对清单。`itinerary/graph.py`要求参考资料按当前目的地择取可核实候选，回复聚焦行程，只有阻止安排的缺项才补问。原件、识别缓存和地图核对规则继续保留。
5. `test_attachment_chat.py`增加空消息合同、已知条件接续、缺项补问及内部证据不外显回归；`test_attachment_plan_history.py`验证真实隔离PostgreSQL中空原话、两日草稿、恢复、幂等及首轮文件标题；`frontend/tests/attachments.mjs`验证空文字请求、空提交拒绝及卡片不泄漏识别清单。

自动验证：后端全量760项通过、0跳过（真实隔离PostgreSQL和Milvus），2条既有警告；mypy 93文件、前端三组Node回归及类型检查/生产构建通过。首次新PDF的视觉读取耗时未在本步优化；成功缓存可复用，不能把缓存验收当作首次OCR速度。

追加用户反馈“系统负责核对”：`services/chat/attachments.py`将模型/工具异常记录为后台请求编号和错误类别，用户侧说明生成服务出错，不再称“未完成核对”；`itinerary/graph.py`明确附件已有候选时优先地图核实，不因公共库缺少该城市就索要同一攻略。最终相关33项回归、修改范围Ruff及mypy通过。

真实验收：在专用账号预置三人、银川至长沙三天、4000元、轻松节奏及等待上传的对话，复制同一原件已有的84点成功识别缓存到专用会话，不更改用户原记录。浏览器选择19.6MB《长沙.pdf》并仅点发送：输入与附件选择立即清空，用户消息只有可打开的附件卡片。第一次真实规划调用21秒后失败，诊断重试成功保存三日草稿，浏览器刷新可恢复岳麓书院、橘子洲、湖南省博物馆、黄兴路步行街等地图核实地点及已采用附件状态，没有84点清单或原文证据倾倒。第一次失败未重现，不能据单次成功声称模型稳定性已充分验证；成功路径使用真实DeepSeek、Embedding/Milvus和百度地图，识别阶段使用缓存，不重复评价OCR耗时。

交付状态：正式8000服务已重启加载本步修改，PostgreSQL/Milvus健康检查均ready；本次专用账号、会话、附件副本、验收脚本和元数据已清理。用户上传原件、原会话及Milvus恢复备份保留，运行中服务日志位于`temp/logs/`。

## M4-B 验收问题修复（2026-09-20）

用户确认：缺人数和总预算时集中补问，补齐再生成草稿；没有实时票务服务，标注待查询并保留入口。以下是本轮新增修复，前一节验收数字属于当时快照。

| 顺序 | 文件 | 职责与调用关系 |
|---|---|---|
| 1 | `frontend/src/useRequirementConversation.ts`、`tests/attachments.mjs` | 发送时立即清空输入和已选附件，待发送消息保留原快照；失败仅在没有新草稿时恢复，账号切换隔离迟到响应 |
| 2 | `backend/app/services/attachment/analysis.py`、`reader.py`、`llm/vision.py`、`pyproject.toml` | PDF文字层不全或无法核对时，以PDFium渲染全部页面，再分批交给Qwen；每张图片标明物理页码，不能回查的地点转明确提醒，不把部分批次成功当全文成功 |
| 3 | `services/requirement/prompt.py`、`extract.py`、`services/chat/service.py` | 明确做攻略走plan，缺项集中补问；只对本轮选中文件提供参考用途兜底，旧附件由用户指代决定；补问理解失败仍保留附件；规划失败不退成看似完成的自由攻略 |
| 4 | `schemas/attachment.py`、`services/itinerary/attachments.py`、`graph.py`、`tools.py` | 单点替换定位旧点所在日并保留其他活动；替换从文档选点，required才要求全点必去；所有新点需附件证据及地图核对。知识检索限定目的地并携带偏好，提示每日节奏上限 |
| 5 | `services/itinerary/rules.py`、`services/chat/conversation.py`、`frontend/src/components/ItineraryCard.vue`、`App.vue` | 标明票务及天气待查询，保留天气/逐日路线问题入口和12306、国航官方链接；按钮只填问题，待用户补充并发送；预算继续标明演示估算 |
| 6 | `frontend/src/api/requirements.ts`、`tests/requirement-stream.mjs` | 普通请求180秒；显式附件或收到attachment事件的请求总期限15分钟，仅延长一次，结束清定时器 |
| 7 | `deploy/milvus/compose.yaml`、`README.md` | etcd先健康再启动Milvus，沿用原卷和同版本etcd数据；保留恢复前备份，不重建索引 |

数据库仍为0012，无新表及SQL字段。真实数据检查：知识库当前覆盖10个城市，没有独立长沙攻略；城市过滤的长沙检索为空。补齐有来源的城市资料优先于新增玩法标签，私人附件不会自动共享。完整合同与验收结果见08第12.12节。

最终验证：后端754项通过（真实隔离PostgreSQL/Milvus、0跳过），mypy 93文件、修改范围Ruff、三组前端回归及构建通过。完整16页Qwen识别约308.6秒，121个候选及9条提醒保留；真实缓存接入正式链，人数预算补齐后生成长沙三日草稿。无附件杭州经真实RAG及地图后生成两日草稿。浏览器查看真实历史、路线入口及发送中立即清空状态；后端8000已更新并通过就绪检查。识别与规划为分段实测，不称单次浏览器全链已跑完；没有实时票务数据，动态查询与演示预算边界保持明确。

## M4-B 附件参与同一份行程（2026-09-20）

本步把D的识别结果接入现有M4-A规划图。用户可自然说明“仅供参考”“这些点都要去”或“用附件替换第二天，第一天不变”。用途不明先补问；只读取或仅供参考且未要求规划时，不改变旅行条件和草稿。

| 顺序 | 文件 | 职责与调用关系 |
|---|---|---|
| 1 | `schemas/attachment.py`、`schemas/requirement/conversation.py`、`services/requirement/extract.py`、`prompt.py` | 增加本轮用途、是否规划和目标日；沿用统一理解调用，只提供附件名称、摘要和地点，不从文件指令推导用户授权 |
| 2 | `schemas/itinerary.py`、`services/itinerary/attachments.py` | 私人来源保留附件编号和地点原文；检查城市、替换范围、必经覆盖、已知顺序和目标日 |
| 3 | `services/itinerary/tools.py`、`graph.py` | 将来源送进同一个PlanTools和规划图；必经点先地图核对，旧地点复用时复制来源、不修改旧对象；提交仍经过原预算、时间和排除项规则 |
| 4 | `services/chat/attachments.py`、`chat/service.py` | 按用途读取、补问或规划；紧接的补问复用上一轮识别快照；生成失败保留已有条件和草稿，没有可接续附件时不转入普通规划 |
| 5 | `schemas/requirement/chat.py`、`services/requirement/history.py` | 保存实际请求的附件编号，区别显式上传和隐式沿用；沿用原会话事务、版本冲突、幂等重试和撤销，不增加数据库表 |
| 6 | `frontend/src/api/attachments.ts`、`api/requirements.ts`、`useRequirementConversation.ts` | 类型与历史恢复增加本轮用途和附件来源 |
| 7 | `App.vue`、`AttachmentCards.vue`、`ItineraryCard.vue` | 提示可自然说明用途，展示目标日和是否用于草稿；行程依据显示“私人附件”，保留原识别快照和用户气泡上方的文件卡片 |
| 8 | `test_attachment_planning.py`、`test_attachment_plan_history.py`、`test_attachment_chat.py`、`frontend/tests/attachments.mjs` | 验证证据绑定、顺序/范围拒绝、旧草稿保留、同库事务、补问重试、撤销及页面状态 |

验证：真实隔离PostgreSQL的附件规划/聊天/流式相关28项通过；全量后端725项通过、2项失败，失败均为既有Milvus实连测试，容器启动报`etcdserver: leader changed`，没有清空或替换向量数据。修改范围Ruff、mypy 93文件，以及前端三组Node回归、Vue类型检查和Vite构建通过。审查的“无附件误规划”和“目标日覆盖保留日”两项问题已修复并独立复验。

真实小样本使用本机配置的文本模型、百度地图和PostgreSQL：先用地图核实地点构造验收旧草稿，再上传自编杭州文字路线，依次读取→仅供参考→指定第二天11:00替换，得到v2；第一天和5000元预算不变，重复消息返回相同结果，撤销v3完整恢复旧计划。浏览器核对历史、用途、私人来源展开及刷新恢复；这些步骤不冒充浏览器发送全流程或真实图片到新行程验收。专用QA账号、会话、附件副本与临时脚本已清理，保留在用服务及日志和用户原件。本轮未重新验证Qwen图片规划、广泛规划质量、实际路线耗时或实时价格，M4-C仍未实现。当前合同和数据流见08第12.12节。

## 附件统一卡片与打开原件（2026-09-18）

先新增`frontend/src/components/AttachmentFileCard.vue`，统一文件类型、名称、大小和“已上传”状态，卡片主体可以打开原件；输入框单独保留移除按钮。`AttachmentCards.vue`复用它，在助手回复中继续展示识别结果；`App.vue`和`chat.css`把用户已发送附件放到对应文字气泡上方并右对齐，移除“发送后识别”的界面提示。设计稿同步布局和可打开的本地文件卡片，不发送真实请求。

随后在`api/attachments.ts`、`useRequirementConversation.ts`接入原件预览链接和历史附件恢复。后端`schemas/attachment.py`及`services/attachment/reader.py`给新快照增加可空的`size_bytes`；旧快照缺少大小时前端查询本人会话的附件元数据补齐，不覆盖历史识别结果、不补跑模型。`api/attachments.py`增加`preview=true`，仅允许已校验的PDF和图片内联打开，其他文件继续下载，保留归属检查和禁止缓存。无新增数据库迁移。

验证：36项后端附件存储、读取、聊天和历史测试通过；预览响应策略最终调整后单独重跑权限与内容用例通过，Ruff和mypy 91文件通过。前端附件、登录、流式对话三组回归及类型检查、生产构建通过。正式页使用专用验收账号核对上传、发送、刷新恢复和卡片打开；最新布局已在浏览器确认附件在用户气泡上方且右边缘一致。使用缓存样例验证界面，不代表真实模型识别验收。PDF接口完整返回原件，但内置验收浏览器无法渲染该PDF及独立普通测试PDF，PDF页面显示仍需在用户浏览器核对。

## 上传交互与攻略顺序容错（2026-09-18）

用户确认“拖入后上传，点击发送后识别”。先更新`designs/travelmind-chat-v1/index.html`、`chat.js`、`chat.css`：加号菜单交代格式、大小、3份上限和私人范围，卡片展示类型/文件名/大小，拖入输入框高亮。设计稿不发送真实请求。

随后修改正式`frontend/src/App.vue`与`chat.css`：同样的菜单和卡片，计数处理拖入子元素的闪烁，文件落在输入框外不导航离开，普通文字拖拽不拦截。选择与拖入共用`addAttachments`，上传中、未登录、撤销待处理等状态拒绝新增；不自动发送、不清空已有文字。`useRequirementConversation.ts`保留可选size_bytes供卡片展示，`api/attachments.ts`补齐后端已有.markdown支持，不改变消息或数据库合同。

截图中的“路线顺序不完整”来自`services/attachment/analysis.py`将不连续编号判为整份失败。多条路线的攻略可能重复编号；现在保留通过原文校验的地点，将不明确顺序统一置null并增加确认提醒，不能猜测重排。普通格式错误、无依据城市/地点仍拒绝。

验证：前端landing-auth、attachments、requirement-stream三组回归与生产构建通过；新增App拖入处理测试覆盖草稿保留、不调用识别、上传中重复拖入、普通文字拖拽、未登录隔离。后端17项附件识别/读取/聊天测试通过，Ruff及mypy 91文件通过。浏览器设计稿与正式页上传19.6MB《长沙.pdf》、菜单、卡片显示通过，控制台无错误；系统文件拖放由处理函数回归验证，未将文件选择器验收冒充操作系统拖放实测。真实模型默认网络一次异常；trust_env=False直连复测返回长沙概述、0个地点、5条提醒，不视为地点召回通过，也未改应用代理设置。正式后端已更新，QA账号/会话/副本已清理；原历史失败快照保留，需重新选择附件发送新消息再识别。

## 私人PDF附件上限调整（2026-09-18）

用户提供的《长沙.pdf》为19,574,663字节，被原10MiB前端限制拦截。本次仅将私人PDF放宽至30MB（30,000,000字节，含上限），其他附件仍为10MiB，公共知识库资料仍为20MiB。

1. `services/attachment/storage.py`按扩展名、MIME和内容共同校验，PDF允许30MB；`api/document/upload_limits.py`在表单解析前允许30MB加64KiB开销，随后由文件校验执行各类型上限。
2. `models/attachment.py`和`0012_private_pdf_size.py`将大小CHECK改为按mime_type区分，已有0011不改写。降级若存在大PDF则事务失败，保留数据。
3. `frontend/src/useRequirementConversation.ts`同步选取校验和提示；前后端回归覆盖30MB成功、多一字节拒绝、非PDF仍受旧限制、带长度和分块请求，以及数据库约束。

验证：53项附件/数据库相关测试通过，mypy 91文件、修改范围Ruff、附件Node回归及Vue/Vite构建通过。public已备份至`temp/backups/pdf30mb-20260918/before-0012.dump`后迁移0012，Alembic check无差异。本机后端已更新，真实5173代理→8000→数据库上传《长沙.pdf》返回201，原件回读SHA-256一致；仅清理专用验收账号、会话与副本，用户原文件未改动。解析得到15段、21,689字，第1页无可提取文字；本次未调用模型或增加扫描PDF OCR。

## D 私人附件理解（2026-09-18）

在M4-A基础上交付下一小步D：登录用户上传私人原件，发送后读取文字或调用百炼Qwen识图，将结果随本轮保存。附件本身不修改旅行条件或行程；M4-B再接用途与规划图。开发按合同、存储、识别、聊天、页面推进。

| 顺序 | 文件 | 职责与调用关系 |
|---|---|---|
| 1 | `schemas/attachment.py`、`models/attachment.py`、`migrations/versions/0011_conversation_attachments.py` | 定义附件展示/识别合同及独立表，关联sessions而不进入documents、片段或Milvus |
| 2 | `services/attachment/storage.py`、`api/attachments.py`、`api/document/upload_limits.py` | 上传前校验类型、体积与真实内容；会话内去重；依赖现有归属校验返回列表、详情与受保护原件 |
| 3 | `llm/vision.py`、`services/attachment/analysis.py`、`config.py` | 一个百炼视觉入口；文字复用parser与已有文本模型；校验城市、地点和原文引用，图片与地点不冒充地图核实 |
| 4 | `services/attachment/reader.py`、`services/chat/service.py` | 先检查消息幂等、版本和整组附件归属，再复用成功识别或重试失败；外部模型调用不持有数据库事务 |
| 5 | `schemas/requirement/history.py`、`chat.py`、`services/requirement/history.py`、`api/requirement/history.py` | 请求携带最多3个不同附件编号；JSON/SSE共用读取流程；保存前再次检查归属；同消息编号换附件返回409，成功轮次保存当时结果 |
| 6 | `services/trip_service.py`、`scripts/cleanup_attachment_files.py` | 删除会话前保存私人原件清理依据；事务回滚保留原件，磁盘失败允许本人重试或通过清理命令恢复 |
| 7 | `frontend/src/api/attachments.ts`、`api/requirements.ts`、`useRequirementConversation.ts` | 上传和消息发送分开；pending携带附件编号，刷新重试、切换和退出不串会话 |
| 8 | `frontend/src/components/AttachmentCards.vue`、`App.vue`、`chat.css` | 输入框选取/移除附件；历史展示识别结果、原文依据、待确认点和受保护原件链接 |

原件限制：PDF已按本页上方后续调整放宽至30MB，其他附件仍为10MiB；静态PNG/JPEG/WebP不超过2000万像素、单边10000像素；PDF须有文字层，DOCX只读正文/表格，扫描页和内嵌图片不作OCR。每次文字识别最多24000字符，超限要求拆分，不静默截断。识别失败保留原件，成功结果复用；同消息重试保持原失败/成功快照，重新选择附件发送新消息才重试识别。

本机0011已在备份后迁移，Alembic check无差异；最终全量713项通过、0跳过，mypy 91文件、修改范围Ruff及前端三组Node检查/生产构建通过。真实百炼样本、浏览器及正式服务核对结果汇总于08第12.5节。新增依赖仅Pillow，用于图片格式、像素和完整性校验。Qwen采用[百炼官方兼容接口](https://help.aliyun.com/en/model-studio/qwen-vl-compatible-with-openai)的base64图片消息。

## 知识库选材与回答排版收简（2026-09-18）

本步落实验收意见：新聊天回答不自动搜索网页，普通景点推荐只展示短导语与一份紧凑列表；真实地点/商户工具卡片继续保留地图操作。历史回答和网页证据保留原快照，不重新生成。

| 顺序 | 文件 | 职责与调用关系 |
|---|---|---|
| 1 | `services/document/search.py` | 向量命中独立标题时，从PostgreSQL回查同文件、同标题路径的紧邻正文，返回正文自己的编号与位置并去重；聊天类别筛选可兼容未分类综合攻略，管理端仍默认严格筛选 |
| 2 | `schemas/requirement/conversation.py`、`services/requirement/prompt.py` | 既有一次对话理解增加可空retrieval_category，区分景点、餐馆、住宿，不增加单独分类模型调用，旧输出缺失时默认null |
| 3 | `services/chat/rag.py` | 按城市/类别召回20段，过滤空标题与纯来源网址，按文件交替选材，再按城市交替取最多5段；取消普通及特色餐饮问答中的网页补查 |
| 4 | `services/chat/service.py`、`services/itinerary/graph.py` | 聊天入口关闭网页能力，包括规划与降级；行程Agent在无网页能力时不绑定该工具。资料回答成功后直接返回，不再多调用一次原生模型重复介绍景点 |
| 5 | `services/document/answer.py`、`places.py` | 普通推荐导语与景点介绍分工；无效票据只清除涉及收费的完整句子，保留其余介绍；未查地图的推荐不自动追问门票地址 |
| 6 | `frontend/src/components/AttractionCards.vue`、`AnswerSources.vue` | 普通推荐使用编号、城市标签、引用角标与细分隔线；相同介绍/理由不重复显示。来源按真实文件编号合并，展开保留每段编号、页码、章节和转义后的原文；旧网页仍可查看 |
| 7 | `frontend/src/App.vue`、`chat.css` | 补问放在景点列表之后并避免历史文本重复；正文改深灰，提高标题层级和可读性 |
| 8 | `test_chat_map_policy.py`、`test_document_metadata.py`、`test_chat_clarification.py`、`test_chat_rag.py`、`test_chat_recovery.py`、`frontend/tests/requirement-stream.mjs` | 覆盖不搜网页、标题回查不串景点、类别/城市隔离、跨文件选材、无效票据保留介绍、来源保存恢复、分组与新旧卡片显示；旧网页调用预期同步改为零调用 |

真实数据排查确认杭州景点文件760段已全部建索引。旧流程中PDF占据前列，独立标题命中又被聊天丢弃，导致专类正文没有进入前5段。当前无需修改原始资料或重建索引。真实模型验收中，泛问杭州景点时专类正文进入输入，但模型可只引用PDF；问“杭州西湖和京杭大运河杭州景区有什么看点”时实际引用杭州景点文件与PDF。每次为理解、回答两次JSON调用，网页与地图调用均为0。引用按实际依据保留，不承诺每轮必须出现某一文件；只读检索与模型试跑使用内存提交替身，不写用户历史。

验证结果见08节10.31；浏览器以真实模型快照加载生产展示组件验收，和正式聊天端到端提交分开记录。

## 地图按需调用与参考资料展示（2026-09-18）

普通旅行咨询现在先检索资料，生成介绍卡片时不自动补查地图，也不逐景点补搜地址。明确问地址、路线、天气或周边商户仍由已有`response_mode=map`进入工具；完整行程仍走原行程Agent。本步不增加关键词路由、地图缓存或点击定位接口。

| 顺序 | 文件 | 作用与调用关系 |
|---|---|---|
| 1 | `schemas/document/answer.py`、`services/document/answer.py`、`services/chat/rag.py` | 聊天资料链传`resolve_locations=False`，程序拦住模型额外提出的地图请求；介绍卡片保存`location.status=not_requested`，实际`map_lookups=[]` |
| 2 | `services/chat/conversation.py` | 结构化失败后也保留实际提供给模型的参考原文或网页，状态仍可为insufficient，不反造引用要点、地点或地图查询 |
| 3 | `frontend/src/api/documents.ts`、`useRequirementConversation.ts`、`App.vue` | 接收未请求定位状态，发送完成与读取历史都把同一份knowledge传给展示组件 |
| 4 | `components/AttractionCards.vue`、`AnswerSources.vue` | 普通卡片隐藏空地址和定位失败栏，展示资料编号；参考资料折叠栏展开文件名、页码、章节、原文或网页链接，纯文本转义并限制链接协议 |

验证：后端完整套件在真实PostgreSQL隔离schema中`660 passed, 2 skipped, 2 warnings`；两项缺少专用Milvus测试地址。修改范围Ruff和83个源码文件mypy通过；前端发送流、来源恢复、链接与原文安全、登录回归、类型检查及生产构建通过。真实backend/.env模型和检索链验收“想去深圳”，返回华侨城旅游度假区、观澜湖旅游休闲度假区、西涌三张卡片，地图连接0次、map_lookups为空；本次模型选用了网页依据，结果未引用本地片段，不将其记成本地知识库引用验收。提交使用内存替身，没有改写用户历史。浏览器用明确标注的组件验收数据检查呼吸灯、隐藏地址栏和展开文件页码/原文，不冒充真实聊天浏览器验收。正式后端已重启，PostgreSQL与Milvus就绪，5173前端继续运行。

## 过程条收简与地图调用审查（2026-09-17）

本步按验收意见恢复单行呼吸灯：先改`ThinkingProcess.vue`，默认折叠，只在长条显示最新真实进度，用户可展开步骤，生成中与完成后都不展示回答草稿；然后在`App.vue`移除草稿传参，把`chat.css`中原属过程内部的样式移入组件自身。原样式使用父页面scoped作用域，组件拆分后未覆盖内部summary和列表，是默认三角与编号列表出现的原因。SSE接收与会话保存合同不变。

`frontend/tests/requirement-stream.mjs`更新默认折叠、当前进度与无草稿回归；发送流、登录回归、Vue类型检查和Vite构建通过。正式前端页面可访问，本轮未发送新消息或调用地图，没有新增真实聊天浏览器验收，也未重跑未改动的后端套件。

地图审查发现`document/maps.supplement_maps`会为每张景点卡片自动补查询，即使用户只是表达出游意愿。后续建议将普通推荐与精确定位拆开，普通RAG展示真实来源，明确问地址、附近店铺或路线时才查地图；本步只完成界面修改和建议，尚未改变地图调用、匹配或缓存策略。

## 初步验收修补：立即清空输入与避免绕过检索（2026-09-17）

先复现用户已保存的第6～11轮：均未保存知识依据，本轮意图为空，连续进入结构化理解失败后的原模型回答。对照调用发现带自然语言助手历史的JSON任务多次只返回空白；仅从流式改成完整读取仍失败，不能据此宣布修复。将历史原角色、顺序和正文作为待分析材料传入后，相同真实历史恢复JSON理解及后续检索。[DeepSeek官方JSON说明](https://api-docs.deepseek.com/zh-cn/guides/json_mode/)也提示可能返回空内容；本项目的具体修复以真实历史对照结果为依据。

| 开发顺序 | 文件 | 作用与数据走向 |
|---|---|---|
| 1 | `frontend/src/useRequirementConversation.ts`、`frontend/tests/requirement-stream.mjs` | 点击发送同步清空输入；原话保留在请求和pending中，失败恢复原话；成功或失败均不覆盖用户新写草稿，保留重试编号及切换会话隔离 |
| 2 | `backend/app/llm/client.py`、`tests/test_chat_streaming.py` | 内部JSON完整读取，历史作为带角色的材料而非助手输出示范；重新核对包装后的总输入预算。最终自然正文仍流式推送 |
| 3 | `services/chat/service.py`、`rag.py`、`services/requirement/extract.py`、`prompt.py` | 解析失败仍用当前原话和近期用户消息检索；旅行问答缺少改写词时使用当前问题；超出行程卡片范围不清空检索。展示实际命中片段数，不虚构检索成功 |
| 4 | `services/chat/dining.py`、`services/itinerary/graph.py`及对应回归 | 历史放入工具任务材料，本轮工具调用与返回仍按框架原生角色配对；无效工具选择或参数转为带资料的自然建议，不保存伪造地图结果或半成品行程 |

真实验收使用backend/.env，读取原有历史及公共知识库，仅以提交替身返回结果，不写入用户会话：杭州景点问答检索5段资料，最终正文输入含《杭州攻略-LvBanGPT.pdf》的3段引用、8条网页依据和3张核对卡片；“我需要具体的店名”实际查询灵隐寺附近，返回5家百度评分4分及以上的餐馆。后一项属于实时地图增强，不能记为知识库引用。输入框用受控网络验证等待、失败恢复、重试、切换与新草稿保留。

本轮最终验证：后端完整套件在真实PostgreSQL隔离schema中`657 passed, 2 skipped, 2 warnings`，两项跳过因未配置专用Milvus测试地址，正式Milvus已用于上述真实检索；修改范围Ruff、83个源码文件mypy检查通过。前端发送流及登录回归、Vue类型检查和Vite生产构建通过。本轮未新增浏览器人工验收记录。正式8000后端已加载修补并重启，PostgreSQL、Milvus就绪，5173前端返回200；保留运行中服务日志，清理本轮已结束的验收临时文件。尚未提交Git。

## 统一对话上下文、自然回答与过程展示（2026-09-17，源码及自动测试已核对）

本步把聊天各分支原先各自截取历史、识别城市和决定回答方式的做法收拢为一次本轮理解。旅行目的地继续表示用户已经确认的旅行条件；`conversation`只表示最近正在咨询的旅行城市和地点。比如已经计划苏州、临时询问杭州景点时，旅行目的地仍为苏州，本轮检索和下一句省略表达接续杭州。知识库、网页或地图只提供回答证据，不能反向修改用户意愿。

| 顺序 | 文件 | 作用与调用关系 |
|---|---|---|
| 1 | `backend/app/schemas/requirement/conversation.py`、`schemas/requirement/chat.py` | 定义`ConversationState`、`HistorySummary`、`TurnUnderstanding`，并在原`response_json`响应模型增加可选话题和摘要快照 |
| 2 | `backend/app/services/chat/context.py`、`summary.py` | 按revision旧到新整理连续角色历史、页面可见卡片顺序、近期12000字符预算、旧轮次连续摘要批次及同会话原文回查 |
| 3 | `backend/app/services/requirement/prompt.py`、`extract.py` | 一次模型调用同时返回需求增量、目的地动作、话题动作、当前查询城市、独立检索问题和`chat/map/plan`回答方式；旧独立提取API/CLI合同不变 |
| 4 | `backend/app/services/chat/service.py` | 读取历史、摘要与话题后统一理解；只有`map`进入地图/周边工具，只有完整规划且为`plan`进入行程Agent，其余按独立检索问题增强自然回答；整轮结果仍原子保存后才发送done |
| 5 | `backend/app/llm/budget.py`、`client.py` | 以64000个序列化字符保守检查每次模型总输入；保留系统规则、必要状态和当前原话，整轮裁剪旧历史并标记省略；普通调用及JSON修复重试都执行检查 |
| 6 | `backend/app/services/chat/conversation.py`、`rag.py`、`dining.py` | 资料命中后也由原生模型综合常识形成自然正文，只保留校验通过的知识卡片；资料为空、检索不可用或结构化输出截断时自然降级，网络、鉴权等真实调用错误仍报错 |
| 7 | `frontend/src/components/ThinkingProcess.vue`、`markdown.ts`、`App.vue`、`useRequirementConversation.ts` | 生成步骤和公开草稿位于折叠过程区，最终正文独立使用禁用HTML和图片的Markdown渲染；即使同轮有景点、餐馆或行程卡片，也不隐藏总体建议 |
| 8 | `backend/tests/test_chat_context.py`、`test_chat_summary.py`、`test_chat_understanding.py`及原聊天回归，`frontend/tests/requirement-stream.mjs` | 覆盖角色顺序、话题/目的地分离、路由、自然降级、幂等隔离、草稿不持久化、Markdown安全和正文与卡片同时展示 |

开发顺序先固定保存合同和纯上下文整理，再扩展一次本轮理解，随后让地图、RAG、自然回答和行程共用结果，最后接前端过程区与安全Markdown。当前完整原文仍逐轮保存在PostgreSQL；近期历史用12000字符近似预算，摘要正文最多2000字符，每次请求至多处理一个覆盖版本连续且总量不超过12000字符的旧轮次批次。摘要失败保留旧摘要和完整原文，不推进`covered_revision`。按需回查最多选择3个命中及相邻轮，去掉近期窗口重复后总量不超过6000字符；主调用已通过`build_recall_messages → recall_history`执行该边界。

`RequirementChatResponse.conversation`为可选对象：`topic_cities: list[str]`默认空列表、最多4项，`topic_places: list[str]`默认空列表、最多20项；`None`表示旧快照没有该字段，显式空对象表示话题已清除，不能回退到更早城市。`history_summary`为可选对象：`text: str`默认空字符串、最多2000字符，`covered_revision: int`必须大于等于0；`None`表示本轮没有新摘要，空文本加覆盖版本仍是有效摘要。两者继续保存在`requirement_turns.response_json`，没有新增表、列、主外键、约束、索引或迁移。

正式配置使用`deepseek-v4-pro`；[DeepSeek官方价格页](https://api-docs.deepseek.com/quick_start/pricing)标示其上下文窗口为1M。项目没有把1M直接当作可用输入：公共守卫以完整消息、系统提示和工具schema序列化后的64000字符为保守总输入上限，这不是token计数；模型输出仍限制为2048 tokens。地图和行程中间件在每次模型调用前连同历史和实际开放工具检查，不拆开工具调用与工具结果。工具内容过大时主入口转为自然回答且不得声称查询完成；必要状态本身无法无损放入预算时停止模型调用，数据库原历史不受影响。

地图主体核对同时排除公交站、入口、停车场等附属地点；省市前缀只有在主体后缀和地址共同印证时才接受。真实百度查询中“西湖”命中风景区，“同里国家湿地公园”命中“江苏同里国家湿地公园”。

已完成的自动验证：后端完整套件在真实PostgreSQL隔离schema中`646 passed, 2 skipped`；两项跳过仅因未配置专用Milvus测试地址。修改范围Ruff及83个源码文件mypy检查通过；前端`requirement-stream.mjs`、`landing-auth.mjs`及Vue类型检查/Vite生产构建通过。另用专用PostgreSQL schema预置30轮并连续调用真实模型：第31轮回查“不想爬山”而不推荐虎丘，摘要覆盖到第13轮、近期从第18轮开始，明确记录第14～17轮未覆盖缺口，并按需回查原话找回原因；第32轮改杭州仍保留不爬山，摘要推进至17且近期18轮无缺口；第33轮清空目的地和话题仍保留该偏好。各轮新服务读取一致，同`message_id`重试没有调用Mock模型，原30轮JSON逐字未改。

浏览器在临时18080端口和隔离schema中使用真实模型、百度和网页服务，Milvus为空：从“想去苏州”接续“推荐热门景点”，最终正文给出苏州建议，并展示地址已匹配的拙政园、苏州博物馆、狮子林三张卡片；过程草稿在`details`中目视可见。刷新并重新进入规划页恢复原两轮和卡片，没有重新生成，过程内容未持久化。上述只是明确列出的真实服务样例，其余C01～C24含自动化替身边界，不能统称实网验收。新会话独立回答“2加3等于几”为5，切回苏州会话后两轮原文与卡片保持不变。专用QA schema已清理。用户要求查看效果后，已按backend/.env启动8000后端和5173前端，PostgreSQL与Milvus就绪；运行中服务及日志保留。

## 未定目的地的规划引导（2026-09-17）

验收原句“想安排一个周末短途旅行，请先问问我的出发城市和偏好。”在保存记录中被标为trip_question，落入知识不足兜底。本步修正规划开场的意图提示、分步补问及检索边界。

| 顺序 | 文件 | 作用与调用关系 |
|---|---|---|
| 1 | `backend/app/services/requirement/prompt.py` | 明确未提供条件的规划/引导请求也属于plan_trip；请问偏好不等于已提供偏好 |
| 2 | `backend/app/services/requirement/extract.py` | 目的地未知时先问尚缺的出发城市与兴趣，示例不写入字段，已知信息不重复问 |
| 3 | `backend/app/services/chat/rag.py`、`services/chat/service.py` | 共用知识问题判断，问号本身不算外部事实；无目的地的纯补问不进入检索上下文，混合知识问题仍检索 |
| 4 | `backend/tests/test_planning_guidance.py` | 原句、问号改写、部分已知条件、多轮保存及重试、混合知识问题回归 |

验证：完整后端584 passed、2条既有警告、91.49秒，含真实隔离PostgreSQL/Milvus；题库已恢复，本轮未排除测试。mypy 78文件和修改范围Ruff通过。真实模型验证原句及两种改写均为plan_trip。HTTP/SSE原句4.94秒完成并询问出发城市/偏好；随后“上海出发、自然风景、两天”3.64秒完成，字段正确保存、knowledge=null。历史revision=2，原消息重试返回同一快照；专用账号和会话已清理。两轮仍有Agent工具发现和需求抽取，不宣称零外部调用；无新增浏览器视觉验收。

服务按README命令从backend目录以`uvicorn app.main:create_app --factory --env-file .env`启动，只读取backend/.env，日志在根目录temp/logs，不再依赖临时启动脚本。无新数据库字段或迁移，不重写旧轮次。

## 项目目录与配置整理（2026-09-17）

| 顺序 | 文件或目录 | 作用与调用关系 |
|---|---|---|
| 1 | `backend/evals/*.v1.json` | 恢复两份固定评测输入，供评测脚本和回归测试读取；生成报告仍进入根目录temp/reports |
| 2 | `backend/.env`、`.env.example` | 合并正式DeepSeek配置，模板改为百度MCP；学习脚本及原配置移至E:/TravelMindAI-learning，密钥不提交 |
| 3 | `scripts/demo_requirement.py`、`evaluate_requirements.py`、README | 指引统一显式读取backend/.env；标准启动命令将日志写temp/logs，不依赖临时启动脚本 |
| 4 | `.gitignore`、AGENTS、06/08文档 | 精简重复忽略规则、更新目录图与保存位置，区分固定题库、永久保留原文和可清理报告 |

18项配置/评测测试通过，1项既有依赖弃用提示；单配置加载及29份上传原文目录核对通过。没有重跑真实模型、地图或浏览器验收。此前餐馆回归中跳过的4项题库测试所依赖文件现已恢复。

后端两份日志已移至temp/logs，前端两份日志仍被运行服务占用。两个uploads兼容联接、空assets与旧根目录缓存暂留：包含目录删除的命令被自动审批审查拒绝，未绕过，也未停止主任务服务。当前目录和清理边界以06及08末节为准；下方保留各阶段历史结果。

## 餐馆过滤与特色菜连续问答（2026-09-17）

本步修复饮品店混入餐馆、含糊的特色追问筛空即结束，以及菜名被当作地点查地址的问题。没有新增依赖、数据库字段或另一套Agent循环。

| 顺序 | 文件 | 作用与调用关系 |
|---|---|---|
| 1 | `backend/app/services/baidu.py`、`tests/test_baidu.py` | 默认检索餐厅，按百度细分类及名称排除咖啡、奶茶等饮品；原有评分、距离、消费核对继续生效 |
| 2 | `backend/app/services/chat/dining.py` | 原生Agent增加local_food动作；明确全城提问只查知识，含糊的周边追问兼顾原预算/半径与城市介绍；新地点仍先核对地图 |
| 3 | `backend/app/services/chat/rag.py`、`services/document/answer.py` | 优先按城市和餐馆类别检索，兼容未分类资料；已有资料时不泛搜网页。特色菜只生成带引用的文字，误填菜品卡片也在地图调用前转成文字 |
| 4 | `backend/app/services/chat/service.py` | 将可选周边结果与知识回答组合，复用原幂等、版本检查及整轮保存，不改变旅行总预算 |
| 5 | `backend/tests/test_food_advice.py` | 饮品排除、全城/周边范围、跨城、新地点、菜名不定位、组合保存与刷新重试回归 |

验证：后端574 passed、4 deselected、2 warnings（82.26秒），包含真实隔离PostgreSQL/Milvus；4项旧评测测试依赖工作区已删除的`evals/documents.v1.json`、`evals/requirements.v1.json`，未恢复用户清理的题库。mypy 78文件、修改范围Ruff与前端SSE Node回归通过。最后将美食提示词与景点定位规则分开，收紧知识库优先及模拟价格规则；定向回归39 passed（含真实隔离PostgreSQL），1条既有警告。

真实DeepSeek、百度MCP和知识库经HTTP/SSE完成“湖滨路步行街饭店→人均100以内→想吃当地特色→杭州有什么特色菜”。前两轮无饮品店；第三轮保留100元限制，同时返回杭帮菜餐厅与`杭州-餐馆-ChinaTravel.md`的文字介绍；第四轮无周边预算、无菜品卡片。流式草稿、历史revision=4及临时账号/会话清理已验证。本轮未新增浏览器视觉验收；不重写旧会话中的历史错误回答。

## 统一百度地图MCP（2026-09-17，源码核对与真实查询）

本步把当前地图查询全部改走百度官方MCP。删除高德配置与客户端，也不再保留百度直连HTTP分支；旧聊天仍保存当时的高德来源与GCJ-02坐标，不把历史结果改写成百度。当前调用链、图和JSON字典见08第10.25节；以下9月16日及更早章节保留当时的实现与验收记录。

| 顺序 | 文件 | 作用与调用关系 |
|---|---|---|
| 1 | `backend/app/config.py`、`.env.example`、`backend/pyproject.toml`、`backend/uv.lock` | 配置与依赖层：地图只读取BAIDU_MAP_API_KEY，引入LangChain原生MCP适配能力及FastMCP依赖 |
| 2 | `backend/app/services/baidu_mcp.py` | 外部工具层：固定官方远端MCP，框架发现并适配工具；限制只读白名单、超时与24次调用，清理大字段并保存本轮依据 |
| 3 | `backend/app/services/baidu.py` | 地图业务层：BaiduMaps按请求延迟连接并复用工具；lookup核对名称/官方别名、同城、唯一UID和BD-09坐标，nearby_places核对商户评分、距离、类别与消费 |
| 4 | `backend/app/schemas/map_tools.py`、`schemas/requirement/chat.py`、`schemas/document/answer.py` | 格式层：增加可空mcp回答与依据快照；坐标兼容BD-09和历史GCJ-02，保留真实provider，不新增数据库表 |
| 5 | `backend/app/services/chat/dining.py`、`services/chat/service.py`、`api/requirement/history.py` | 编排层：归属、幂等与版本检查后才按需连接；Agent选择地图工具、受控周边筛选、补问或继续聊天，最终仍由原事务保存 |
| 6 | `backend/app/services/document/maps.py`、`services/itinerary/tools.py`、`services/itinerary/graph.py`及其接口调用方 | 既有景点/行程链改用同一百度地图服务；旧高德锚点再次用于查询时按名称重新核对，不把GCJ-02直接当BD-09 |
| 7 | `frontend/src/mapLink.ts`、`api/documents.ts` | 前端链接与合同层：共用百度网页marker链接，明确纬度在前、经度在后，分别声明bd09ll或gcj02 |
| 8 | `frontend/src/components/AttractionCards.vue`、`ItineraryCard.vue`、`RestaurantCards.vue` | 展示层：三类卡片复用链接函数，入口坐标优先；新商户默认百度，历史高德评分仍如实标记 |
| 9 | `backend/tests/test_baidu_mcp.py`、`test_baidu.py`、`test_nearby.py`、`frontend/tests/requirement-stream.mjs`等相关回归 | 验证层：协议适配、白名单、失败/额度、地点与商户筛选、引用保存、旧历史以及新旧坐标网页链接 |

开发顺序先把外部连接和业务核对分开，再让聊天、知识及行程共用请求中的BaiduMaps，最后统一卡片入口。MCPAdapter负责协议与工具参数，业务代码只保留项目自己的评分、归属、引用和保存规则；没有再手写一套MCP协议或Agent循环。AnyIO BlockingPortal负责让现有同步服务调用框架异步工具，请求结束时统一释放。

连接超时10秒；框架调用超时15秒，外层等待上限20秒；当前请求最多24次外部工具调用，有地图工具时Agent最多8轮决策。多个只读MCP调用可以同轮返回，由框架max_concurrency=1顺序执行；回答提交、补问与分流必须独立选择。分享地图、IP定位和未知工具不开放。

验证按证据分开记录：最终后端565 passed、2 warnings、84.05秒（含真实隔离PostgreSQL/Milvus），两条提示为既有AnyIO与原生LangChain MCP beta；mypy检查78文件、修改范围Ruff、前端Node回归、生产构建及diff检查通过。配置的AK在用户调整访问白名单后，真实天气、地点及周边工具调用成功。真实DeepSeek经HTTP/SSE连续完成杭州明天天气、湖滨路步行街饭店、100元以内、第一家店附近住宿，单次完成时间分别5.90/2.68/2.73/2.65秒，来源均为百度、坐标均为BD-09；历史读取revision=4，专用QA会话删除返回204，QA账号已清理。路线问题修正同轮多个只读工具规则后，真实完成两端地理编码、路线查询和带依据回答。上述为特定样例实测，不作时延、所有路线或完整逐日路线校验承诺；最终后端按最新源码重启健康200。杭州东站到西湖断桥公交HTTP200、10.40秒，接续100元内餐饮HTTP200、3.77秒；浏览器恢复两轮并核对百度评分和bd09ll地图链接，专用账号退出，QA会话及用户清理回查为0。外部百度落地页未打开验收；旧GCJ-02链接由自动回归覆盖。

## 周边查询Agent与整段会话删除（2026-09-16，自动检查与本轮真实验收通过）

以下为统一百度MCP之前的历史记录；其中高德、可选百度HTTP及当时测试条数不代表当前配置，最新实现见本文件顶部与08第10.25节。

本步处理两件事：让模型结合历史选择周边餐饮、住宿或继续普通聊天；在左侧历史栏确认后真实删除本人整段会话。周边理解放在旅行需求抽取之前，防止“人均一百以内”被误写成整趟旅行预算。已有M4-A行程Agent、知识问答和资料管理保持原职责。

| 顺序 | 文件 | 作用与调用关系 |
|---|---|---|
| 1 | `backend/tests/test_nearby.py`、`test_dining.py`、`test_baidu.py` | 测试层：覆盖短追问、锚点、消费与类别切换，以及两家地图的评分、价格、距离和异常响应；外部服务用离线替身 |
| 2 | `backend/app/schemas/dining.py`、`schemas/document/answer.py` | 格式层：沿用dining快照，增加category、max_cost和可选百度provider；MapLookup保留来源，旧快照默认餐饮/高德 |
| 3 | `backend/app/services/amap.py`、`services/baidu.py` | 外部工具层：统一nearby_places；核对具体锚点、同城、类别、GCJ-02坐标、圆形范围、4分以上评分与可选消费上限，最多返回5家 |
| 4 | `backend/app/services/chat/dining.py` | 业务层：create_agent绑定search_nearby、ask_location、continue_chat，接收最近对话和已核实地点；同查询继承筛选，换类别/锚点清旧筛选，拒绝模型填写坐标 |
| 5 | `backend/app/services/chat/service.py`、`api/requirement/history.py` | 编排及接口层：先处理周边语境，命中时保留原旅行需求并保存knowledge轮次；否则继续需求抽取、知识回答或M4-A。JSON/SSE入口均注入可选百度客户端 |
| 6 | `backend/app/config.py`、`.env.example`、`README.md` | 配置层：百度是独立可选来源，密钥仅在后端；未配置不能显示成已完成百度查询 |
| 7 | `frontend/src/api/requirements.ts`、`api/documents.ts`、`useRequirementConversation.ts` | 前端合同与状态层：保留商户类别和来源，刷新从数据库dining快照恢复，不重新查询 |
| 8 | `frontend/src/components/RestaurantCards.vue`、`App.vue` | 展示层：复用商户卡片显示餐馆/住宿、真实评分、地图来源和参考消费；住宿明确参考消费不等于每晚房价 |

前两步明确“哪些字段保存下来”；地图客户端只核实事实，周边Agent负责理解“第一家附近”“人均一百以内”等上下文。它先决定查询、必要补问或继续聊天，再由原会话事务保存。新锚点名称仍通过高德lookup核对；百度目前提供周边查询，未实现独立的百度名称定位或跨来源自动回退。圆形半径默认2000米、可按本轮明确范围调整，不是步行路线或耗时。

删除功能先补失败测试，再接存储、HTTP、页面确认和状态清理：

| 顺序 | 文件 | 作用与调用关系 |
|---|---|---|
| 1 | `backend/tests/test_session_delete.py` | 隔离PostgreSQL中覆盖登录/CSRF/归属、完整删除、其他会话及知识库保留、与追加/保存/撤销竞争、失败事务回滚 |
| 2 | `backend/app/services/trip_service.py`、`api/trips.py` | 存储层按session_id和user_id锁会话，在同一事务按requirement_turns→itineraries→travel_requests→sessions顺序硬删；DELETE成功204，缺失或他人404 |
| 3 | `frontend/src/api/requirements.ts`、`useRequirementConversation.ts` | 封装DELETE；成功删除当前会话才清空消息、需求、待发、撤销及sessionStorage，保留账号存储键用于下次发送；删除非当前会话不清空当前内容 |
| 4 | `frontend/src/App.vue`、`frontend/tests/requirement-stream.mjs` | 每项历史提供独立可访问按钮和中文Arco确认框；失败保留列表，账号代数和列表代数防旧响应复活；验证确认、取消前不写、当前清理及跨账号隔离 |

两项功能都复用0010既有表与JSON，不新增数据库表、列、约束、索引或迁移。删除不调用模型、不触碰资料原件、documents/document_chunks或Milvus，也不等同于先前的资料停用清理流程。

验证记录：删除及相关存储回归51项通过，其中新增删除6项；修改范围Ruff、mypy 77文件、前端SSE/状态回归和生产构建通过。审查修正了补问保留价格/半径、原锚点挤占商户序号、同查询来源继承；全套557 passed、1条既有Starlette/AnyIO警告（78.73秒）。之后将Agent上下文缩为条件和地点编号，坐标/地址留给程序保管，再跑71项相关回归通过。旧节中的506/514/479项只对应此前版本。

真实DeepSeek与高德已完成四轮接续：附近餐馆 → 人均100元以内，5家参考消费为60/49/56/36/58元 → 第一家餐馆“炉鱼”附近住宿 → 第一家酒店“枕湖望星居”附近餐馆。后3轮完成时间分别为4.11/2.01/2.22秒，首SSE事件约0.02～0.05秒；最终精简上下文后再次实测餐馆→100元上限→第一家餐馆附近酒店，分别2.52/2.78/2.03秒，均为单次观测，不作时延承诺。专用测试账号浏览器核对景点附近酒店卡片、中文删除确认、删除后刷新不恢复，以及删除后新问未知住宿位置时正确补问酒店名与城市。百度未配置密钥，本轮只有离线测试证据，未做真实百度调用。最终后端重启健康检查200，浏览器测试账号退出；仅清理本轮3个QA账号、3个剩余QA会话及关联记录并回查为0，另1个QA会话已在浏览器删除并刷新验证。

## Ponytail：框架接管行程循环（2026-09-16，源码与回归已核对）

本次审查范围是M4-A的通用调用循环，不扩展私人附件、Reviewer或持久恢复。替换方向依据[LangChain官方Agent接口](https://docs.langchain.com/oss/python/langchain/agents)与[内置次数限制中间件](https://docs.langchain.com/oss/python/langchain/middleware/built-in)，并核对实际安装的LangChain 1.4.0 / LangGraph 1.2.11源码。

| 审查结论 | 已落实替换 |
|---|---|
| native：手写StateGraph状态、路由、消息追加与工具分发 | create_agent、ToolNode和原生AIMessage/ToolMessage |
| delete：generate_action把原生工具调用转JSON，图再解析回来；ToolModel仅为兼容替身增加生产分支 | ChatOpenAI直接交给Agent；旧测试数据只在tests/helpers.py适配 |
| native：工具名分支分发与手工JSON Schema | StructuredTool.from_function自动生成契约和分发；显式保留extra=forbid |
| native：自己维护模型循环上限 | ModelCallLimitMiddleware(run_limit=6)；before_model在成功后直接结束 |
| 保留：权限、来源核验、总查询额度、局部修改、预算、版本与撤销 | 这些是业务约束，不能由通用框架替代；工具顺序执行，提交和规则修正不占12次查询额度 |

开发顺序：先把tools.py改成三个独立查询函数；再让graph.py注册标准工具并用中间件限制证据准入；最后删除client.py的转换器，chat/service.py直接传model.model，extract.py的现有模型接口声明标准模型属性。调用顺序为`聊天服务 → create_agent → 标准工具 → 原生工具结果 → submit_plan规则校验 → 原会话事务 → SSE done`。公开progress仍在实际查询和校验时发送，未暴露模型内部推理。

行数按本轮修改前工作区快照计算，包含注释和空行，不把上轮未提交的M4代码算成本轮新增：graph.py 245→195，tools.py 101→99，client.py 121→84；三个核心文件合计467→378，净减89行。模型接口声明另增加6行；未为省行数删除中文说明或安全规则。

验证：最终后端514 passed、0 skipped，含真实隔离PostgreSQL/Milvus，1条既有Starlette/AnyIO弃用警告；mypy 76文件、修改范围Ruff、前端Node SSE/撤销/切换账号测试、Vue类型检查及Vite构建通过。新增回归核对原生ToolMessage、6轮停止、12次查询上限、额外参数拒绝、来源准入、流式公开进度，以及工具之后模型失败仍无半轮入库。

真实请求曾因模型继续选择已关闭工具而被拒绝，失败没有保存。保留原业务所需的动态额度/地点提示后复验：首次progress为0.09秒，生成并提交为19.58秒；仅为一次观测，不是提速基准。浏览器读取v1（第二天09:00灵隐寺、13:30九溪），只改第二天11:00开始生成v2，第一天完整保留；撤销为v3并刷新恢复成功。HTTP逐字段核对v3整个计划与需求均等于v1。专用QA账号已退出，仅清理本次1会话、3对话轮次、3行程、3需求及该账号登录态，既有账号与数据未动。

net: -89 lines in the three core files.

## M4-A文本行程智能体（2026-09-16，本轮范围已验收）

开发顺序是“行程格式与规则 → 事务保存和撤销 → 受控工具图 → 聊天页面”。完整规划需求进入LangGraph，知识问答和不完整需求沿用原业务链；本步不包含私人附件、独立审查或持久节点恢复。

| 顺序 | 文件 | 作用与调用关系 |
|---|---|---|
| 1 | `backend/app/schemas/itinerary.py`、`schemas/requirement/chat.py` | 定义逐日活动、原文/地图证据、行程版本及撤销请求；聊天响应新增可空itinerary，旧快照默认None |
| 2 | `backend/app/services/itinerary/rules.py` | 将模型提交的地点编号和时间转成草稿；核对天数、已核对POI、排除项、时间重叠、节奏与明确某天开始时间，调用预算服务；明确指定日时保留其他天，空patch可保留活动重算预算 |
| 3 | `backend/app/services/trip_service.py`、`services/requirement/history.py` | 共用事务内需求/行程保存；会话锁与双版本检查后原子写入整轮，从数据库前后需求及计划计算实际变化；撤销复制上版需求/行程为新draft，不覆盖历史 |
| 4 | `backend/app/api/requirement/history.py` | 增加`POST /sessions/{id}/drafts/undo`，沿用本人会话权限；返回SavedRequirementTurn，冲突返回409 |
| 5 | `backend/app/services/itinerary/tools.py`、`graph.py` | 工具白名单限制知识检索、网页检索和高德地点核对；create_agent接管原生消息与工具循环，最多6轮决策、12次外部查询，证据不足可结束补问 |
| 6 | `backend/app/services/chat/service.py`、`backend/app/llm/client.py`、`backend/pyproject.toml`、`uv.lock` | 完整需求分支把ChatOpenAI直接交给create_agent；LangChain 1.4.0运行于LangGraph 1.2.11之上，复用公开progress和最终整轮保存 |
| 7 | `frontend/src/api/requirements.ts`、`useRequirementConversation.ts` | 定义行程/撤销合同，发送双版本和操作编号；恢复历史行程，撤销后同步会话需求与版本，处理旧页冲突和账号切换 |
| 8 | `frontend/src/components/ItineraryCard.vue`、`App.vue` | 在消息内展示逐日活动、来源、预算、局限和修改摘要；显示有效撤销入口，文字修改继续走聊天 |
| 9 | `backend/tests/test_itinerary_agent.py`、`test_itinerary_chat.py`、`test_itinerary_model.py`、`test_itinerary_history.py`、`frontend/tests/requirement-stream.mjs` | 覆盖条件图、原生工具调用、规划分流、工具及规则边界、事务/幂等/撤销/权限和前端协议、撤销及滚动行为 |

前两步先固定“草稿保存什么、哪些安排可接受”；第三、四步保证“生成与撤销一起保存需求、草稿和消息”；第五、六步让模型根据工具结果决定后续动作，最后把保存后的结果接入消息卡片。调用顺序为`页面 → SSE接口 → 历史/需求提取 → 完整规划分支 → create_agent原生工具循环 → submit_plan业务校验 → 会话短事务 → done → 行程卡片`，工具结果不会直接作为已保存草稿展示。

正式DeepSeek模型由`create_agent`直接绑定标准工具；`StructuredTool.from_function`自动生成参数契约，`ToolMessage`传递工具结果与可修正的规则错误。程序不再维护JSON行动协议、分发器或手写图路由。需求抽取和M3知识问答仍保留`generate_json`；测试的旧数据简写转换只存在于tests/helpers.py。程序只接受有原文来源且地图核对成功的具体POI；交通方式与预留分钟是建议，不证明路线可达。预算使用`demo-cny-v1`演示单价；无法核实的硬条件先解释和补问，不伪称满足。只有明确指定某天、且原行程天数/目的地/开始日期仍适配时，程序强制限定局部修改。

本步不新增数据库表或迁移，沿用`travel_requests.request_json`、`itineraries.itinerary_json`和`requirement_turns.response_json`。完整JSON字典、唯一约束、撤销合同和实际条件图见08第10.23节。

首次M4-A验收（框架替换前）：后端全套506项通过、0跳过，含真实隔离PostgreSQL/Milvus，1条既有Starlette/AnyIO弃用警告；mypy检查76个文件、修改范围Ruff、前端Node协议/撤销/滚动测试及Vue类型检查/Vite构建均通过。

真实浏览器使用专用QA账号完成杭州两天草稿：v1第一天10:00湖滨、第二天09:30苏堤；自然语言只将第二天改到11:00，生成v2且第一天完全不变；点击撤销生成v3，恢复第二天09:30，刷新后仍一致。HTTP另外核对三个版本均为draft，v3完整plan与完整需求均等于v1。SSE公开节点可见，知识库、地图和网页原生工具选择均在真实调用中发生。

本机Alembic current为`0010_required_session_owner (head)`，check返回`No new upgrade operations detected`，本轮未执行迁移。最新行程卡片滚动到顶部已通过真实截图复验。QA账号`qa_m4_0916`已退出，清理严格限定于其1条测试会话、6轮对话、3份行程和3份需求；用户既有数据未动。

验收过程中修复了纯JSON行动不稳定、餐饮抢入规划、保留日误解析、重复地点和明确11点被当作待核实硬条件等问题。规则现在核对“第二天11点开始游玩”这类明确开始时间；无障碍、真实路线时长等缺依据条件仍需补证。此结果证明本轮文本生成、指定日修改、撤销及恢复闭环，不代表广泛问题集的规划稳定率或完整M4-B/C验收。

## 流式回复、目的地引导与附近餐饮（2026-09-16，源码已实现）

本步按“结果格式与地图筛选 → 聊天分支与城市引导 → 真实流式事件 → 页面展示与恢复”开发。旅行知识继续依赖检索证据；附近餐饮使用专门的高德分支，不被无关知识库结果阻断。

| 顺序 | 文件 | 作用与调用关系 |
|---|---|---|
| 1 | `backend/app/schemas/dining.py`、`schemas/requirement/chat.py` | 定义餐厅、查询状态与可空dining快照；随原有整轮JSON保存，旧历史默认None，没有新表或迁移 |
| 2 | `backend/app/services/amap.py`、`services/chat/dining.py` | 高德核对具体锚点后查询2000米圆形周边；只筛真实4.0～5.0分，按高德名称/分类/口味标签二次核对菜系，最多5家；短菜系追问接续地点，有歧义先问 |
| 3 | `backend/app/services/chat/service.py`、`services/chat/rag.py` | 提取并合并需求后选择餐饮或普通知识链；新目的地先尝试简短介绍，再自然询问缺项；纯个人条件不追加无关的资料不足提示 |
| 4 | `backend/app/services/document/answer.py`、`services/requirement/extract.py` | 调整缺项自然补问；回答服务在实际生成、工具核对时通知进度，城市简介限制景点卡片，保留最终证据和引用校验 |
| 5 | `backend/app/services/chat/events.py`、`llm/client.py` | 用ContextVar隔离每个请求的公开事件；模型真实stream分片仅抽取合法本轮引用的points草稿；source_ids必须先于text并闭合，顺序不符等最终校验；缩短/清空立即推送，不显示原始JSON、工具参数或内部推理 |
| 6 | `backend/app/api/requirement/history.py`、`main.py` | 新增POST流式入口，保留原JSON入口；独立工作线程持有连接，断流后已开始的业务继续校验保存；应用正常关闭先等待当前工作任务再释放数据库；进度、草稿、完成和脱敏错误通过SSE返回 |
| 7 | `frontend/src/api/requirements.ts`、`useRequirementConversation.ts` | 增量解析SSE、更新公开步骤和草稿；只有done后显示已保存轮次，断流保留原消息编号用于读历史或重试 |
| 8 | `frontend/src/App.vue`、`components/RestaurantCards.vue`、`style.css` | 显示待核对草稿、公开处理步骤、真实评分及地图链接；退出或切换账号清除过程内容，已保存餐饮从历史恢复 |
| 9 | `backend/tests/test_chat_streaming.py`、`test_dining.py`、`test_chat_rag.py`及相关测试 | 覆盖分片、事件隔离、错误、断流保存、餐饮筛分与指代、目的地简介及个人条件回复 |
| 10 | `docs/08-架构设计说明.md`、`docs/diagram/travelmind-chat-rag.*`、`travelmind-overview.*` | 同步接口、调用分支、JSON数据字典与架构图；不把草稿当成已保存回答 |

前两步先确定“地图返回什么、什么餐厅能进入结果”；第三、四步决定“本轮该查什么、何时补问”；之后才接上流式传输和页面。完整顺序是`页面 → SSE接口 → 读取历史/提取需求 → 餐饮地图或普通知识链 → 最终校验 → 短事务保存 → done → 页面正式回答`。progress来自实际步骤；draft/reset只管理临时公开草稿。模型仍为thinking disabled，没有开启思维链展示。

范围说明：2000米是地图圆形半径，不能当成步行距离或时间；当前只筛高德首批最多25条，不保证覆盖区域内全部餐厅。缺评分不补造；无可用评分、无匹配、工具未配置/失败分别说明。浏览器断开后服务进程仍存活时已开始的工作继续，服务器重启不具备聊天任务持久恢复队列。

本步验证：完整后端含真实PostgreSQL/Milvus回归479项通过，1条既有AnyIO弃用提示（70.72秒）；mypy 71文件、最终修改范围Ruff、前端构建及SSE Node契约测试再次通过。真实浏览器确认访客“想去杭州”先登录再续答、实际处理步骤展示、城市简介后自然补问人数/天数/预算；湖滨路步行街周围饭店查询使用真实高德，返回5家4.5～4.7分餐厅及地址、人均参考和地图卡片。真实“火锅”短追问保持湖滨路步行街锚点，口味过滤修复后5家全部为火锅、评分4.6～4.7。最终简短城市介绍通过Vite代理单次观测：首progress 0.062秒、首draft 10.078秒，3次草稿为24/48/52字，done 10.625秒；最终为52字苏州简介加自然引导，不作为服务时延承诺。源码、事件边界和文档最终复核已完成。

## 首问登录、默认管理员与渐变页面（2026-09-16）

本步按“口令边界 → 首问续答 → 页面样式 → 实库与页面验收”修改，沿用双角色的服务端授权。

| 顺序 | 文件 | 作用与调用关系 |
|---|---|---|
| 1 | `backend/tests/test_auth.py`、`app/api/auth.py`、`app/services/auth.py` | 先验证5/6/12/13位边界，再把登录、注册和账号创建统一为6～12位 |
| 2 | `backend/scripts/manage_accounts.py` | 增加显式init-admin命令，首次创建admin/123456，重跑不重置已存在账号、不提升同名普通用户 |
| 3 | `frontend/src/App.vue`、`useRequirementConversation.ts` | 默认展示匿名对话页；首次发送暂存问题并打开登录dialog，登录/注册成功后从新会话自动回答；取消保留输入，昵称当前使用账号名 |
| 4 | `frontend/src/style.css` | 首页和登录弹窗的薄荷青/浅蓝渐变、右上角首字母头像与账号菜单、键盘焦点及窄屏布局 |

本地管理员已按用户要求改为admin/123456，旧登录令牌已撤销。本轮没有新增数据库表或迁移。前端口令只在表单临时持有，服务端仍保存哈希。

验收：真实浏览器确认首页不强制登录、首问才弹窗、取消保留问题、登录失败后可切换注册、六位管理员口令登录后自动回答、普通用户六位密码页面注册并自动续答、右上角账号菜单与退出回到首页。密码边界及默认账号重复初始化测试通过；全量真实PostgreSQL/Milvus回归436项通过（1条已有Starlette/AnyIO弃用提示），账号专项18项、mypy 68文件、修改范围Ruff、前端类型检查/构建及git diff --check通过。390像素宽度的首页、账号菜单与注册弹窗已检查。


## M3双角色、会话归属与共享知识库基础（2026-09-16，源码已实现）

本步按“迁移/身份 → 会话与权限 → 资料范围与管理页 → 前端”开发。普通用户现在可自助注册，但注册只能创建`user`；管理员经部署命令创建。所有未来新会话必须有已注册账号归属；本次用户授权删除32条旧本地会话，本机已备份PG/原件/向量，清理32会话、42轮次、2需求、2草稿；保留32资料与24101片段，真实库应用0008/0009/0010，22685条向量原编号和值迁移读回；自动测试与浏览器验收见本节下方记录。

| 顺序 | 文件 | 负责什么、由谁调用 |
|---|---|
| 1 | `backend/app/models/auth.py`、`models/trip.py`、`models/document.py` | 定义账号、登录态、会话所有者、资料类别和上传管理员；服务按这些关系保存和查询 |
| 2 | `backend/migrations/versions/0008_accounts.py`、`0009_document_categories.py`、`0010_required_session_owner.py` | 建账号与过渡归属、补城市/类别、最后拒绝带孤立会话的非空收紧；Alembic显式执行 |
| 3 | `backend/app/services/auth.py`、`backend/app/api/auth.py` | Argon2口令与随机令牌；HTTP登录/注册/退出/读取本人，来源与限频校验由接口集中处理 |
| 4 | `backend/scripts/manage_accounts.py` | 部署命令创建管理员、列出/显式认领旧会话；注册入口只创建普通用户 |
| 5 | `backend/app/services/requirement/history.py`、`backend/app/api/requirement/history.py`、`backend/app/api/trips.py` | 从登录态取得用户，列本人会话并在读写历史、需求和草稿前检查归属；别人编号返回404 |
| 6 | `backend/app/services/knowledge_scope.py`、`backend/scripts/migrate_knowledge_scope.py` | 固定共享范围`knowledge-base`；旧范围存在时资料业务503，维护命令先复用向量并读回，再改PostgreSQL，不重新Embedding |
| 7 | `backend/app/api/document/*`、`backend/app/services/document/*` | 所有旧管理路径和`/admin`路径校验管理员；城市/类别、文件名、城市数量及游标分页，旧source/review_status/poi_id只保留档案列 |
| 8 | `frontend/src/api/auth.ts`、`frontend/src/api/http.ts`、`frontend/src/App.vue`、`frontend/src/useRequirementConversation.ts` | 前端先恢复登录身份，再按角色导航；聊天取服务端历史、分页、新建与切换，写请求带同源校验头 |
| 9 | `frontend/src/components/DocumentPanel.vue`、`DocumentMetadataFields.vue`、`DocumentUpload.vue` | 管理员紧凑资料页、城市/类别两标签、文件级任务和默认片段预览；用户端固定需求面板与搜索组件已移除 |
| 10 | `frontend/src/components/DocumentJob.vue` | 后台任务完成时通知资料页重新读取当前文件片段，避免预览仍显示旧的0段 |
| 11 | `frontend/vite.config.ts` | 本地开发与预览代理保留浏览器访问的Host（`changeOrigin: false`），让后端同源Origin校验正常工作 |

一次用户消息的调用顺序是`App.vue → requirements.ts → api/auth.py登录依赖 → 会话归属检查 → RequirementHistoryService → chat/service.py → 普通知识检索或餐饮地图 → 整轮保存 → 页面更新`。管理员上传则是`DocumentPanel → documents.ts → 管理员依赖 → DocumentService → 解析/任务/切片/索引`。私人附件、逐日行程智能体、批量标签和文件替换尚未进入这些调用链。

本机实际PostgreSQL/Milvus全量自动测试431 passed、1 warning（Starlette/AnyIO弃用别名），mypy app 68文件通过，前端build通过；Alembic check无差异。追加登录令牌过期断言后`test_auth.py` 13项通过，目标修改Python Ruff通过。管理员账号已用部署流程初始化，不在文档保存初始口令。

真实页面验收：普通用户登录后无资料菜单；新建两条会话，发“想去杭州”获得真实DeepSeek回答，后台使用Embedding、Milvus和原文回查；首轮自动生成标题，切换会话与刷新均恢复本人历史。退出后页面清空；管理员登录时历史为空，不能看到普通用户会话。管理员资料页按城市展开、按文件名筛选并同步数量，默认预览当前文件752段片段，可切换原文；后台完成状态显示紧凑。普通账号自助注册经真实HTTP返回201并核对会话owner，页面注册入口已展示；本次未把页面入口展示写成页面提交验收。

## M3口语偏好追问与补充字段容错（2026-09-15）

用户说“杭州有哪些值得玩的景点？最好是三五个离得不远”时，现在先自然询问步行时间或是否乘车；追问本身不需要伪造资料引用。“步行半小时以内”“最多走个二三十分钟吧，老人多”等接续回复带回上一轮地点。已经确认的方式和时长继续使用，助手举的例子不当成用户选择。

| 顺序 | 文件 | 作用与调用关系 |
|---|---|---|
| 1 | tests/test_chat_clarification.py、test_chat_rag.py | 先复现口语追问、简短回复丢话题、门票漏引用及无效地址泄漏；真实数据库验证保存、重发和接续 |
| 2 | schemas/document/answer.py | clarification是可空的追问/未核实项提示，默认None，旧快照兼容 |
| 3 | services/chat/service.py、rag.py、services/document/search.py | 防止景点追问的短回复被误当完整旅行计划；先保留主聊天检索，再对明确的模糊距离提出追问；历史保存助手追问，短回复带回原问题 |
| 4 | services/document/answer.py、places.py | 模型可自然询问其他含糊偏好；程序补挂实际证据、逐字段核对门票/地址，并隔离缺失或错误详情 |
| 5 | frontend/src/api/documents.ts、useRequirementConversation.ts、App.vue | 恢复clarification；有卡片时把追问单独显示，普通对话仍显示文字 |
| 6 | 08第10.21节、聊天SVG/PNG | 同步数据合同和调用流程；没有新增表或迁移 |

景点主体、真实来源编号仍严格核对，不能把模型编造的景点当成可靠推荐。门票/地址字段未通过时移除该字段；重复这项错误信息的正文也清理，总述换为不包含详情结论的中性提示。还未接入步行路线或交通耗时计算，因此接受“半小时以内”只是记录筛选偏好，不等于已经验证路线可在半小时走完。此前33题分数不是本次提示词版本的实测成绩。


本步验证：后端全套412项通过（1条已有依赖弃用警告），修改范围Ruff、mypy 66个文件、前端类型检查及Vite构建通过；Alembic检查无待生成迁移。真实DeepSeek与浏览器完成原问题→距离追问→“步行半小时以内”→杭州景点回答，两轮保存并刷新恢复；没有再索要完整旅行计划。此次真实回答返回西湖、灵隐寺两张卡片，不代表已满足三五个景点或步行半小时约束；本步验收的是追问、接续与保存，路线筛选尚未验收。

## 历史记录：双角色与对话附件文档方案（2026-09-15，当时待实现）

2026-09-15当时根据五张截图及四项优化要求更新方案，没有修改代码或执行迁移；2026-09-16已完成上方A～C源码及本机迁移。管理员仅管理共享知识库，用户附件只属于自己的对话；完整设计见[09](./09-双角色与对话附件改造方案.md)，目标数据字典见08第12节。下方M3记录仍是各自开发时的历史成果，不表示新设计已上线。

本次续补：用户接受附件用途、修改摘要与撤销、切片质量、批量管理和文件替换五项建议，写入09第12节。09第13节及03第7节明确M4分步接入，启动点提前到A～C基础完成后；先文本智能体，再接附件，不等管理增强全部做完。这里只更新文档，没有新Agent代码。

### 本轮文档各自负责什么

| 文档 | 本次作用 |
|---|---|
| 01-项目需求文档.md | 明确两种角色、私人附件、两标签和历史聊天的用户需求 |
| 02-技术设计文档.md | 改写目标架构、接口、存储边界与前端职责，取消旧的单租户/来源审核过滤目标 |
| 03-功能模块实施文档.md | 按A～C基础、M4-A、D/E、M4-C调整顺序，补充五项建议验收 |
| 04-项目进度文档.md | 记录仅完成方案，保留真实开发和测试的历史证据 |
| 05-参考文档实施取舍.md | 将登录和用户隔离从以后补做改为当前优先目标 |
| 07-各阶段开发说明.md | 记录本轮逐文件文档用途与后续代码阅读顺序 |
| 08-架构设计说明.md | 保留当前源码说明，单列目标字段、约束、关系与未执行迁移 |
| 09-双角色与对话附件改造方案.md | 汇总页面、权限、附件流、迁移兼容、分步交付和验收场景 |

### 后续代码的建议阅读/开发顺序（尚未改动）

| 小步 | 文件范围 | 通俗作用与调用关系 |
|---|---|---|
| A | 拟新增models/user.py、schemas/auth.py、services/auth.py、api/auth.py及身份依赖；修改models/trip.py、services/trip_service.py、services/requirement/history.py和对应API/迁移 | 先认识当前用户，再确定哪条会话属于他；HTTP准备身份，服务限制读写范围，表约束守住归属 |
| B | frontend/src/api/requirements.ts、useRequirementConversation.ts、App.vue；拟新增登录和历史列表组件 | 前端先取账号和历史，选定会话再读消息；新建不清除历史，需求面板移除但后台状态保留 |
| C | models/document.py、schemas/document/base.py、services/document/service.py/metadata.py/search.py、api/document及DocumentPanel/Upload/MetadataFields/Editor/Chunks/Job组件 | 后端先统一两标签和共享知识库范围，前端再做城市分组和简洁预览；移除独立DocumentSearch页面块 |
| M4-A（当前已实施，见本文M4-A文本行程智能体节） | 实际位于services/itinerary/graph.py、tools.py、rules.py与schemas/itinerary.py；扩展llm/client.py、services/chat/service.py及草稿/历史服务 | 图状态和原生工具选择调用真实业务，代码校验并原子保存行程、需求与差异摘要，支持撤销 |
| D | 拟新增models/conversation_attachment.py、schemas/attachment.py、services/attachment.py、api/attachments.py、llm/vision.py及聊天附件组件 | 保存当前对话原件，读取文字或图片，返回识别结果；不进入知识库切片/索引 |
| E / M4-B | services/itinerary/graph.py、services/chat/service.py、附件服务及行程展示组件 | 将附件用途和识别结果送入M4-A已有规划/校验/保存流程，不新增第二套规划器 |
| M4-C | services/itinerary内审查/恢复节点、main.py生命周期及检查点配置 | 独立审查与有限返工，持久暂停/恢复；业务存储仍使用原服务 |
| C后增强 / F | 资料服务/元数据/任务API及前端；F单独扩展documents和迁移 | 批量只改选中标签，只重试失败文件；新版就绪后切换当前文件，失败保留旧版 |

表中拟新增路径是职责建议，不要求一次创建所有空文件。实际编码继续按同层风格、每两三个文件一批推进；每步补真实逐文件说明，不能提前将上表写成完成清单。A/B/C基础后即M4-A文本智能体；D验收理解附件，E/M4-B验收附件更新草稿，M4-C验收完整恢复。切片质量另以代表样本检查后决定是否调整parser/chunker，不因增加“Agent”文件而重复实现已有业务。

## M3景点详细地点与门票展示修复（2026-09-15）

截图中的问题发生在问答后的地点补查，重新上传、切片或建索引不会补出不存在的地址或票价。本步修复地图别名、市区县识别，并为已经选出的景点定向补查缺失门票/地址。页面只显示有依据的收费或部分收费信息，免费与未知均省略门票栏；后台仍区分这两种状态，未知不会改成免费。

| 顺序 | 文件 | 所属层与通俗作用 |
|---|---|---|
| 1 | backend/tests/test_amap_matching.py、test_attraction_details.py | 测试层：复现地图别名、区县、行政镇以及第二遍回答无法取得补查证据的问题 |
| 2 | backend/app/services/amap.py | 外部工具层：核对POI/行政区结果，组合省市区详细地址，保留地图实际名称 |
| 3 | backend/app/schemas/document/answer.py | 格式层：新增匹配类型、地图原名、地址原文和逐景点补查记录，旧快照均有默认值 |
| 4 | backend/app/services/document/answer.py | 问答业务层：先形成景点，再按缺项查询，以新证据完成第二遍回答，补查不重复执行 |
| 5 | backend/app/services/document/places.py | 证据核对层：地址必须逐字来自同地点原文；免费也支持明确“无需门票”等说法 |
| 6 | frontend/src/api/documents.ts、components/AttractionCards.vue | 前端合同与展示层：隐藏免费/未知门票栏，显示完整地点、地图名称、行政镇中心说明和地图入口 |
| 7 | docs/08与现有聊天流程图 | 文档层：同步查询次数、证据编号和保存合同；数据库表及迁移仍为0007 |

数据走向：本地/网页首查 → DeepSeek景点草稿 → 高德定位 → 对缺项定向搜网页 → 核对地址/门票原句 → 保存完整卡片。地图失败时只能展示有来源的地址，不能自行制造门牌、坐标；行政镇中心不当作具体景点入口。旧聊天保存当时结果，不自动重写；新问答使用修复后的流程。详细验收见08第10.20节。后端397项测试通过、0跳过，66文件mypy、目标Ruff、前端类型检查与构建、alembic check通过；现有聊天SVG流程图已同步并重新渲染核对。

整轮验收曾复现模型把地图地址填写成资料引文而导致502：新增失败回归后，后端在已有地图地址时丢弃重复的模型地址，前端直接使用地图结果；地图无地址时仍严格要求同地点原句。模型输出为空引用编号时仍最多修正一次，失败不保存，不能把这类输出稳定性问题写成全部消失。最终新会话原问题成功保存一轮，页面显示湖滨路步行街完整地址与地图入口、无门票占位栏，刷新恢复通过；三处地点的独立真实地图核对与页面验收分开记录。

## M3批量自动处理与标签预填（2026-09-15）

当前使用顺序：多选文件 → 点击一次“上传并自动处理” → 每份保存后自动切片和建立索引 → 在本批结果看各自进度。切回聊天继续上传；关闭或刷新页面后，已提交任务继续，尚未发送的文件需重新选择。后台失败或暂停后可点每份的“重试自动切片和索引”，不重传原文件；服务中断仍需显式重试。

新资料保存时预填城市、来源和“待审核”，可人工修改。文件名按常见城市词表识别；正文支持独立的“城市：景德镇市”“来源：作者笔记”或city/source字段行。城市去掉末尾“市”；多城市或与文件名冲突则留空，不从出发城市或正文偶然提及的城市推断。来源优先取唯一明确标注，否则识别文件名中的ChinaTravel/LvBanGPT；无法确定留空。当前是本地规则，不是完整地名识别模型。

已有资料点击“识别并补全空标签”：只填数据库空字段，保留人工城市、来源、审核和POI；重复上传也保留原标签。POI是地图平台的单个地点标识，多地点攻略通常留空，不能把POI标注当成坐标已核验。标签不控制自动入库审批，待审核资料的索引仍可用于当前聊天。

| 顺序 | 文件 | 所属层与通俗作用 |
|---|---|---|
| 1 | backend/tests/test_document_automation.py | 测试层：先复现缺少预填和自动任务，再验证重复上传保护、模糊城市、归属、提交失败和并发上限 |
| 2 | backend/app/services/document/metadata.py | 标签业务层：从文件名和明确标注提取可确定的城市、来源，不调用模型或地图 |
| 3 | backend/app/services/document/service.py、schemas/document/base.py | 资料业务与格式层：新建时保存预填值；已有资料仅补空值；上传结果区分文件保存与任务提交失败 |
| 4 | backend/app/api/document/routes.py | HTTP层：auto_process=true上传后提交索引任务；新增metadata/prefill接口供旧资料补全 |
| 5 | backend/app/api/document/jobs.py、services/document/jobs.py、app/main.py | 后台入口与任务业务层：最多同时执行两份任务，等待空位时不占线程或数据库连接，复用原切片和8段索引批次 |
| 6 | frontend/src/api/documents.ts、components/DocumentUpload.vue | 前端接口与上传层：自动处理随上传提交，整批轮询逐份进度，失败可单独续建 |
| 7 | frontend/src/components/DocumentMetadataEditor.vue、DocumentMetadataFields.vue | 标签页面层：展示预填值、补全空值，保留未保存输入，说明POI用途 |
| 8 | backend/tests/test_document_metadata.py | 既有测试：新资料审核初值改为pending，原归属与人工标签修改检查保留 |
| 9 | docs/08、docs/diagram/travelmind-documents及travelmind-overview | 文档层：更新自动处理调用链和两张现有SVG/PNG，数据库仍为0007 |

真实浏览器已完成一次三份Markdown批量上传，每份20/20段自动索引；同批.exe文件单独拒绝。预填杭州/ChinaTravel/pending/空POI可见；清空城市并手填来源后补全，城市恢复杭州而人工来源保留。三份临时资料核对任务和真实Milvus进度后已通过DELETE清理，原有攻略保留。最终373项后端测试通过、0跳过，66文件mypy、本轮目标Ruff、前端类型检查与构建、数据库结构一致性检查通过。两个断线排队问题已通过失败回归复现、修复并独立复核。更多契约见08第10.19节。以下各节为此前小步记录。

## M3收尾：资料标签、后台恢复与固定集评测（2026-09-15）

此前小步补齐阶段文档列出的三项工程工作；最新自动流程见顶部。此前使用顺序：上传并核对正文 → 在“人工资料标签”保存城市、来源、审核状态或POI → 点击“后台建立 / 续建索引” → 页面可关闭，回来刷新进度 → 用标签搜索。失败资料可选择后台重新读取原件；暂停和服务中断后均由用户显式重试，索引复用已确认片段。来源主次不自动加权，标签为空时不猜测。

| 顺序 | 文件（后端相对backend，前端相对frontend） | 所属层与通俗作用 |
|---|---|---|
| 1 | tests/test_document_metadata.py | 测试层：先复现缺少标签功能，再验证归属、修改、删除态、前置筛选和501份资料跨批排序 |
| 2 | app/models/document.py、migrations/versions/0006_document_metadata.py | 数据库层：增加四个可空标签，已有资料不重传、不重建向量 |
| 3 | app/schemas/document/base.py、search.py | 格式层：规定标签长度、审核值及搜索条件 |
| 4 | app/services/document/service.py | 资料业务层：按归属保存标签、读回详情和筛选列表 |
| 5 | app/services/document/search.py、vector_store.py | 检索层：先筛允许的资料编号，再在其中进行向量搜索 |
| 6 | app/api/document/routes.py、search.py | HTTP层：接收标签更新和筛选，调用业务服务 |
| 7 | src/api/documents.ts、components/DocumentMetadataFields.vue、DocumentMetadataEditor.vue | 前端标签组件：编辑和保存；后台刷新同份资料时保留未保存草稿 |
| 8 | tests/test_document_jobs.py | 测试层：验证进程强制中断、重复提交竞争、暂停续建和失败状态 |
| 9 | app/models/document_job.py、models/__init__.py、migrations/versions/0007_document_jobs.py | 数据库层：保存每份资料最近任务，索引失败不破坏已读正文 |
| 10 | app/schemas/document/job.py、services/document/jobs.py | 格式与业务层：五种任务状态，持锁执行，逐批存进度，中断可重试 |
| 11 | app/api/document/jobs.py、main.py、api/health.py | 接口与入口层：返回202后后台执行，启动识别遗留任务；数据库断开仍有存活检查 |
| 12 | src/components/DocumentJob.vue、DocumentPanel.vue、DocumentSearch.vue、DocumentChunks.vue、api/http.ts | 页面层：启动、暂停、轮询、完成后刷新索引，阻止旧请求覆盖新状态；正常处理尚无任务的null |
| 13 | evals/documents.v1.json、scripts/document_eval.py、evaluate_documents.py、tests/test_document_eval.py | 评测层：冻结29段33题，走真实生产链路，分别评分召回、引用和全文语义 |
| 14 | app/services/document/answer.py | 问答层：根据真实失败收紧提示，不凑字数，不把沙盒经营参数当现实事实 |
| 15 | docs/08及两张现有架构图 | 文档层：同步七张表、字段字典、调用链、接口和证据边界 |

先让标签实际影响检索，再让任务离开页面后仍能处理，最后用固定问题核对回答。07说明逐文件顺序，08第7.5/10.17/10.18说明字段、契约和运行证据。本机0007已迁移，ORM无差异；后台按单Uvicorn进程验收，没有增加Celery/Redis、OCR或上传字节断点续传。

在backend运行 `.venv/Scripts/python.exe -X utf8 -m scripts.evaluate_documents` 只检查题库。真实调用加 `--live --env-file .env --output ../temp/reports/新报告名.json`，会调用Embedding和DeepSeek；不用旧报告名覆盖成绩。

题库及全文核对由Agent阅读原文完成，不是独立人类盲测。最终保留第二版：Recall@5=100%、MRR@5=0.9464、事实覆盖51/51、有效引用28/28、无证据5/5、注入2/2，严格全文忠实度32/33。第三次只增加条件提示未改善，已撤回并保留报告。**工程闭环和召回基线达标，不等于所有回答都正确**；q26把“需自带野餐垫”写成场地提供，作为明确质量限制保留。见三轮对比（历史报告`m3-comparison-20260915.md`，本地阶段产物已清理）。

两套runtime来源目录保持原样，没有新增README或临时测试脚本。下方均为历史小步记录，其中“尚未实现”只对应当时。

**最终验证：**367项后端测试通过、0跳过，56.22秒，1条既有Starlette/AnyIO弃用提示；63个app文件及2个评测脚本共65文件mypy通过，本轮目标Ruff通过，Vue类型检查与Vite构建通过。全app Ruff仍有3条原有长行（api/trips.py两条、services/budget_service.py一条），未顺手改动。真实浏览器覆盖标签保存/多条件搜索/无匹配、后台中断与刷新恢复；两份本轮临时资料均经名称与正文核对，用DELETE清理后读回404，既有资料保留。三轮真实评测各自的临时库、集合和目录均清理。未提交或推送。

## M3批量上传与文件名查找（2026-09-15）

这一小步方便后续上传多份景点资料：一次多选、逐份上传、逐项显示成功或失败；支持当前文件结束后暂停、继续待传文件、单独重试未确认的上传。文件名查询由数据库先筛选再分页，覆盖整个当前归属的资料库。未改动向量排序或自动建立索引。

| 顺序 | 文件 | 所属层与通俗作用 |
|---|---|---|
| 1 | backend/tests/test_documents.py | 测试层：先复现关键词被忽略，再验证筛选后分页、归属隔离、符号原字匹配和参数长度 |
| 2 | backend/app/services/document/service.py | 业务层：list按文件名查找，不读取全文，也不把其他归属的资料混进来 |
| 3 | backend/app/api/document/routes.py | HTTP层：给原列表接口增加可选q参数，省略时仍列出全部资料 |
| 4 | frontend/src/components/DocumentUpload.vue | 上传组件层：选择多份文件，逐份调用原上传接口，保留各自结果与重试入口 |
| 5 | frontend/src/api/documents.ts | 前端接口层：用URLSearchParams传递关键词和分页参数 |
| 6 | frontend/src/components/DocumentPanel.vue | 页面层：组合上传组件、资料列表和预览；列表关键词与聊天检索范围互不影响 |

调用顺序：选多份文件 → DocumentUpload逐份发送 → 原POST /documents查重并保存 → 批次结束刷新列表和预览。查找顺序：文件名关键词 → GET /documents?q=关键词 → DocumentService.list → PostgreSQL筛选后分页。迁移仍为0005，没有新增表、依赖或后台工作进程。

### 后续景点资料怎么准备

优先使用可复制文字的Markdown、TXT或文档；一份文件可以只写一个景点，也可以按同一城市整理多个景点，但每个景点需要独立标题。当前不直接上传CSV/Excel；这类表格先整理成Markdown或TXT。不要只填景点名字和几行标签，正文要保留有依据的介绍。

文件名建议：`城市-景点-来源-核对日期.md`。例如占位格式为`某城市-某景点-官网-YYYY-MM-DD.md`，日期应填实际核对时间，不能拿上传时间代替。正文可以沿用下面的简短结构，缺项直接写“待核实”，不要补造：

```markdown
# 城市｜景点正式名称
来源：原始页面链接或资料名称、版本
核对日期：YYYY-MM-DD
景点介绍：有依据的背景、特色、主要看点
适合怎样游玩：散步/亲子等适用情况及理由
地址：城市、区县、详细地址；地图POI编号有可靠结果时再填
门票：免费/收费/部分收费/待核实；说明票种、金额、适用日期与依据
开放时间和预约：保留适用日期、预约方式和依据；未知写待核实
```

ChinaTravel-Sandbox继续作主参考，LvBanGPT作补充。两套资料的原始目录和清单保持原样，没有新增README。文件中的来源/城市描述是正文；本次收尾增加独立人工标签，须在资料页明确填写才参与筛选，不从文件内容自动推断，也不代表来源优先级。

使用步骤：选择资料（可多选） → 上传待传资料 → 打开资料核对正文 → 查看并生成片段 → 刷新索引进度并建立索引 → 在资料搜索或聊天中验证。上传本身不调用模型；未建立索引的资料不会自动用于回答。页面刷新后，已经保存的资料还在，未发送的文件需要重新选择；这不是后台自动续跑。

验证：351项后端测试通过、0跳过；59文件mypy、目标Ruff、前端类型检查与构建通过。真实页面验收多选、空文件拦截、损坏PDF失败后继续上传、重复内容复用、文件名筛选、当前文件后暂停以及服务断开/恢复后的单份重试。本步新建的4份验收档案已按文件名和内容哈希核对后通过正式删除接口清理，未建立索引；原有资料保留。M3仍待来源/POI元数据过滤、完整后台恢复及固定题库召回和事实忠实度评测。

## M3重新解析与资料目录整理（2026-09-15）

本步沿用ChinaTravel-Sandbox主参考、LvBanGPT补充。`backend/data/runtime`根目录只保留`chinatravel-sandbox`和`lvbangpt`：前者的documents保存30份整理资料、raw保存原包及上游来源说明；后者的raw保存杭州PDF；两者各自保留manifest.json，路径已更新且31个内容文件哈希一致。没有新增README。临时样本、调试JSON及旧的自写README已移出项目；永久删除被自动审批审查拒绝，仍位于系统临时目录`travelmind-runtime-cleanup-273394c5aa764255bdc9e567e1eee5a9`，不能写成已永久删除。正式回归测试继续位于backend/tests，不再向runtime写入测试脚本。

| 顺序 | 文件 | 所属层与通俗作用 |
|---|---|---|
| 1 | backend/tests/test_documents.py | 测试层：先复现没有重试功能，再验证临时故障恢复、仍损坏、缺原件、文件被替换、归属、锁和重复请求 |
| 2 | backend/app/services/document/service.py | 业务层：retry_parse只重新读取失败档案；核对原文件路径、大小和内容指纹，调用已有parser并整体保存结果 |
| 3 | backend/app/api/document/routes.py | HTTP层：POST /documents/{id}/retry返回最新详情；有竞争立即409，避免排队反复解析 |
| 4 | frontend/src/api/documents.ts | 前端接口层：发送重试请求，把最新状态和正文交给页面 |
| 5 | frontend/src/components/DocumentPanel.vue | 页面层：失败资料显示重新解析；处理期间避免重复操作，切换资料时清掉旧提示 |

重试不调用模型；已有parsed资料直接返回，不改动原文、片段及向量。失败处理整体在事务中完成，进程中断回滚后仍可手动重试。本步没有引入后台队列或parsing状态；迁移仍为0005。

验证：350项后端测试通过、0跳过，59文件mypy、目标Ruff和前端构建通过。真实页面对缺失原文件的旧验收档案显示明确失败原因。LvBanGPT杭州PDF已实际上传为87060ad6-08d0-4080-94bf-3b651bebda5a，18页正文、46片段，6批完成46/46索引；限定资料搜索返回5段并保留PDF页码，页面显示可搜索。正文和票价是历史资料，不能称为实时信息。详细边界见08第10.15节。

## M3资料删除与中断重试（2026-09-15）

本小步补齐资料退出知识库的流程：确认删除 → 提交停用状态 → 清理各模型版本向量 → 清理原文件 → 删除档案和关联片段。失败时停在“待清理”，用户可刷新后重试；只删除本次指定资料，历史聊天快照保持原样。

| 顺序 | 文件 | 所属层、作用和调用关系 |
|---|---|---|
| 1 | backend/tests/test_document_delete.py、test_vector_store.py | 测试层：先证明缺少删除功能时用例会失败，再验证中断、隔离、行锁、路径、迁移和真实跨版本清理 |
| 2 | backend/app/models/document.py | 数据库模型层：增加deleting状态及约束，保留待清理记录 |
| 3 | backend/app/schemas/document/base.py | 数据格式层：把待清理状态传给前端，不误写为读取失败 |
| 4 | backend/migrations/versions/0005_document_delete.py | 迁移层：只扩展两项CHECK约束，不新增表或字段；有待清理记录时阻止回滚旧约束 |
| 5 | backend/app/services/document/service.py | 业务层：先单独提交停用状态，再持行锁完成清理；文件或向量故障后仍可重试 |
| 6 | backend/app/services/document/vector_store.py | 向量存储层：枚举本应用集合，按owner和document编号删除，强一致回查确认无残留；不删集合 |
| 7 | backend/app/api/document/routes.py | HTTP接口层：提供DELETE接口，只有存在片段时才创建向量客户端；不调用模型 |
| 8 | frontend/src/api/documents.ts | 前端接口层：接收deleting状态，正确处理没有JSON正文的204响应 |
| 9 | frontend/src/components/DocumentPanel.vue | 页面层：确认/取消、待清理/重试；删除时清空旧预览和搜索结果，停止后续索引批次 |

现有DocumentSearchService只返回parsed资料，因此删除后的检索过滤沿用原逻辑。未切分的资料无需向量配置；已有片段时，即使当前没有向量连接，也必须保留停用记录，不能跳过可能存在的索引。

验证：344项后端测试通过、0跳过，含真实PostgreSQL临时schema及Milvus两个随机模型版本；59文件mypy、变更目标Ruff、前端构建通过。本机已应用0005且alembic check无差异。浏览器验证取消、真实文件占用失败、刷新恢复待清理、解除占用后重试成功；仅操作本步创建的m3-delete-ui-fixture.txt。细节见08第10.14节。

本步是资料删除和删除重试，尚不是失败文件重新解析、后台任务恢复或M3全阶段验收。随后仍须完成固定题库召回/事实忠实度评测及其余生命周期目标。

## 换地点和住宿搜索修正（2026-09-15）

先读取失败两轮的快照，确认搜索实际返回了哪些内容；再以原问题调用搜索服务作对照。测试文件先复现新问题混入旧地点及门票词：tests/test_attraction_cards.py负责网页搜索词，tests/test_chat_rag.py负责知识库检索与住宿补问。

三个业务文件依次处理：services/document/search.py提供共用追问判断；services/document/answer.py去掉固定门票搜索词并约束住宿价格依据；services/chat/rag.py在知识库检索时也隔开新旧话题，按预算筛选住宿先检查入住条件，缺项时先补问，避免模型推测现实房价。前端卡片显示保持不变，未增加依赖、接口或数据库迁移。实际验收见08第10.13节。

## 景点回答只保留卡片（2026-09-15）

先修改frontend/src/App.vue的显示条件：有景点卡片就显示卡片，否则显示普通文字。再同步08与聊天架构图；接口、数据库和已保存reply不变。前端构建通过，浏览器刷新后两轮三张卡片恢复，重复的介绍气泡隐藏，苏堤的真实高德位置链接仍可见。

## 高德Key复验（2026-09-15）

先重启后端加载Key，再用amap.py查询真实地点，最后由原聊天验证卡片。苏堤已返回真实POI编号和坐标；湖滨路步行街因高德名称为湖滨步行街而未严格匹配，不是Key故障。

本次两文件顺序：先在tests/test_attraction_cards.py复现同日期不同写法被拒绝；再在app/services/document/places.py统一中文日期与YYYY-MM-DD的比较格式，原句和事实检查保留。37项相关测试、59文件mypy及目标Ruff通过。运行记录见08第10.12节。

## M3景点卡片与外部证据（2026-09-15，源码已接入）

目标：知识库优先查找景点，按需查询网页证据，推荐景点都核对地图位置；自然回答和景点卡片使用同一份已保存结果。后续地图草稿可直接使用坐标，当前不生成每日行程。

实施顺序：

1. 先定义地图坐标、网页证据、景点卡片和门票格式；测试无效坐标、票价与来源不匹配、旧快照读取。
2. 扩展高德地点信息；增加Tavily搜索连接（默认未启用，不自动注册账号），只使用返回的网页摘要，不使用搜索服务代写的答案。
3. 接入知识库＋网页证据生成、景点定位与最终卡片；限制查询和生成次数，失败保留已确认内容，禁止凭模型记忆造票价或坐标。
4. 前端每轮回答展示景点卡片；已有会话刷新不重新联网。同步08数据契约、总图和实际验收。

事实边界：地图人均消费不代表门票；网页查询时间不代表信息更新日；公开网页票价展示为参考，必须保存原句、来源、票种及适用说明。最初无高德和搜索Key；运行时发现backend/.env已提供搜索Key，真实Tavily查询成功。高德后续已由用户补齐Key并完成真实查询，最新结果见本文件顶部复验记录。


逐文件开发顺序（同一步内先写对应边界测试，再接业务）：

| 顺序 | 文件 | 通俗说明与调用关系 |
|---|---|---|
| 1 | backend/app/schemas/document/answer.py | 数据格式层：先约定网页证据、地图坐标、门票和景点卡片长什么样，旧会话也能读 |
| 2 | backend/app/services/amap.py | 工具连接层：向高德查询真实地点，解析中心和入口；人均消费仍与门票分开 |
| 3 | backend/app/config.py、backend/.env.example | 配置层：增加独立搜索密钥和允许网站名单；未配置不发请求 |
| 4 | backend/app/services/web_search.py | 工具连接层：通过Tavily查允许域名，只取网页内容摘要，限制数量、长度和超时 |
| 5 | backend/app/services/document/places.py、maps.py | 景点业务层：检查名称、票价原句与说明中的数字，给每个推荐景点安排定位 |
| 6 | backend/app/services/document/answer.py | 问答业务层：将原文和网页交给DeepSeek，再交入地图结果，正常生成两遍，格式问题可额外修正一次 |
| 7 | backend/app/services/chat/rag.py、service.py | 聊天业务层：接入整轮流程；保留历史景点顺序，重试不会重复查询 |
| 8 | backend/app/api/requirement/history.py | HTTP接口层：准备搜索与地图连接，继续调用聊天业务，不在接口里写推理逻辑 |
| 9 | frontend/src/api/documents.ts、useRequirementConversation.ts | 前端合同与状态层：接收卡片、放进聊天消息，并从历史恢复 |
| 10 | frontend/src/components/AttractionCards.vue、App.vue | 页面层：显示介绍、推荐原因、门票、地址；有可靠工具坐标才显示高德链接 |
| 11 | backend/tests/test_attraction_cards.py、test_web_search.py | 测试层：外部HTTP替身、坐标范围、真假票价、草稿保留、网页回退与无Key行为 |
| 12 | backend/tests/test_amap.py、test_document_answer.py、test_chat_rag.py | 回归层：已有票价也要定位；顺序指代、整轮持久化和幂等重试继续正确 |
| 13 | docs/08-架构设计说明.md、docs/diagram/* | 说明层：同步当前调用链、JSON字段字典、总图与两张流程图 |

实际运行：真实DeepSeek已在页面生成湖滨路步行街和苏堤两张卡片，刷新后完整恢复一轮；未出现引用编号或“资料中”等来源套话。高德未配置，所以显示位置未核实、没有地图链接。独立诊断调用Tavily返回允许域名的网页摘要；成功保存的页面轮次网页结果为空，正确保留知识库回答并提示没有可用补充。先前一次页面请求返回502校验错误且未保存，未留存该次原始模型响应，不能断言已找到其具体失败字段；后续成功不代表模型输出完全稳定。

本轮327项测试通过、0跳过；59文件mypy、目标Ruff及前端构建通过，没有新增依赖和数据库迁移。自动验证与边界详见08第10.11节；当前仍在codex/m3，未提交或推送。


## 架构图阅读优化（2026-09-15）

本次只调整图和文档，业务源码、接口、数据库及服务状态均未修改。按用户反馈统一采用浅色背景、白色卡片和低饱和度模块配色。开发与阅读顺序如下：

1. `docs/diagram/travelmind-overview.svg`：重画M1～M3总图，以竖向分层和三个业务卡片取代细碎文件连线。
2. `docs/diagram/travelmind-chat-rag.svg`：说明M2需求与M3回答的组合流程；重试、缺少证据和地图补查放在侧边说明。
3. `docs/diagram/travelmind-documents.svg`：说明资料从上传到索引、再到聊天或原文搜索的顺序。
4. 导出三张同名PNG；同步`travelmind-overview.mmd`结构参考和08文件第2节中的图片、链接及维护规则。

验证：三张SVG均可解析；2倍PNG已生成并逐图检查；文字边界检查无越界，图内未放入未实现的自动行程能力。此次仅作图文验收，不重复运行或声称通过业务测试。


## M3结构整理（2026-09-15）

本轮目标：按“技术层 / 业务文件夹 / 具体职责”查找代码，并清除HTTP接口之间直接复用处理函数的关系。沿用现有中文注释；不增加依赖，不修改数据库表和接口URL。

开发计划与验收顺序：

1. 将schemas、services、api下资料与需求文件分别归入document、requirement；更新调用方、命令脚本和测试的导入。验证：导入安全与现有测试。
2. 提取services/chat/service.py：处理需求回复及持久化对话；api只处理请求和连接资源。验证：连续对话、幂等重试、旧版本冲突、RAG失败不得保存。
3. 将地图补查规则移入services/document/maps.py；主回答函数保留模型循环。分开模型内部map_queries和最终结果，兼容已保存的空map_queries快照。验证：地图次数、证据校验、旧快照恢复。
4. 将统一错误和健康检查放入api/errors.py、api/health.py；前端共用api/http.ts；测试共用tests/helpers.py。验证：健康/错误响应、前端构建、测试独立导入。
5. 同步本文逐文件说明、08架构说明及总图；运行完整测试、类型检查和实际服务健康/历史读取。

重构前基线：312项测试通过，0跳过，1条现有依赖弃用提示（连接真实PostgreSQL与Milvus，模型由测试替身提供）。本次改动前源码副本保存在临时目录，用于比较本轮变化，避免把此前未提交的M3代码当成本轮新改动。


本文作为各阶段统一的逐文件开发说明；整体模块关系、当前接口与逐字段数据库字典维护在[08-架构设计说明](./08-架构设计说明.md)。


本轮文件顺序与职责：

| 顺序 | 文件（相对backend/app，前端与测试另标） | 功能和调用关系 |
|---|---|---|
| 1 | schemas/document/base.py、search.py、answer.py | 先归类资料、片段、搜索结果和回答格式；由对应业务服务使用 |
| 2 | schemas/requirement/base.py、chat.py、history.py | 归类旅行条件、单轮回复和多轮历史 |
| 3 | schemas/requirement/update.py；services/document/parser.py、chunker.py | 归类需求修改格式；parser先读正文，chunker再切短片段 |
| 4 | services/document/service.py、search.py、vector_store.py | 管资料读写，建立和查找索引，由Milvus客户端存取向量 |
| 5 | services/document/answer.py；services/requirement/prompt.py、merge.py | 整理证据回答；需求提示词与合并规则各守其责 |
| 6 | services/requirement/extract.py、history.py；services/chat/rag.py | 提取条件、保存历史、普通知识检索与证据回答 |
| 7 | api/document/routes.py、search.py、upload_limits.py | 接收资料和索引请求，控制上传体积 |
| 8 | api/requirement/routes.py、history.py；services/chat/service.py | 两个HTTP入口共用需求回复业务；service串联整轮处理 |
| 9 | api/document/dependencies.py及两处接口调用方 | 共用检索连接准备；完成重试检查后才启用连接 |
| 10 | services/document/maps.py、answer.py | maps处理补查依据与去重，answer处理模型循环与回答 |
| 11 | schemas/document/answer.py；tests/test_document_answer.py | 先用测试复现内部字段泄漏，再分开模型数据与最终结果，验证旧快照兼容 |
| 12 | frontend/src/api/http.ts、requirements.ts、documents.ts | 共用响应解析，保留各自业务请求；useRequirementConversation从http导入ApiError |
| 13 | main.py；api/errors.py、health.py | 拆开应用装配、统一错误和健康检查 |
| 14 | backend/tests/helpers.py及调用它的测试文件 | 共享数据和模型替身，测试不再互相借函数 |

调用顺序：`前端 → api/requirement/history → services/chat/service → 需求提取与合并 → chat/rag → document/search → document/answer → 按需document/maps → requirement/history保存`。无状态接口只调用共用的需求回复函数，仍不保存或做知识问答。

本轮自动验收：314项测试通过、0跳过；mypy检查57个源码文件通过；前端构建通过。实际服务验收和完整架构以08文件10.10节为准。

## 当前成果：M3第五小步——主聊天默认RAG与后台证据保存（2026-09-14）

### A. 这一小步能做什么

直接在原有“旅行对话”输入框提问。本节最初实现要求知识性回答必须有检索证据；2026-09-17统一上下文改造后，知识库和网页改为增强材料，资料不足或可选检索失败不再阻断一般知识、景点介绍和闲聊。地图实时事实、结构化卡片和可追溯引用仍按各自严格合同校验，不能由模型补造。登记人数、预算等个人需求时继续显示补问或登记提示，餐馆人均消费不写入全团旅行预算。当前调用链和边界以本文顶部及08第10.29节为准。

资料页负责上传、片段、索引和原文搜索，没有额外的问答按钮。回答与引用一起保存，刷新恢复不调用模型。聊天只展示自然语言；不说“资料中”“根据知识库”等来源套话，不显示引用编号或原文展开区。历史中已有的引用编号仅在显示时隐藏，不重写已保存的旧回答。

### B. 开发顺序与文件作用

| 顺序 | 文件 | 所属层与作用 |
|---|---|---|
| 1 | `backend/tests/test_document_answer.py` | 测试层：先检查缺失功能，验证空资料、坏JSON、漏引用和伪造编号 |
| 2 | `backend/app/schemas/document/answer.py` | 数据格式层：规定回答状态、要点和来源，不让模型生成文件名或伪造原文 |
| 3 | `backend/app/services/document/answer.py` | 问答业务层：组织不可信资料提示词，调用DeepSeek，按需补查地图后最多重写一次，校验格式与引用编号 |
| 4 | `backend/tests/test_chat_rag.py`、`app/services/chat/rag.py` | 测试与聊天业务层：普通旅行知识检索、兼容长消息、合并证据，组合知识回答与需求补问 |
| 5 | `backend/app/schemas/requirement/chat.py`、`api/requirement/history.py`、`services/requirement/prompt.py` | 数据格式、HTTP与提示词层：增加knowledge快照；主聊天保存前执行RAG；同句登记条件和知识问题时优先保留条件 |
| 6 | `frontend/src/api/documents.ts`、`api/requirements.ts`、`useRequirementConversation.ts` | 前端合同与状态层：携带引用、恢复整轮，知识问答不覆盖有效需求卡片 |
| 7 | `frontend/src/App.vue` | 页面组合层：原发送按钮直接使用RAG，只展示自然回答；原文仍可在资料管理页查看 |
| 8 | `backend/tests/test_requirement_history_api.py`、`test_import_safety.py` | 回归层：旧需求链经过空知识库替身，导入新增模块不联网 |

### C. 一次消息的执行顺序

当前顺序为`原聊天输入 → 查本人历史/消息编号/版本 → 统一历史、话题和摘要 → 一次本轮理解 → chat/map/plan路由 → 可选知识库、网页、百度或行程工具 → 自然回答或结构化结果 → 保存整轮JSON → SSE done后显示最终回答`。旧无状态需求接口仍只执行需求提取。

已经保存的消息重试直接返回旧结果，不再次检索或生成。检索时没有持有会话写事务；完成后才锁行核对版本，所以慢回答不会覆盖其他页面的新消息。数据库失败、模型失败或坏引用均不会提交半轮。

### D. 验收与限制

最终全量299项测试通过、0跳过；44文件mypy、变更Ruff、中文注释检查和前端构建通过。真实模型和浏览器记录见[08第10.8节](./08-架构设计说明.md)。没有新增数据库列或迁移，引用快照写入既有`requirement_turns.response_json.knowledge`；旧历史的knowledge默认为null。

本步是固定RAG调用链，还不是自动规划行程的Agent。主参考ChinaTravel-Sandbox、补充LvBanGPT的选择沿用；尚无来源权重、城市元数据硬过滤、重排序或正式召回评测。引用编号校验能防止引用不存在的片段，不能自动证明每句总结的含义都准确。沙盒价格与时间不得解释为实时事实。

### E. 本次补充：景点介绍、门票说明与高德查询

2026-09-14：景点回答默认包含名称、具体介绍、游览特色和门票说明。公开静态票价保留年月，沙盒价格与无单位时长不能解释成现实事实。后台可执行模型提出的高德查询，再将结果交给模型；仍只展示自然文字，不新增按钮。

开发顺序如下：

| 顺序 | 文件 | 通俗说明 |
|---|---|---|
| 1 | `backend/tests/test_amap.py`、`test_document_answer.py` | 先用模拟响应验证地点匹配、缺密钥、非法查询、消费含义和次数上限 |
| 2 | `backend/app/config.py`、`schemas/document/answer.py`、`services/amap.py` | 配置层保管Key；数据层规定查询与快照；工具连接层只查同城同名唯一地点 |
| 3 | `services/document/answer.py`、`services/chat/rag.py`、`api/requirement/history.py` | 知识回答提出补查，程序对结构化景点的缺门票漏查补请求，统一校验后执行；最多一次补查阶段、三个地点，再重写一次答案 |
| 4 | `backend/tests/test_chat_rag.py`、`test_import_safety.py` | 验证聊天跳过纯标题、地图快照保存恢复、重试不重复查询、导入不联网 |
| 5 | `backend/.env.example`、`frontend/src/api/documents.ts` | 提供地图配置位置与响应类型；不新增界面入口 |
| 6 | `data/knowledge/杭州-散步景点介绍与门票补充.md` | 补充云栖竹径、湖滨路步行街、苏堤的公开介绍，来源与日期留在知识文件 |

实际已上传补充文件`c9d63453-9703-4d81-a388-a8fc14a6c03c`，7段全部索引完成，与原2444段合计2451段。聊天对景点关键词附加“景点介绍 游览特色 门票”，取10个候选并跳过纯标题，再选最多5段；原资料页搜索不变。不硬编码杭州或指定景点优先级。

地图配置位置是`backend/.env`中的`TRAVELMIND_AMAP_API_KEY=`，需填写高德“Web服务”类型Key并重启后端。当前用户没有Key，留空。高德公开接口`business.cost`只有人均消费语义，不能证明门票或免费；源码没有虚构门票字段。没有Key时返回unconfigured，不发送HTTP请求；真实高德调用仍待配置验收。知识介绍、公开门票参考和真实DeepSeek已可运行。最终312项测试通过，45文件mypy、目标Ruff与前端构建通过。原聊天真实DeepSeek已输出两处景点的具体介绍和门票说明，地图未配置状态实际保存并刷新恢复。详细验收及模型表达限制见08第10.9节。

## 历史成果：M3第四小步——Milvus索引、续建与前端语义搜索（2026-09-14）

### A. 这一小步能做什么

现在可以把已经生成的片段保存为搜索索引，再用一句话找相关原文。Milvus存“数字向量和片段编号”，PostgreSQL继续存“正文、文件名、页码和标题”。这相当于给资料柜加了一份按含义查找的目录。

选择资料后点击“建立搜索索引”。每次处理8段，页面连续发下一批并更新进度。可以暂停；刷新、切换资料、切回聊天或关闭页面后不再发送后续批次。已经发出的当前批次可能继续完成，重新打开时读取真实进度，点击“继续建立索引”会跳过已存部分。搜索覆盖所有已经写入的部分，未完成的资料也能搜到已完成的片段。

### B. 开发顺序与逐文件作用

| 顺序 | 文件 | 所属层与作用 |
|---|---|---|
| 1 | `deploy/milvus/compose.yaml`、`embedEtcd.yaml` | 部署层：启动Milvus 2.6.17单机容器，向量和元数据保存在Docker命名卷；只开放本机端口 |
| 2 | `backend/tests/test_vector_store.py` | 测试层：先验证缺失功能，再检查REST请求、向量空间隔离和错误脱敏 |
| 3 | `backend/app/config.py`、`services/document/vector_store.py` | 配置与存储层：独立Milvus地址、建集合、固定编号upsert、进度查询和搜索；复用httpx，没有增加SDK依赖 |
| 4 | `backend/tests/test_document_search.py` | 测试层：真实PostgreSQL与Milvus验收，只模拟收费模型；覆盖续建、错误、响应丢失、锁冲突、归属和重复正文 |
| 5 | `backend/app/schemas/document/search.py`、`services/document/search.py` | 数据格式与业务层：规定进度/命中格式，协调片段读取、模型、向量保存和原文回查 |
| 6 | `backend/app/api/document/search.py`、`main.py` | HTTP与入口层：注册进度、分批索引、搜索接口及错误处理；就绪检查增加已配置Milvus |
| 7 | `frontend/src/api/documents.ts`、`components/DocumentSearch.vue` | 前端请求与组件层：调用新接口，显示进度、暂停续建和原文搜索结果 |
| 8 | `frontend/src/components/DocumentPanel.vue`、`App.vue` | 页面组合层：嵌入检索组件并传入活动页签状态；v-show隐藏时也停止后续批次 |
| 9 | `backend/.env.example`、`tests/test_import_safety.py`、`tests/test_trips_api.py` | 补配置示例、导入无副作用检查和新增健康状态的契约断言 |

### C. 一次索引和一次搜索分别怎么走

索引：`页面 → POST /documents/{id}/index → 锁定当前资料 → 查已有向量编号 → 取最多8个缺失片段 → 阿里云Embedding → Milvus upsert → 强一致读回数量 → 页面更新进度`。重复点击时行锁立即返回409，避免排队形成重复模型请求。数据库与Milvus没有联合事务；如果写入成功但确认丢失，下次查真实编号即可恢复，不把本地临时状态当成事实。

搜索：`页面问题 → POST /document-search → 阿里云Embedding（一次）→ Milvus按归属和相似度找编号 → PostgreSQL再次检查归属并读取原文 → 去重后展示文件名、页码、标题和片段`。若重复段落或失效编号挤满候选，扩大候选直到够用、没有更多结果或达到200个上限；不会再次生成问题向量。相似度不是答案正确率。

### D. 启动与操作

项目根目录运行：

```powershell
docker compose -f deploy/milvus/compose.yaml up -d
```

`backend/.env`设置`TRAVELMIND_MILVUS_URL=http://127.0.0.1:19530`后，按下方原有命令重启后端。当前本机已启动且完成配置。只读健康接口`/api/v1/health/ready`同时检查PostgreSQL和Milvus，不调用收费模型。

打开`http://127.0.0.1:5173/`的“旅行资料”，在列表里选择资料，上方查看索引进度。片段数为0时先到“查看片段”生成，再刷新进度。建立完成后输入“杭州有哪些适合散步的景点？”等问题，点击“搜索已建索引的资料”。命中文件名可以打开对应资料，结果卡片本身显示命中的原文。

### E. 验证与范围

全量283项测试通过、0跳过，含真实PostgreSQL和真实Milvus；1条既有依赖弃用提示。变更目标Ruff、41个源码文件mypy和前端构建通过。审阅发现的页签隐藏继续索引、重复正文挤占候选两项问题均已修复，后者有先失败再通过的真实库回归。

没有新增PostgreSQL表或迁移，仍是6张业务表、0004迁移。Milvus集合的字段字典、模型隔离规则、真实语料数量和重启验收见08。本步只提供原文检索，没有让DeepSeek阅读命中内容或生成问答；来源主次权重、城市/POI元数据、删除/重新切分、后台队列和Recall@5评测仍待后续实现。上传仍不会自动调用Embedding。

## 历史成果：M3第三小步——Embedding独立配置与试跑入口（2026-09-14）

### A. 这一小步能做什么

Embedding是“把一段文字转换成一组数字”，以后才能按含义找相近资料。本步做好独立配置、兼容接口客户端和小批量试跑命令。用户已重新填写阿里云配置，`text-embedding-v4`真实调用通过，返回1024维向量。既能只读预览输入，也已验证从上传资料读取少量片段后调用服务。

### B. 开发顺序和每个文件的作用

| 顺序 | 文件 | 所属层与作用 |
|---|---|---|
| 1 | `backend/tests/test_embeddings.py` | 测试层：先验证配置隔离、文字和向量对应关系、错误处理；最初因功能缺失失败 |
| 2 | `backend/app/config.py` | 配置层：增加向量服务的提供方、地址、模型、密钥、维度、版本标签、超时，默认未配置 |
| 3 | `backend/app/llm/embeddings.py` | 模型连接层：发送文字，校验返回的数字，再按输入顺序交回；不保存结果 |
| 4 | `backend/tests/test_embedding_cli.py` | 测试层：先检查命令缺失，再验证无密钥提示、真实临时数据库只读取样、模拟HTTP调用 |
| 5 | `backend/scripts/demo_embedding.py` | 命令入口层：选择文字或资料片段，执行只读预览或一次真实请求，并关闭连接 |
| 6 | `backend/.env.example` | 部署配置示例：提供阿里云填写说明；没有把示例写入个人`.env` |
| 7 | `backend/tests/test_import_safety.py` | 测试层：确认导入新模块不会自动联网、读取配置或打印 |

调用顺序：`demo_embedding.main → load_settings → DocumentService.list_chunks（资料模式）→ EmbeddingClient.embed → 配置的/embeddings接口 → 校验模型、序号、维度与数字 → 输出摘要`。`--dry-run`在读取片段后直接输出，不创建HTTP客户端。

### C. 现在可以运行的命令

在`E:\TravelMindAI\backend`执行。下面使用本机已上传的杭州景点资料编号；其他资料需要替换成自己的编号。

```powershell
# 只读预览：跳过前6段，取3段，不需要向量密钥、不发送资料。
./.venv/Scripts/python.exe -X utf8 -m scripts.demo_embedding --env-file .env --document-id 12f37158-0101-4045-a557-2c8c9bb95a23 --offset 6 --limit 3 --dry-run

# 配好独立向量配置后试跑一句话；此命令会调用配置的外部服务。
./.venv/Scripts/python.exe -X utf8 -m scripts.demo_embedding --env-file .env --text "杭州西湖适合步行游览"
```

本机已按`.env.example`把`TRAVELMIND_EMBEDDING_*`填写到`backend/.env`，并补齐版本标签`aliyun-text-embedding-v4-1024-v1`。换环境时使用控制台对应地域和业务空间的兼容API根地址，末尾不要重复添加`/embeddings`。密钥不发到聊天里。去掉资料命令的`--dry-run`会发送选中的少量片段；本机已验证该调用成功。

### D. 验证与边界

本机已读取三份杭州主资料：景点760段、餐馆922段、住宿762段，共2444段。上述只读命令成功读到3个固定编号，正文长度29、285、30字符。早先未配置密钥时正确拒绝；用户补齐配置后，真实调用2次成功：一句10字符短文本返回1个向量，景点资料3段返回3个向量，均为1024维且通过客户端完整校验。结果仅在内存中使用，没有保存向量。

最终测试结果见08第10.6节。本步没有新增依赖、表、迁移、Web接口或前端按钮。向量仍未写入Milvus；没有批量索引、语义搜索、问答或来源优先级检索。最多8段、每段800字符是本地试跑限额，不是token计数；正式索引时需增加选定模型的token预算。版本标签由项目填写，不能锁定服务商内部模型权重。

## 历史成果：M3第二小步——正文切分、片段保存和前端预览（2026-09-14）

### A. 这一小步能做什么

在“旅行资料”选择已读取的资料，点击“查看片段”，首次点击“生成片段”。系统从数据库里的正文生成短片段并保存，之后重新打开或刷新都读取同一批片段，不需要重新上传。长资料每页显示50段，可以翻页；“查看原文”可以切回读取结果。

每段最多800个Unicode字符。同一原文单元内的相邻长片段重叠100个字符，优先在后半段的换行或句末切开；没有合适断点时按长度切。不同原文单元分别处理，不合并标题、段落、表格或PDF页。页码、标题、原文单元编号和字符范围保留下来，空格及换行也不删除。

片段是后续检索的准备材料，目前没有调用Embedding、Milvus或模型回答。800是字符上限，不是token上限；标题随片段重复后的正文总量限制为200万字符，超限会提示缩短标题或拆分文件，并保留原资料。

### B. 开发顺序和逐文件作用

| 组 | 文件 | 先做什么、文件负责什么 |
|---|---|---|
| 1 | `backend/tests/test_document_chunks.py`、`backend/app/schemas/document/base.py`、`backend/app/services/document/chunker.py` | 先写不丢字和位置检查，再规定片段格式，最后实现不依赖数据库的切分函数 |
| 2 | `backend/app/models/document.py`、`backend/migrations/versions/0004_document_chunks.py`、`backend/app/models/__init__.py` | 在模型中定义片段表，写升级脚本，让迁移工具认识新表 |
| 3 | `backend/app/services/document/service.py`、`backend/app/api/document/routes.py`、`backend/app/main.py` | 业务服务负责归属和事务，接口负责接请求，启动后的就绪检查增加片段表 |
| 4 | `backend/tests/test_schema.py` | 原迁移测试增加片段表，验证升级、回滚和ORM结构一致性 |
| 5 | `frontend/src/api/documents.ts`、`frontend/src/components/DocumentChunks.vue`、`frontend/src/components/DocumentPanel.vue` | 定义网页请求，独立展示片段，再接入原有资料页 |
| 6 | `document_chunker.py`、`document_service.py`、`test_document_chunks.py` | 复核补上重复标题总量限制；先复现，再验证超限不保存任何片段 |
| 7 | 本文件、08架构说明、总图及03/04阶段文档 | 同步开发顺序、数据库字典、调用链和实际验收结果 |

注释继续按根目录`AGENTS.md`约定：文件说哪一层，类说存什么，函数上方说做什么，关键字段和步骤说明原因。

### C. 点击“生成片段”后发生什么

`DocumentChunks.vue → api/documents.ts → POST /api/v1/documents/{id}/chunks → DocumentService.generate_chunks() → split_sections() → document_chunks表 → 返回首页 → 页面展示`。

业务服务先锁住当前资料并检查归属和读取状态，再检查是否已经有片段。没有才生成，在同一个事务里一次保存；失败会全部撤销。同时点击不会生成两批编号。刷新与翻页走GET接口，只查询已存片段。GET返回总数0时表示尚未生成；读取失败的资料返回409，不能生成空的“成功结果”。

数据库关系是“一份资料对应多个片段”：`documents.id → document_chunks.document_id`。资料原文和已有M1/M2数据不重写。片段字段和约束详见08第7.5节。

### D. 验证与当前边界

本机已升级到`0004_document_chunks (head)`，`alembic check`没有发现差异。全量测试、最终静态检查见08第10.4节；真实PostgreSQL测试使用临时schema，回滚不会作用于本机业务数据。

浏览器已验证：上一小步保存的两页PDF生成片段并保留页码；虚构长Markdown生成55段，首页50段、第二页5段；刷新后直接读取已保存片段；失败PDF只显示读取失败原因；原文/片段及不同资料之间切换正常。虚构样本位于Git忽略的`backend/data/runtime/m3-browser/chunks-long.md`。

后端与前端启动方式沿用下方第一小步说明，迁移命令仍为`upgrade head`。当前分支`codex/m3`，本步未提交或推送。尚无片段重新切分、删除接口或向量状态；后续接入Embedding时再确定供应方、token预算和索引生命周期。

### E. 后续语料选择与本地准备（2026-09-14）

按用户选择，以**ChinaTravel-Sandbox为主要参考，LvBanGPT为补充**。2026-09-15目录整理后，源文件分别位于`backend/data/runtime/chinatravel-sandbox`与`backend/data/runtime/lvbangpt`，被Git忽略。早期维基导游、验收样本和自写README已移出项目；详情见本文件顶部。

本次先下载ChinaTravel中文原包，核对官方发布清单与SHA-256，再把10城的景点/餐馆/住宿CSV逐条转换成30份Markdown，保留全部字段、原始记录键、来源和版本。共11934条记录；原包其余交通和POI文件仍保留原格式。所有导出文件都已通过现有读取/切分函数，每条记录键都能找回。本次没有修改应用源码、建表或自动导入数据库；这属于语料准备，不是Embedding或检索已完成。

来源等级、冲突处理和实际下载版本见08第10.5节；补充PDF下载及读取情况以资料目录中的说明与清单为准。

## 历史成果：M3第一小步——资料上传、读取和前端接入（2026-09-14）

### A. 这一小步能做什么

打开网页顶部的“旅行资料”，选择PDF、DOCX、TXT或Markdown文件，点击“上传并读取”。左侧出现已保存资料，右侧展示正文与页码/标题。刷新后重新进入资料页，仍能点开已保存内容。同一份文件换名字上传也不会再存一份。

原文件存在本机目录，读取结果存在PostgreSQL。文件没有文字或损坏时，保存失败档案并显示原因，不会写成“已读取”。单文件最多20MiB，纯扫描PDF暂不支持OCR。资料尚未用于聊天回答或行程生成；这一小步并不等于M3整阶段完成。

当前开发分支为`codex/m3`，从原`m0-engineering`当前代码建立，原分支保留。本次未提交或推送代码。

### B. 按什么顺序看代码

先理解“规定结果长什么样 → 把文件读出来 → 存起来 → 给网页用”。不必先背框架名称。

| 组 | 开发文件（相对项目根目录） | 用直白的话解释 |
|---|---|---|
| 1 | `backend/pyproject.toml`、`backend/tests/test_document_parser.py` | 准备PDF/Word读取工具，先写固定文件的检查题；`uv.lock`由工具同步锁定依赖 |
| 2 | `backend/app/schemas/document/base.py`、`backend/app/services/document/parser.py` | schema规定统一结果；parser真正读文件，输出正文、页码、标题和顺序 |
| 3 | `backend/tests/test_documents.py`、`backend/app/models/document.py`、`backend/migrations/versions/0003_documents.py` | 先写保存验收，再画“资料柜”结构，最后写真正建表的迁移 |
| 4 | `backend/app/services/document/service.py`、`backend/app/api/document/routes.py` | 服务管校验/查重/读写；接口接收网页请求，把工作交给服务 |
| 5 | `backend/app/config.py`、`backend/app/models/__init__.py`、`backend/app/main.py` | 指定保存目录，让迁移工具认识资料表，将资料接口接到应用 |
| 6 | `backend/tests/test_schema.py`、`backend/.env.example`、`frontend/src/api/documents.ts` | 原有迁移核对增加新表；给出目录配置示例；前端建立上传、列表和详情请求 |
| 7 | `frontend/src/components/DocumentPanel.vue`、`frontend/src/App.vue` | 资料组件管理文件和预览；页面入口增加对话/资料页签，切换保留已有对话 |
| 8 | `backend/app/api/document/upload_limits.py`、`backend/app/main.py`、`backend/tests/test_documents.py` | 复核时补接收层限额，避免框架先完整接收超大文件才拒绝 |
| 9 | `.gitignore`、本文件、`docs/08-架构设计说明.md`及其总图 | 忽略实际原文目录，记录开发顺序、模块关系和资料表字典 |

Python注释沿用简洁、通俗的写法：文件开头说明“哪一层、负责什么”，类说明“什么类、保存或处理什么”，函数在`def`或装饰器上方说明“什么函数、负责什么”。字段和关键步骤用简短中文解释含义或原因，完整约定见根目录`AGENTS.md`，后续开发继续沿用。TypeScript/Vue的接口、状态和交互也有中文说明。代码复核补充了长标题重复路径预算、嵌套表格、合并单元格只读一次等回归，避免“看似读成功，实际丢内容或产生巨型结果”。

### C. 真正运行时，先叫谁、后叫谁

**启动时：**Uvicorn调用`main.create_app()` → 加载配置、注册接口和接收限额 → 启动数据库连接池。启动不会自动建表，建表要显式运行Alembic。

**上传时：**

1. `DocumentPanel.vue`拿到你选的文件。
2. `frontend/src/api/documents.ts`用FormData发送`POST /api/v1/documents`。
3. `upload_limits.py`限制实际接收总量，`api/document/routes.py`取得文件。
4. `DocumentService.upload()`检查类型、大小和内容指纹；有同样内容就直接返回原资料。
5. `parse_document()`将文件读为统一段落格式，PDF有页码，Word/Markdown有标题路径。
6. 服务保存UUID命名原文件，再把读取结果提交到`documents`表。
7. 网页拿到响应，按`parsed/failed`显示正文或失败原因，并重新读取列表。

**刷新或点开资料时：**网页调用列表/详情接口 → 资料服务查询数据库 → 返回已保存正文。这条路径不重新解析文件，也不调用DeepSeek。重新开始旅行对话不会删除资料库。

可以对应到熟悉的Java分层：schema像DTO，model像Entity，service像Service/Repository，api像Controller；Vue组件是用户操作窗口。

### D. 本机启动与手动操作

本次已启动项目原有Docker PostgreSQL容器，已执行本地`0003_documents`迁移，并启动前后端。日后重新启动可在两个终端运行：

```powershell
# 后端：先确保Docker Desktop已启动。
docker start travelmind-postgres
Set-Location E:\TravelMindAI\backend
python -m uv sync --locked --link-mode copy
./.venv/Scripts/python.exe -m alembic -x env_file=.env upgrade head
python -m uv run --no-sync --env-file .env uvicorn app.main:create_app --factory --host 127.0.0.1 --port 8000
```

```powershell
# 前端：另开终端，端口被已有服务使用时不要重复启动。
Set-Location E:\TravelMindAI\frontend
npm run dev
```

打开[本地页面](http://127.0.0.1:5173/)，点顶部“旅行资料”。上传一份带标题/表格的DOCX和两页带文字的PDF，检查预览顺序与页码；再上传同一内容，确认提示重复；刷新网页重进资料页，确认能读取旧资料。最后用空白/扫描PDF检查失败原因。

上传目录默认`E:\TravelMindAI\backend\data\uploads`；需要调整可在后端配置中设置`TRAVELMIND_DOCUMENT_UPLOAD_DIR`绝对路径。文件名只用于显示，磁盘使用UUID名称。生成的浏览器验收样本在`backend/data/runtime/m3-browser`，与原文件目录一起被Git忽略；样本是虚构内容。

### E. 验证和后续边界

- 最终全量自动测试**240项通过、0跳过**（原216项+新增24项），包括真实PostgreSQL临时schema；1条既有Starlette/AnyIO弃用提示。变更Python目标Ruff、后端mypy、前端TypeScript检查和生产构建通过。
- 浏览器已实际验证Markdown上传/去重/刷新读取、PDF两页页码、DOCX标题与表格、无文字PDF失败原因、正文HTML纯文本显示，以及页签切换保留对话输入。
- 迁移只给本机业务库新增资料表；往返迁移、并发、归属与失败场景在隔离的测试schema执行。没有上传私人资料，没有执行付费模型评测。
- 当前同步读取限制为20MiB文件、500页PDF、50MiB展开内容、100万字符（含标题路径）、5000阅读单元。具体格式限制与资料表字典见08第7.5和10.3节。
- 后续小步再做切分、Embedding、Milvus和引用；当前没有OCR、账号认证、后台任务恢复、删除或重新解析接口。当前固定`local-demo`只是本地单用户归属，不是登录权限系统。

## 历史成果：M2第六小步——首次意图校正与阶段验收（2026-09-11）

### A. 这一步修了什么

上一轮评测中，用户第一次说“杭州三天两人预算5000元，节奏轻松一点”，模型字段都提取正确，却把本轮标成modify_trip（修改）。旧代码会将需求表中的intent改为plan_trip（规划），但message_intent仍保留模型的修改标签，两处不一致。

现在由程序按已有上下文处理：**没有previous时，modify_trip校正为plan_trip；有previous时，真实修改保持modify_trip。** 是否有档案看有效需求，不看已经发送了几条消息。用户先闲聊再说旅行需求，仍是在建立档案。

无档案的删除/清空检查先执行，不会把“取消不爬山的限制”冒充建档成功；other等非规划意图不转换。缺预算仍留空并集中追问。没有修改题库、提示词或业务支持范围，也没有新增HTTP字段和数据库表。

### B. 逐文件开发顺序

| 顺序 | 文件（相对项目根目录） | 它负责什么 |
|---|---|---|
| 1 | `backend/tests/test_requirement.py` | 故意让假模型首次返回修改标签；分别检查完整需求与缺预算需求。两个用例先失败再通过 |
| 2 | `backend/app/services/requirement/extract.py` | 在删除/清空检查之后、合并之前校正首次修改标签；后续生成回复与保存共用这个结果 |
| 3 | `backend/app/schemas/requirement/base.py` | 说明message_intent是经过程序校正的业务意图，不是模型原始标签 |
| 4 | `backend/tests/test_requirement_history_api.py` | 扩展已有真实数据库测试：首次误标校正后保存，刷新重读保持一致，后续修改仍标modify_trip |

实际调用顺序：**前端 → 读取历史需求 → 模型提取 → 校验删除/清空 → 校正首次意图 → 合并/日期计算/补问 → 保存整轮 → 前端显示。** 更新放在服务层，网页与命令行评测都会得到相同规则。

### C. 验收结果与成绩口径

- 全量216项测试通过、0跳过，含真实PostgreSQL临时schema回归；1条既有依赖弃用提示。
- 变更Python文件Ruff通过，mypy检查38个文件通过。
- 同题库真实DeepSeek评测30/30组、36/36轮通过；36次调用，0轮修复。
- 四项必要字段132/132、开始/结束日期12/12、最终业务意图36/36。新报告保留，之前两份失败报告也保留。

**评分对象是整个提取服务的最终结果，包含程序校正，不是模型原始意图准确率。** 人工开发集全部通过不代表真实用户的任意表达都正确；本次没有保存原始模型标签，因此不能计算原始标签误判率。题库和提示词保持不变，规则没有读取标注答案。

报告：本次成绩单（历史报告`m2-intent-fix-20260911.md`，本地阶段产物已清理） · 逐字段JSON（历史报告`m2-intent-fix-20260911.json`，本地阶段产物已清理）。复跑时使用新的输出文件名，避免覆盖历史：

```powershell
Set-Location E:\TravelMindAI\backend
./.venv/Scripts/python.exe -X utf8 -m scripts.evaluate_requirements --live --env-file .env --output ../temp/reports/m2-intent-fix-20260911.json
```

M2按当前完成标准验收：结构化需求、一次修复、集中追问、多轮合并、保存恢复、模型配置状态/超时和固定开发集目标均已具备。下一阶段按既定路线进入M3资料上传与检索。交通时间建模、每日行程、账号归属及跨设备找回尚未实现，不计为M2成果。

前端真实复核：重新开始后发送M2-20原话，得到杭州、3天、2人、5000元、轻松；刷新后发送“改成三个人”，人数变为3，其他条件保留，显示已保存2轮。后端已重启到本次代码，前端源码未改动，本步未重复执行Vue构建。

## 历史记录：M2第五小步——首轮日期补算与超范围提示（2026-09-11）

### A. 三天到底算到哪一天

**10月1日开始，旅行三天，日期范围是10月1日～3日。** 开始当天算第1天，公式为“结束日＝开始日＋天数－1”。日期运算由Python处理，因此跨月、跨年、闰日也按同一规则计算。

如果10月3日早上9点赶飞机或高铁，仍然跨了三个自然日，但第3天通常只安排退房、前往车站/机场及候乘，不再安排景点。前两天也不一定是两个完整游玩日，要看第1天几点抵达。**旅行自然日数不等于可游玩的完整天数。** 当前M2整理需求，尚未实现交通时间字段及按到离时间编排行程；不能把日期修复写成已经能够安排早班机接驳。

### B. 逐文件开发顺序

| 顺序 | 文件（相对项目根目录） | 用直白的话解释 |
|---|---|---|
| 1 | `backend/tests/test_requirement.py` | 先写四道日期题：普通三天、跨月、跨年、闰日；旧代码四题都因结束日为空而失败 |
| 2 | `backend/app/services/requirement/merge.py` | 首次对话已有开始日和天数时，也调用原有日期协调函数；沿用首日计入和明确清空优先的规则 |
| 3 | `backend/app/services/requirement/prompt.py` | 告诉模型三天怎么算；超范围时用other，days/end_date留空，原始跨度和原因记入assumptions，不把六天改成五天 |
| 4 | `backend/tests/test_requirement_api.py` | 检查空会话和已有需求收到超范围回复时，都说明支持范围；旧日期不会被覆盖 |
| 5 | `backend/app/api/requirement/routes.py` | 给非规划请求返回2～5天、1～8人、单目的地及人民币预算的引导；已有需求明确说明保留 |

调用顺序仍然是：**前端发送 → 服务端读历史 → DeepSeek提取 → 合并与日期协调 → 补问/回复 → 保存 → 前端显示**。没有新增接口、表或前端字段；页面直接展示后端回复和补算后的结束日。上述测试只替换远程模型，实际运行JSON校验、合并和HTTP逻辑。

### C. 验证结果与剩余差距

全量**214项测试通过、0跳过**，含真实PostgreSQL临时schema测试；有1条既有依赖弃用提示。变更Python文件Ruff通过，mypy检查38个文件通过。本步前端源码未修改。

保持原题库及旧报告不变，重新调用真实DeepSeek：**29/30组、35/36轮通过**；36轮全部有效返回，36次模型调用、0轮修复。四项必要字段132/132，日期字段12/12；原失败题M2-16和M2-22均通过。

本次另有M2-20将首次完整需求判成modify_trip，而标注为plan_trip；目的地、天数、人数、预算、节奏均正确，仍按题库记为失败，不改答案、不重跑到全绿。M2继续进行中，这份人工开发集不代表独立用户准确率。

报告：本次成绩单（历史报告`m2-date-fix-20260911.md`，本地阶段产物已清理） · 逐字段JSON（历史报告`m2-date-fix-20260911.json`，本地阶段产物已清理）。复跑命令如下；报告已有时需换新文件名，禁止覆盖历史。

```powershell
Set-Location E:\TravelMindAI\backend
./.venv/Scripts/python.exe -X utf8 -m scripts.evaluate_requirements --live --env-file .env --output ../temp/reports/m2-date-fix-20260911.json
```

### D. 前端检查方法

1. 点击“重新开始”，发送“想去杭州三天，从10.1开始，两个人，总预算5000元”：卡片应显示3天、2026-10-01～2026-10-03。
2. 再说“改成10月1日到10月6日”：应说明支持2～5天，并保留原三天需求。
3. 刷新页面：聊天及原需求应能从已保存会话恢复。

以上三步已在本地前端真实执行：首轮显示2026-10-01～2026-10-03，六天修改返回范围提示并保留卡片；刷新后两轮聊天、三天日期及预算均恢复。后端已重启到本次代码，页面仍可继续测试。

## 历史记录：M2第四小步——固定样例评测（2026-09-11）

这一步给模型准备一份固定考卷。单元测试检查“程序有没有按规则做事”，真实评测检查“模型面对这些话能否给出正确字段”，两种成绩分别记录。

### A. 本次结果和仍存在的问题

2026-09-11使用真实`deepseek-v4-pro`运行人工编写的**30组、36轮**开发评测集：**28/30组通过，34/36轮通过**。35轮返回有效结果，1轮最终校验失败；38次模型请求，2轮触发一次修复。完整报告见评测报告（历史报告`m2-baseline-20260911.md`，本地阶段产物已清理），逐字段证据见JSON结果（历史报告`m2-baseline-20260911.json`，本地阶段产物已清理）。

| 指标 | 本次结果 | 如何理解 |
|---|---|---|
| 目的地、天数、人数、预算四项 | 132/132 | 只计算规划/修改轮次，包含明确应为空的字段 |
| 上述四项中的已提供值 | 115/115 | 单列，避免空字段多而抬高成绩 |
| 上述四项中的应未知值 | 17/17 | 没说的信息应保持未知 |
| 单独标注的开始/结束日期 | 11/12 | 不包含在上面以days计分的四项中，也参与整题判定 |
| 当前消息意图 | 35/36 | 包含超范围和非旅行消息 |

四项字段指标达到90%目标，但**不代表所有日期处理正确或M2全部完成**。这是我们手工编写的开发集，并非独立盲测或真实用户随机样本。当前保留两个明确差距：

1. **M2-16**：“明天去杭州玩三天”得到出发日和3天，但结束日为空。`merge_requirements`仅在有previous时调用`reconcile_dates`，首次对话没有执行同样的日期推导。
2. **M2-22**：“2026年10月1日到6日”超出5天，应正常返回不支持意图，实际两次输出都未通过schema校验，返回通用错误。与前端此前出现的6天失败一致。

本小步保留原始基线结果，没有修改题库答案迎合输出，也没有在同一轮评测中修改提示词。下一小步优先修复上述日期/超范围处理，再对同一份题库生成新报告比较。

### A.1 同步改进：没有旅行档案时的提示

用户从空会话直接说“取消不爬山的限制”时，现在提示：

> 我们还没有建立旅行档案，暂时没有可以修改或取消的内容。可以先从目的地开始，告诉我你想去哪里，比如“我想去杭州”。之后我们再一起补充时间、人数和预算。

这次按“先测试→后服务”的顺序修改`tests/test_requirement_api.py`与`app/services/requirement/extract.py`。没有previous时收到实际删除/清空指令，直接提醒缺少档案；空删除列表不触发拦截。不再请求模型修复一次，也不假装已取消成功。普通输出连续校验失败也按有没有previous给出相应的温和说明；已有需求提示“之前的旅行需求已保留”。

HTTP错误格式和502状态保持兼容，前端原有提示区域直接展示新文字，不新增数据库字段或前端状态。最终全量208项测试通过，真实网页已显示上述提醒。上述30组基线在此文案调整前生成，保留原始报告；没有把更新后的页面验证冒充重新跑过整套真实模型评测。

### B. 逐文件开发顺序

| 顺序 | 文件（相对项目根目录） | 它负责什么 |
|---|---|---|
| 1 | `backend/tests/test_requirement_eval.py` | 先考判卷程序：错误值要扣分，失败不能从分母删掉，多轮不能偷用标准答案 |
| 2 | `backend/scripts/requirement_eval.py` | 读题、校验标注、调用正式抽取服务、比较字段、汇总计数 |
| 3 | `backend/evals/requirements.v1.json` | 30组人工考题，每题note解释标注原因，固定参考日期2026-09-11 |
| 4 | `backend/scripts/evaluate_requirements.py` | 命令入口：默认只查题库，显式运行真实模型，写JSON与Markdown成绩单 |
| 5 | `backend/tests/test_import_safety.py` | 保证导入新脚本时不会读个人配置、调用模型或打印内容 |

这批代码没有新增应用HTTP接口、数据库表或前端状态；现有Vue聊天继续调用同一套抽取和合并服务。Python函数说明保留在def/装饰器上方并空一行。JSON不能写`#`注释，因此题库使用description和每题note说明设计。

### C. 怎么运行

```powershell
Set-Location E:\TravelMindAI\backend
# 默认只校验30组人工标注，不读密钥，不调用模型。
./.venv/Scripts/python.exe -X utf8 -m scripts.evaluate_requirements
# 显式进行真实评测：会调用DeepSeek并产生费用；默认生成新的带时间戳报告。
./.venv/Scripts/python.exe -X utf8 -m scripts.evaluate_requirements --live --env-file .env
```

也可以用`--dataset`指定另一份同格式题库，用`--output ../temp/reports/新文件名.json`指定新报告。已有同名JSON或Markdown时拒绝覆盖。默认报告位于`temp/reports/`（项目根目录），模型密钥只从显式配置读取。

退出码：0表示全部标注检查通过且达到必要字段目标；2表示评测完成但存在错误题或指标差距；1表示题库/配置等启动失败。每完成一组就写入结果，中途停止的JSON保留`completed:false`，不得作为完整成绩。

### D. 判卷规则为什么这样写

**题库 → 评测脚本 → 正式extract_requirements → DeepSeek → 正式合并/校验 → 逐字段比较 → 报告。**

模型只看到原话、实际上一轮需求和正式系统提示，不会看到expected、note或题号。多轮题上一轮答错，错误会自然传到下一轮；不能给模型一份正确历史假装它答对了。

金额5000与5000.00按Decimal相等处理；列表忽略顺序，但不自动把不同措辞认作同义。缺项列表与意图也参加判定。非规划题只计意图及明确标注项，不将它们的大量空字段计入四项必要字段分数。模型/最终校验失败时，本轮所有标注检查都记错。

报告记录题库及提示词SHA-256、模型名、运行时间、依赖版本、每轮耗时和请求次数。没有提供方账单，因此不推算实际费用。此次不测RAG资料冲突、实时预算或行程生成。

### E. 前端怎样继续验收

保持前端5173和后端8000运行，启动步骤沿用下文第三小步。可以从JSON题库复制一组原话，逐轮输入网页。相对日期题的期望值以2026-09-11为参考，而网页新会话以实际当天为参考，换一天运行不能直接照抄固定日期答案。

评测脚本不往旅行会话库写36条记录；网页手工验收才走保存接口。脚本成绩和网页保存/刷新能力分别核对，不相互替代。

## 历史成果：M2第三小步（2026-09-11，现行前端启动方式）

现在对话成功后会保存到PostgreSQL；刷新Vue页面后恢复聊天、需求和参考日期，并继续修改。
沿用现有`sessions`表，新增`requirement_turns`表，每行原子保存一条用户消息和对应回复/需求结果。
浏览器的`sessionStorage`记住会话编号和待确认消息，同一标签页刷新可恢复。完整历史及需求以数据库为准。

### A. 先理解这次改变

之前像在网页上用铅笔记事，刷新就没了。现在每轮成功结果都会装进一个“文件袋”，放到数据库里。网页只拿着会话编号，刷新时凭编号取回文件袋。

一轮记录包括：消息编号、轮次、用户原话、助手回复、结构化需求、参考日期和状态。原话存放在`response.result.original_message`中，不重复设计第二套消息字段。

### B. 本次逐文件开发顺序

先写测试看到失败，再实现对应文件。以下路径从项目根目录开始。

| 批次 | 文件 | 用最直白的话解释职责 |
|---|---|---|
| 1 | `backend/tests/test_requirement_history.py` | 考卷：必须能重读、不串话、不重复保存，并验证真实事务回滚 |
| 1 | `backend/app/models/requirement_turn.py` | 文件袋格式：数据库一行需要哪些栏 |
| 1 | `backend/migrations/versions/0002_requirement_turns.py` | 给已有数据库新增对话表；不修改第一版迁移 |
| 2 | `backend/app/schemas/requirement/history.py` | 前后端约定：原话、消息编号、旧轮次，以及保存结果 |
| 2 | `backend/app/services/requirement/history.py` | 仓库管理员：读取、检查版本、原子保存整轮结果 |
| 2 | `backend/app/models/__init__.py` | 注册新模型，让迁移比较时能发现它 |
| 3 | `backend/tests/test_requirement_history_api.py` | 考卷：从HTTP模拟刷新、续聊、重试和模型失败 |
| 3 | `backend/app/api/requirement/history.py` | 接待窗口：先读旧需求，再复用抽取/回复，最后保存 |
| 3 | `backend/app/main.py` | 接上路由、409冲突提示，并把新表加入数据库就绪检查 |
| 4 | `frontend/src/api/requirements.ts` | 发HTTP请求，前端不再提交previous或参考日期 |
| 4 | `frontend/src/useRequirementConversation.ts` | 保管会话编号，恢复聊天，处理未确认的发送及重新开始 |
| 4 | `frontend/src/App.vue` | 把会话功能接到界面上，显示保存轮数、恢复状态及支持范围 |

配套更新`tests/test_schema.py`验证迁移与旧数据兼容，`tests/test_import_safety.py`验证新增模块导入不联网。
Python函数说明继续放在def/装饰器上方并空一行；Vue和TypeScript保留模块、函数及关键分支中文注释。

### C. 请求怎样走完整条链

**发送：App → 会话状态 → HTTP → 读取数据库历史 → DeepSeek抽取 → Python合并校验 → 保存一整轮 → 返回并更新界面。**

**刷新：读取浏览器会话编号 → GET历史 → 恢复聊天和右侧卡片。** 这条链不调用模型。

| 接口 | 输入与作用 |
|---|---|
| `POST /api/v1/sessions` | 沿用M1创建会话；首次发送时才调用 |
| `GET /api/v1/sessions/{id}/requirement-messages` | 返回完整有序历史及当前revision |
| `POST /api/v1/sessions/{id}/requirement-messages` | 提交message、message_id、expected_revision，返回保存的一轮 |
| `GET /api/v1/requirements/status` | 仍只检查模型配置，不调用模型 |

旧`POST /requirements/messages`继续保留作为无存储接口，但Vue已切换到新接口。

`message_id`就像快递单号：同一次发送重试沿用它；如果已经保存，直接取回原结果。
`expected_revision`表示“我是在第几轮的基础上说这句话”：旧页面遇到新数据返回409，网页恢复最新内容后让用户检查并重新发送，不静默覆盖。

模型调用期间不持有数据库事务；提交时短暂锁住当前会话行，再检查版本。消息与需求存在同一JSON快照里，不会只写一半。
模型失败不保存一轮；HTTP超时不能证明后端没保存，因此保留消息编号，并允许重新读取确认或原消息重试。
`unsupported`回复也会保存，之后续聊使用最近一次有效旅行需求。参考日期固定为第一轮日期，跨天恢复也不会重新解释旧的“明天”。

### D. 当前电脑的启动命令

先确保PostgreSQL运行。根目录`.env`保留模型配置，`backend/.env`保留数据库配置，本次没有修改这些真实文件。

```powershell
Set-Location E:\TravelMindAI\backend
python -m uv sync --locked --link-mode copy
./.venv/Scripts/python.exe -X utf8 -m alembic -x env_file=.env upgrade head
python -m uv run --no-sync --env-file .env uvicorn app.main:create_app --factory --host 127.0.0.1 --port 8000
```

这里的`uv run`明确读取两份配置，后面的同名配置优先。若模型和数据库已放在同一份`backend/.env`，只保留`--env-file .env`即可。先停止旧的8000端口服务再启动。

```powershell
Set-Location E:\TravelMindAI\frontend
npm ci
npm run dev
```

打开[旅行需求对话页](http://127.0.0.1:5173/)。正式库仅升级到`0002_requirement_turns`，不要为日常运行执行downgrade。

### E. 你可以这样检查

1. 输入“从银川去杭州玩三天，两个人，总预算五千元，节奏轻松一点”。页面显示保存1轮。
2. 刷新当前标签页：聊天、银川、杭州、3天、2人和5000元仍然出现。
3. 输入“改成三个人”：人数改为3，其他条件保留，显示保存2轮。
4. 点击“重新读取”：从数据库重读，刷新和读取均不调用模型。
5. 点击“重新开始”，输入“想去苏州”：人数和预算应留空，不带入旧会话。

当前仍支持2～5天、1～8人、单目的地，页面已明示。10月1日至6日共6天，仍超范围；本次没有扩大需求/预算的业务限制。

### F. 验证与本小步边界

真实PostgreSQL自动测试使用随机临时schema并清理，覆盖保存/读取、并发旧版本拒绝、同编号重试、模型失败不落库、INSERT后失败回滚、旧库升级保留会话。
真实前端验证覆盖刷新续聊和后端重启恢复。最终检查计数见[项目进度](04-项目进度文档.md)。

本次不包含登录、跨设备恢复、历史会话列表、行程生成或30条准确率评测。关闭标签页或清除浏览器会话数据后，虽然数据库记录仍在，当前页面没有历史列表帮你找回旧编号。“重新开始”创建新会话，不删除旧数据库记录。旧版页面内存里已经丢失的聊天无法追溯恢复。

## 历史记录：M2第二小步（2026-09-10，启动与存储方式以上文为准）

现在可以在Vue网页里连续聊天，观察旅行需求如何补齐和修改。前端采用 **Vue 3 + TypeScript + Vite + Arco Design Vue**，提前进入M2验收，不再等到M6才创建前端。M6继续负责完整行程工作台等后续页面。

左边是对话，右边是结构化需求。模型从最新一句话提取“本轮修改”，Python程序负责合并，不让模型重写整份旧表格。客户端当前沿用已同步接入的LangChain `ChatOpenAI`，调用的提供方仍然是DeepSeek。

### A. 先跑起来

在两个PowerShell终端分别启动后端和前端，保持终端运行。

**终端一：后端。** 正式配置已统一到`backend/.env`，在backend目录显式传入`.env`。

```powershell
Set-Location E:\TravelMindAI\backend
python -m uv sync --locked --link-mode copy
./.venv/Scripts/python.exe -X utf8 -m uvicorn app.main:create_app --factory --env-file .env --host 127.0.0.1 --port 8000
```

**终端二：前端。** 需要Node.js 20.19及以上的兼容版本，本机20.19.5已验证。

```powershell
Set-Location E:\TravelMindAI\frontend
npm ci
npm run dev
```

浏览器打开 [旅行需求对话页](http://127.0.0.1:5173/)。开发服务器将`/api`转发到本机8000端口；密钥只在后端，不写入Vue代码。
页面“已配置”只表示后端找到了密钥，不等于已验证提供方在线。每次发送会产生真实模型调用，正常一次，输出修复时最多两次。

### B. 按这个顺序试一遍

1. 输入“想去杭州”：只记住杭州，一次追问时间、人数、预算。
2. 输入“三天，两个人，总预算五千元，不爬山，住宿必须有电梯”：补齐需求。
3. 输入“改成三个人”：人数变成3，杭州、3天和5000元仍保留。
4. 输入“取消不爬山的限制”：删除指定条目，保留有电梯的住宿要求。
5. 输入“预算没定，清掉之前的预算”：预算为空，重新追问预算。
6. 点击“重新开始”：清空当前页面，另一趟旅行不会带入旧人数预算。

浅绿色标出本轮变化，卡片底部可以展开完整JSON。Enter发送，Shift+Enter换行；中文输入法选词时不触发发送。失败时不覆盖旧需求，输入框保留原话，可自行重试。

### C. 本次开发顺序：后端

以下路径相对`backend/`。每批开发两三个主要文件后讲解并验证，先写测试，再完成对应实现。

| 顺序 | 文件 | 它负责什么 |
|---|---|---|
| 1 | `tests/test_requirement_merge.py` | 先写“考卷”：改人数、保留预算、删除限制、日期联动如何才算正确 |
| 2 | `app/schemas/requirement/update.py` | 定义“修改单”：新增什么、清空哪栏、删掉哪条旧限制 |
| 3 | `app/services/requirement/merge.py` | 按确定规则把修改应用到旧需求，不修改传入对象 |
| 4 | `app/services/requirement/prompt.py` | 告诉模型只提取最新消息；删除时复制历史条目的准确原文 |
| 5 | `app/services/requirement/extract.py` | 抽取、合并、校验、一次修复、重新计算缺项 |
| 6 | `app/schemas/requirement/base.py` | 保留旅行表格，同时记录本条消息是规划、修改还是其他意图 |
| 7 | `tests/test_requirement.py` | 验证多轮服务确实保留上下文，合并失败也只修复一次 |
| 8 | `tests/test_requirement_api.py` | 验证真实HTTP契约、输入错误、无密钥、上下文隔离 |
| 9 | `app/schemas/requirement/chat.py` | 定义浏览器发什么、后端回什么，禁止前端提交密钥 |
| 10 | `app/api/requirement/routes.py` | 对话入口：接收原话和previous，调用服务，生成回复与变化字段 |
| 11 | `app/main.py` | 注册新路由和模型异常处理，复用原有统一错误格式 |

导入安全测试也增加了新模块，确认`import`不会读取个人配置或联网。

### D. 本次开发顺序：前端

以下路径相对`frontend/`。使用已有组件库，没有提前增加多页面路由或全局状态库。

| 顺序 | 文件 | 它负责什么 |
|---|---|---|
| 1 | `package.json`、`package-lock.json` | 声明并锁定Vue、Arco等版本；记录dev/build命令 |
| 2 | `vite.config.ts` | 启动5173端口，将/api请求代理给8000端口的FastAPI |
| 3 | `tsconfig.json` | 检查自己的TypeScript类型，防止字段使用错误 |
| 4 | `index.html` | 浏览器最先加载的HTML入口 |
| 5 | `src/main.ts` | 加载样式、创建Vue、挂载App |
| 6 | `src/api/requirements.ts` | 定义前后端字段格式，发送消息，处理HTTP错误与等待超时 |
| 7 | `src/App.vue` | 管理本页面的聊天记录、输入、发送、重试提示、重新开始 |
| 8 | `src/components/RequirementPanel.vue` | 展示需求、缺项、变化高亮及推导依据 |
| 9 | `src/style.css` | 两栏布局、聊天滚动、输入区和窄屏样式 |

手写代码保留中文模块说明、函数说明和关键分支注释。Python函数说明继续放在def或装饰器上方，空一行。标准`package.json`与自动生成的锁文件不支持/不适合手工注释，其职责在本表说明。

### E. 一次点击怎样走完整条链

**App.vue → api/requirements.ts → Vite代理 → FastAPI对话路由 → 抽取服务 → DeepSeek → Python合并/校验 → JSON响应 → 聊天区与需求卡片。**

前端第一次发送`previous:null`。收到成功结果后保存在当前页面；第二次发送时把这份需求放进`previous`。后端不使用一份全局变量记住所有人的旅行，因此不同页面不会串话。

| 接口 | 用途 |
|---|---|
| `GET /api/v1/requirements/status` | 只检查模型配置，不发送模型请求 |
| `POST /api/v1/requirements/messages` | 原话、previous、参考日期 → 回复、结果、状态和变化字段 |

`status=complete`表示必需旅行条件已齐，**不表示行程已生成**；`needs_clarification`表示还要补充；`unsupported`表示本页未执行该意图，旧需求保持原样。HTTP请求错误返回422，模型不可用返回503，输出连续校验失败返回502。

### F. 合并规则与边界

- 未提及的单值为null、列表为[]：保留旧值。
- 明确给出的单值覆盖；列表新增按顺序合并去重。
- `clear_fields`才代表清空；`remove_items`只删除指定的旧条目。
- 删除名称必须与历史准确匹配，匹配失败让模型修复，不能假装删成功。
- 新首尾日期决定天数；只改出发日则按已有天数推导结束日；只改天数时保留出发日并更新结束日。
- 更新后再次校验2–5天、1–8人、正数预算和日期一致性。
- 每条结果的assumptions只展示本轮推导，避免旧的人均预算等推导在新人数下误导用户。
- 页面状态只存内存，刷新即清空。尚未实现聊天消息入库、刷新恢复、归属鉴权、行程生成或SSE。
- 整个M2仍需消息持久化以及30条标注数据集和准确率验收。当前页面是M2的实际验收入口，不代表M6全部完成。

### G. 本次验证

2026-09-10：当前工作区全量后端测试152通过、34项数据库测试跳过，1条既有依赖弃用提示。本步新增28项多轮/HTTP测试；工作区同期模型客户端调整增加了其他测试，所以总数以实际全量输出为准。

前端`npm run build`已通过TypeScript检查和Vite生产构建。浏览器实际完成四轮补充/修改/撤销，以及连接失败保留输入、恢复后重试、重新开始后上下文隔离、Enter发送与Shift+Enter换行。正常对话控制台未见错误；断连检查故意产生一次502。移动端布局有媒体查询，尚未用真机验收。

变更Python文件Ruff检查通过，mypy检查31个文件通过，compileall通过；最终多轮/HTTP/导入安全相关58项测试通过。全量Ruff仍保留已有3处长行提示（原会话路由2处、预算服务1处）。文档本地链接和代码围栏检查通过。

下面是第一小步的历史讲解；涉及“尚未多轮/HTTP”的句子仅描述当时阶段，当前能力以上述第二小步为准。

# M2第一小步：一句话怎样变成旅行需求

这一步已经接通DeepSeek，入口是独立命令，不用先启动网页或数据库。
它完成单条消息的抽取、字段校验、缺项追问和一次输出修复。完整M2还需要多轮合并、消息保存、HTTP接口和评测集。

2026-09-10：模型访问已改为LangChain的`ChatOpenAI`，当前通过DeepSeek的兼容接口调用。现有命令、配置名、需求校验及一次修复规则保持兼容。

## 1. 先看最终效果

你输入：

> 从上海去杭州，玩三天，两个人，全团总预算五千元，节奏轻松，喜欢美食，不爬山。

程序输出的关键部分是：

```json
{
  "destination": "杭州",
  "origin": "上海",
  "days": 3,
  "travelers": 2,
  "total_budget": "5000.00",
  "pace": "relaxed",
  "interests": ["美食"],
  "hard_constraints": ["不爬山"]
}
```

这只是完整响应中`extraction`的一部分。完整响应还包括日期、其他偏好、推导依据、原消息和追问。

如果你只输入“想去杭州”，程序不会擅自替你决定人数和预算，而是输出一个问题：

> 请一起补充：旅行日期或天数、出行人数、全团人民币总预算。

## 2. 开发顺序和每个文件的作用

开发先写测试，再写实现；理解代码时可以先看“表格”，再看“填表流程”。

| 批次 | 文件（相对backend目录） | 通俗作用 |
|---|---|---|
| 1 | `tests/test_requirement.py` | 先写考卷：完整需求怎么返回、缺项怎么问、错误怎么处理 |
| 1 | `app/schemas/requirement/base.py` | 定义旅行表格：有哪几栏、每栏能填什么 |
| 2 | `app/services/requirement/prompt.py` | 给模型的填表说明，包括正反例和参考日期 |
| 2 | `app/services/requirement/extract.py` | 组织整个流程：发给模型、检查、汇总缺项、必要时修复 |
| 3 | `tests/test_llm_client.py` | 模拟网络成功、超时、限流、错误响应，检查客户端行为 |
| 3 | `app/config.py` | 读取密钥、模型名和超时设置 |
| 4 | `app/llm/client.py`、`app/llm/__init__.py` | 用ChatOpenAI创建模型、invoke调用并取回content |
| 4 | `pyproject.toml`、`uv.lock` | 声明LangChain模型接入包及底层依赖，锁定安装结果 |
| 5 | `tests/test_requirement_cli.py` | 检查命令参数、结果打印和配置错误 |
| 5 | `scripts/demo_requirement.py` | 你实际运行的按钮：读一句话、调用服务、打印结果 |

收尾还更新了`.env.example`和`tests/test_import_safety.py`，用于提供配置示例和验证导入没有联网行为。
自动生成的`uv.lock`不手写注释。新增手写Python文件有中文模块、类、函数和关键分支说明。
函数说明继续放在`def`或装饰器上方，中间空一行，沿用项目约定。

目前尚未启用Agent目录，提示词先集中放在需求服务旁边；后续引入Agent时再按实际需要迁移。

## 3. 运行时的数据顺序

主线：**命令行入口 → 需求服务 → 提示词与DeepSeek客户端 → Pydantic校验 → 汇总结果**。

1. 入口拿到你的原话、配置路径和参考日期。
2. 服务把填表规则放在`system`消息，把你的原话放在独立的`user`消息。
3. 客户端使用`ChatOpenAI(...)`创建模型对象，调用`self.model.invoke(messages)`发送消息。
4. LangChain把DeepSeek响应转换为`AIMessage`，我们只需要读取`response.content`。
5. `TravelRequestExtraction.model_validate_json()`把文本转换成旅行需求对象，并执行校验。
6. 如果答案有格式或业务错误，服务附上校验原因，让同一个模型修一次；仍失败就报错。
7. 日期齐全但天数未填时，程序按首尾日计算天数，并记录推导依据。
8. 服务自己找缺失字段，生成一个集中问题，最后把结果交还入口打印。

**模型填写内容，程序检查规则。**程序不会自动知道模型是否准确理解了所有自然语言，所以还需要真实样例评测。
JSON模式只约束输出格式，本地校验负责检查业务字段。这一接法参考[DeepSeek JSON输出文档](https://api-docs.deepseek.com/guides/json_mode/)和[思考模式文档](https://api-docs.deepseek.com/guides/thinking_mode/)。

### 先读懂client.py的三步

以下是帮助理解的缩略示例，完整的配置和异常处理以源码为准：

```python
# 1. 创建一个模型对象；base_url决定请求发给谁。
self.model = ChatOpenAI(
    model=settings.deepseek_model,
    api_key=settings.deepseek_api_key,
    base_url="https://api.deepseek.com",
)

# 2. invoke表示“现在把消息发给模型”，JSON模式要求返回JSON文本。
response = self.model.invoke(messages, response_format={"type": "json_object"})

# 3. content就是模型回答，交给需求服务继续检查旅行字段。
return response.content
```

`ChatOpenAI`是LangChain的接入类名，使用DeepSeek地址和密钥时，请求仍然发给DeepSeek。后续接入兼容接口的模型可以沿用这个类；各提供方专用参数仍需要分别确认，当前`thinking`是DeepSeek参数，不是所有模型都支持。

完整代码中的`timeout`控制等待时间，`max_retries=0`关闭SDK自动重试。DeepSeek的`thinking`和`max_tokens`通过`extra_body`传递，避免框架把token参数改成另一种名字。

`http_client=http`仅用来复用并关闭底层连接，也方便离线测试；业务代码不再手写`http.post`、Authorization请求头或解析`choices`。函数末尾的异常处理把SDK错误翻译为可展示的中文。

调用方式参考[LangChain ChatOpenAI文档](https://docs.langchain.com/oss/python/integrations/chat/openai)。当前仍用`DeepSeekClient`作为业务包装类名，因为配置和专用参数只接了DeepSeek，尚未实现多提供方路由。

## 4. 几个容易混淆的地方

- `schemas/requirement/base.py`是数据格式，不是建表文件；不会改变PostgreSQL表。
- `null`表示用户没说；字段在JSON里直接消失则表示模型没有遵守输出格式。
- `[]`表示列表没有内容，比如用户没提兴趣。
- `total_budget`是全团人民币预算。金额用字符串保存，内部使用`Decimal`。
- `missing_required_fields`由程序计算；模型不能自己声称“信息齐了”。
- `assumptions`记录日期或金额推导依据，不是把猜测当作用户原话。
- 首版支持2–5天、1–8人。负预算、错误类型和日期天数矛盾会被拒绝。
- 最小规划条件为目的地、日期或天数、人数、总预算；出发地在当前抽取阶段不强制要求。
- 暂时只对`intent=plan_trip`生成追问。其他意图只识别和返回，尚未执行对应业务。
- 因此不能只看`missing_required_fields=[]`就判断可以规划；还要看`intent`。
- “不是两个人，是三个人”这类消息可以提取新信息，但目前不会替你更新此前的需求。

## 5. 直接运行

当前电脑根目录学习配置已有`DS_API_KEY`，可以显式复用：

```powershell
Set-Location E:\TravelMindAI\backend
./.venv/Scripts/python.exe -X utf8 -m scripts.demo_requirement --env-file .env --message "从上海去杭州玩三天，两个人，总预算五千元，轻松一点"
```

查看缺项追问：

```powershell
./.venv/Scripts/python.exe -X utf8 -m scripts.demo_requirement --env-file .env --message "想去杭州"
```

固定日期，复现相对时间：

```powershell
./.venv/Scripts/python.exe -X utf8 -m scripts.demo_requirement --env-file .env --reference-date 2026-09-09 --message "下周末两个人去杭州，每人预算2500元"
```

不传`--reference-date`时采用上海今天。规则将“下周末”解释为下一自然周的周六、周日；上述固定日期对应2026-09-19到2026-09-20。

这些命令会真实调用模型并产生API费用。每次成功的正常抽取通常只请求一次；输出修复时最多两次。
30秒配置是HTTP各网络阶段的超时，不是整次业务流程的绝对总时限。网络错误不自动重试。

正式配置推荐放在已有`backend/.env`中追加以下变量，不要覆盖其中的数据库设置：

```dotenv
TRAVELMIND_DEEPSEEK_API_KEY=填写你自己的密钥
TRAVELMIND_DEEPSEEK_MODEL=deepseek-v4-pro
TRAVELMIND_DEEPSEEK_TIMEOUT_SECONDS=30
```

然后在backend目录执行上述显式读取`.env`的命令。
本次没有修改任何真实`.env`；只有配置示例新增了字段。密钥不出现在日志或文档中。

## 6. 本小步的验证证据

2026-09-10切换ChatOpenAI：新增一项LangChain消息转换测试，保留HTTP请求参数、异常、超时和业务回归验证；全量124通过、34数据库测试跳过。真实DeepSeek重新验证完整需求和缺项追问两条输入。依赖锁定为langchain-openai 1.6.2、openai 2.54.0，没有保留langchain-deepseek接入包。

2026-09-09：

- 需求、客户端、入口三个测试文件分别先因实现不存在而失败，实现后通过。
- 新增52项离线测试：需求29项、模型客户端19项、命令入口4项。
- 全量测试123通过、34跳过；跳过项需要显式测试数据库地址，本次没有重跑真实数据库验收。
- 新增/修改的Python文件通过Ruff检查；mypy检查27个文件通过，compileall通过。
- 全量Ruff仍有原来的3处长行：`app/api/trips.py`两处、`app/services/budget_service.py`一处。
- 测试仍有原有的2条依赖弃用提示。
- 真实模型`deepseek-v4-pro`验证了完整需求、缺少人数预算、相对日期及人均转总额、要求编造字段的输入，共4个样例。
- 第四个样例没有产生编造的人数预算；这不等于已证明对所有提示注入都能防御。

离线测试命令：

```powershell
./.venv/Scripts/python.exe -X utf8 -m pytest tests/test_requirement.py tests/test_llm_client.py tests/test_requirement_cli.py -q
```

下一小步：明确“保留旧值、覆盖新值、删除旧限制”的规则，实现多轮需求合并；随后接消息HTTP接口及保存记录。30条标注数据集和准确率验收仍未完成。


## 背景定向调整：参考山峰照片的青绿淡彩版（2026-09-17）

用户补充山峰参考图后，生成`designs/travelmind-brand-v1/mountains-pastel-reference.png`淡彩绘画素材，由`index.html`背景SVG引用；保留三层以上山体、雾带和细山路，CSS控制山景与雾层缓慢移动。背景之外全文比对一致，未改Logo、正文、输入框、页面布局和交互。前版保存为`index-v4-svg.html`。SVG解析、脚本语法和动画暂停通过，浏览器无警告或错误，仍为独立设计预览。

## 品牌预览第四版：只细化淡彩背景（2026-09-17）

用户明确保留淡彩风，不采用写实方向。`designs/travelmind-brand-v1/index.html`恢复SVG背景，随后仅细化背景的CSS与SVG：不对称山脊、局部坡面晕染、低透明纸面颗粒、虚化远山、漂移谷雾，以及更细的山路。细化前保留为`index-v4-before-refine.html`。Logo、正文、输入框、布局和交互均保持不变。

通过剔除背景块后的完整文本对比验证修改范围；内联脚本语法、SVG解析和背景暂停检查通过，浏览器未报告警告或错误。第三版仅留作未采用的设计尝试，当前页面不使用其写实素材。

## 品牌预览第三版：自然云山背景（2026-09-17）

按背景质量批注调整独立预览，先保留`index-v2.html`，再生成`mountains-v3.png`山水摄影风格素材，并由`index.html`引用。该素材为生成图，不指代真实景点；画面用自然山脊、细山路和云海取代重复几何色块。CSS背景移动与独立雾层漂移共用原有暂停、减少动态效果及页面隐藏控制。Logo字距和AI加粗保持不变。

内联脚本语法通过；已查看桌面和390×844小屏，确认背景和雾层暂停状态、无横向溢出，浏览器未报告警告或错误。仍为独立设计稿，未接入业务服务。

## 品牌预览第二版：山路背景与字标（2026-09-17）

按浏览器批注修改独立设计稿：先将原页面和字标保留为`index-v1.html`、`logo-v1.svg`，再把`designs/travelmind-brand-v1/index.html`的背景替换为分层山峦、由远及近的山路和移动光点。使用原生SVG与CSS动画，暂停按钮同步控制所有背景动画；减少动态效果与页面隐藏处理继续保留。

同一页面中的导航字标和品牌弹窗扩大字距、加粗AI，下载文件`logo.svg`同步更新。README与设计索引记录两版文件；新版截图使用`preview-desktop-v2.png`和`preview-mobile-v2.png`，保留原版截图。验证了脚本语法、SVG解析、桌面与390×844小屏显示、暂停状态，当前仍是独立预览，未接入正式Vue页面。

## 品牌与动态首页独立预览（2026-09-17）

本次先交付设计首版，目录为`designs/travelmind-brand-v1/`，尚未接入正式Vue页面或后端服务。

1. `logo-mark.svg`定义路线M图标；`logo.svg`把图标与TravelMindAI文字组合，均为透明背景矢量文件。
2. `index.html`先按湖蓝青、暖白、日光橙建立首页，再用原生WebGL绘制缓慢流动的丝绸褶皱。页面支持暂停背景、色板查看、旅行示例填入和本地发送预览；减少动态效果偏好、页面隐藏及WebGL不可用均有对应处理。
3. `README.md`说明文件职责、主题色与启动方法；`_d_meta.json`记录设计待评审状态；`preview-desktop.png`、`preview-mobile.png`保存实际浏览器预览截图。

验证：内联JavaScript语法和两个SVG解析通过；在1280×720与390×844浏览器尺寸下检查页面，无横向溢出；实际点击验证品牌弹窗、示例填入、发送预览和动画暂停。浏览器未报告警告或错误。本次不代表正式前端已换肤，也不涉及数据库或模型运行验收。

## 本地目录整理（2026-09-17）

先将两份研究数据整理为根目录`data/chinatravel`与`data/lvbangpt`，再把已有检测报告移到`temp/reports`、可移动日志移到`temp/logs`、上传原文移到`temp/uploads`。`backend/app/config.py`把默认上传目录改到根目录temp；两个evaluate脚本把默认报告输出改到temp/reports。`.gitignore`排除temp及本机数据，AGENTS记录阶段结束清理规则。上传原文、资料原稿与备份不随阶段删除。

运行中的主任务仍使用`backend/data/runtime`中的文件，整体搬迁因日志被进程占用而暂缓，已恢复尝试移动的文件；未停止或重启主任务服务。`data/uploads`和`backend/data/uploads`临时目录联接指向`temp/uploads`，兼容已启动服务保存的旧配置。该次整理时两份配置尚未合并；2026-09-17已合并正式配置并将学习脚本移出项目，见本文顶部目录整理记录。


## 品牌预览背景避让与山间公路（2026-09-17）

本次只调整独立预览的背景：先在`designs/travelmind-brand-v1/index.html`中压低山景、调整横纵比例与竖屏裁切，让输入框下方露出主要山脊；再在背景SVG中添加由宽变窄的淡色公路与中心线，复用山景动画并叠加现有雾层。素材仍为`mountains-pastel-reference.png`；README记录变化，`preview-pastel-road.png`保存宽屏效果。未改正式前端与后端。

验证：非背景HTML、CSS与脚本逐段对比一致；SVG解析通过；1600×900与390×844浏览器实看通过，手机无横向溢出，浏览器无警告或错误。


## 品牌预览输入区域下移（2026-09-17）

`designs/travelmind-brand-v1/index.html`恢复背景SVG等比例铺满，取消压缩比例和竖屏特殊裁切；仅给输入框、推荐按钮与提示小字增加30px相对位移。公路、标题、导航、页脚和业务交互保持不变，README同步记录。浏览器1600×900实测三个目标元素均下移30px，标题、导航、页脚位移为0，并已查看整体画面。


2026-09-17追加微调：`index.html`中的输入框、推荐按钮和下方小字在现位置再下移20px，相对原始位置累计50px。浏览器实测三个元素均增加20px，导航、标题、页脚位移为0；背景未变。


2026-09-17背景选定：用户指定第三张图，其SHA256与现有`mountains-pastel-reference.png`完全一致，继续直接引用该素材。`index.html`移除后来叠加的SVG公路，保留图片自带的细山路、等比例显示和现有动画；输入区域累计下移50px保持不变。


## 品牌首页接入登录与智能体（2026-09-17）

1. `frontend/tests/landing-auth.mjs`先验证旧页面会在加载时请求身份的差异，再以真实App状态与模拟HTTP检查入口分支；不使用真实口令或模型。
2. `frontend/src/components/LandingPage.vue`承接确认稿的山景、字标、等比例背景与输入区累计下移50px，只向App上报开始规划或发送事件；保留品牌弹窗和背景暂停。手机端保留开始规划按钮。
3. `frontend/public/brand/`保存与确认稿一致的背景和两个SVG Logo，`frontend/index.html`设置品牌图标、标题和主题色。
4. `frontend/src/App.vue`在操作时用现有auth/me检查Cookie，再进入已有智能体。401复用登录/注册弹窗；发送登录后新会话续答一次，开始规划带入已有文字但不自动发送。登录失败、取消与断网保留首页问题；请求中拦截重复操作。首页不读取私人历史；退出或登录失效仍清除私人状态。`style.css`移除被新首页替代的访客样式，聊天功能继续使用原样式。
5. README说明正式入口，08文档更新组件职责与登录调用链；不修改后端鉴权、API合同或数据库。

验证：`node tests/landing-auth.mjs`通过首页无请求、401/取消/失败重试、有效Cookie、续发一次、重复点击和断网检查；`node tests/requirement-stream.mjs`既有流式/行程/会话回归通过；`npm run build`类型检查和打包通过。真实浏览器在5173检查两个入口收到401后弹出登录，取消后原问题仍在；宽屏与390×844手机页面已查看，手机无横向溢出。未使用真实账号登录或调用模型，本轮已登录与续发分支为模拟HTTP验证。


## 登录弹窗绿色主题（2026-09-17）

`frontend/src/App.vue`将Logo和TravelMind AI合为横排品牌行，AI沿用加粗青绿色；`frontend/src/style.css`将登录/注册弹窗、选项卡、输入框及按钮统一为淡绿和品牌青绿，自动填充底色也保持淡绿。脚本逐段比对未改，登录与注册逻辑保持不变。已检查浏览器登录/注册状态，`npm run build`通过类型检查与打包。

## 对话页独立交互设计（2026-09-17）

用户选择先确认可交互设计稿，本轮新增 `designs/travelmind-chat-v1/`，未替换正式对话页，也未接入私人附件保存或智能体读取。

1. `index.html` 定义固定侧栏、历史菜单、底部个人信息、新对话居中输入区及弹窗；用户和管理员都不再展示右上角绿色旅行对话框。
2. `chat.css` 沿用青绿品牌，以淡绿圆角区分新建和历史选中态；姓名完整显示，长回复独立滚动，输入框留在底部。手机侧栏使用抽屉。
3. `chat.js` 把点击和输入转换为内存中的示例会话变化，支持历史搜索、重命名、删除确认、附件卡片选择和移除、示例发送及身份预览。附件不上传，回复明确标注为示例；知识库和退出仅展示入口说明。
4. README 说明设计范围和启动方式，设计索引记录待确认状态，浏览器截图保存新对话、已有对话和手机效果。

验证：JavaScript 语法检查通过；1440×1000 桌面和 390×844 手机浏览器实看，手机无横向溢出。实际验证历史切换、搜索、重命名、删除取消与确认、完整姓名、管理员布局、文件选择与移除、发送后输入框定位和侧栏收起。控制台未报告警告或错误。不涉及正式服务、数据库或真实模型验收。

## 确认稿对话页正式接入与旧稿清理（2026-09-17）

1. 先在 `backend/tests/test_session_rename.py` 和 `frontend/tests/landing-auth.mjs` 增加改名与空白新建验证，确认旧接口405、旧新建立即创建记录不符合目标，再完成接入。
2. `backend/app/schemas/trip.py` 增加仅含title的改名请求；`services/trip_service.py` 在事务内锁定当前账号会话、修改标题；`api/trips.py` 暴露PATCH接口，继续使用Cookie身份与现有写请求校验。复用sessions.title，不新增表或迁移，updated_at保持不变以避免影响分页。
3. `frontend/src/api/requirements.ts` 调用改名接口；`App.vue` 把确认稿接到真实会话状态，左栏显示分组历史及三点菜单，左下角完整显示账号，管理员保留知识库。改名失败保留原名与弹窗，成功后更新；登录/列表代数阻断过时响应。新建仅清空当前选择，第一次发送才建会话。
4. `frontend/src/chat.css` 管理限定于App的对话样式；`components/ChatIcon.vue` 提供原生线条图标。首页与登录视觉继续保留，景点/餐馆/行程、撤销、流式进度与失败重试复用原组件。`style.css` 清理旧导航、账号、历史及卡片布局。私人附件未接入，“＋”显示尚未开放提示，用户可以粘贴要点；不带入原型的模拟身份、示例回复和假上传。
5. `frontend/tests/requirement-stream.mjs` 将旧“直接删除按钮”检查更新为三点菜单入口，当前会话通过正式选择入口建立，原有删除、在途响应和账号隔离断言继续保留。README记录产品行为，08同步调用链与改名合同。
6. 按用户授权删除品牌目录下15个未采用的HTML、旧字标、写实背景和过时截图（约2.46MB）；保留当前品牌及对话确认稿。设计README与索引不再引用已删版本。上传原文、知识资料、数据库和其他任务文件未清理。

验证：重命名及原删除8项测试使用backend/.env的数据库，在独立测试schema运行并通过，含持久化、归属、并发和失败回滚；修改范围Ruff及mypy通过。前端首页登录/空白新建/改名失败保留、SSE/行程/撤销/删除隔离回归和类型检查/生产构建通过。浏览器使用真实Vue组件与隔离模拟接口，检查1440×1000及390×844界面、三点菜单、改名/删除、新建发送、管理员完整姓名、资料入口；手机无横向溢出，浏览器无警告或错误。正式后端已重启加载PATCH路由，真实匿名PATCH返回401；未使用真实用户调用模型，不将模拟HTTP截图视为真实模型验收。


## 新建对话空状态精简（2026-09-17）

1. `frontend/src/App.vue` 将品牌下方标题与说明合并为“这次，想去哪里？”，移除新建页的推荐按钮与底部小字；已有对话仍保留AI内容提示。
2. `frontend/src/chat.css` 将品牌与短句间距收紧为12px，短句与输入框间距收紧为18px，并移除本次删除元素对应的样式；桌面及手机沿用相同间距。
3. 此次仅调整空状态展示，不修改发送、登录、历史记录或后端接口。

验证：前端类型检查及生产构建通过，确认旧空状态文案、推荐列表与关联变量已移除。本轮未进行登录后的浏览器实看。


## 智能体上传附件接入 MinerU（2026-09-20）

1. `deploy/mineru/requirements.txt`固定MinerU 4.0.4，`requirements.lock.txt`记录独立Windows环境依赖，`start.ps1`以ONNX CPU和llama.cpp启动本机8010服务；模型放.gitignore保护的.runtime，不改后端依赖环境。
2. `services/attachment/mineru.py`负责提交原件、等待任务、下载结构化结果并校验完整页数。真实长沙PDF证实自动模式会复用错码文字层，因此PDF/图片固定ocr模式；DOCX仍读原生结构。
3. `services/attachment/storage.py`允许合法扫描PDF上传；`models/attachment.py`与迁移0013增加私有完整正文缓存parsed_json。缓存读写保持会话归属锁，删除仍沿用既有流程。`schemas/attachment.py`新增可空parser_version，旧历史可读，重新选择旧附件时可升级结果。
4. `services/attachment/analysis.py`按批读取全文并核对同页依据，保留表格内容、物理页码和后续批次；`reader.py`先缓存正文再提取。PDF图片页及独立图片复用Qwen补读，`llm/vision.py`支持非连续物理页标签。
5. `api/requirement/history.py`在普通HTTP及SSE中注入本机客户端；`config.py`和.env.example记录地址与600秒解析期限；`llm/client.py`为附件提取单独提供8192输出容量，普通对话仍2048。不新增智能体，不改变共享知识库、普通问答或行程用途合同。
6. `tests/test_mineru.py`覆盖扫描件入口、完整页数、失败任务、长文末页、引用页、引用改写、非连续图片页、失败复用及真实隔离数据库缓存；原上传大小测试改为替换当前PDF封装读取入口。README及08同步部署、调用图、数据字典与实际验收。

验证记录：数据库备份后升级0013，Alembic check通过。最终全量后端777 passed、2 skipped（未配测试Milvus）、2 warnings；修改范围Ruff与mypy 94个源码文件通过。真实16页长沙PDF强制OCR约79.7秒，正文提取约72.5秒得到54个地点，四批图片补读约226.9秒，合并为113个地点及25条待确认提醒。以上为同一原件分阶段调用结果，不是该大文件单次HTTP端到端耗时或准确率统计。扫描页和DOCX表格独立解析各约4秒；真实HTTP扫描附件完整保存约10.4秒，再次发送约3.1秒复用；最新SSE实测约11.8秒、9条进度事件，6个地点包含返程杭州东站。未新增浏览器验收，不把样本耗时外推为所有文件的保证。专用验收账号、会话及其上传副本已通过原服务清理；temp/runtime/mineru-setup、旧日志及旧blobs的文件删除命令被自动审批审查拦截（blocked by policy），未改用其他方式绕过，剩余文件均被Git忽略。用户原件、数据库备份、模型与当前服务日志保留。


## 旅行草稿整程设计稿（2026-09-20）

用户选择三天连续展示。新增 `designs/travelmind-itinerary-v1/`：先用 `index.html` 排好行程总览、每日时间线、侧边预算与查询入口，再由 `itinerary.css` 适配桌面和手机，最后由 `itinerary.js` 加上跳转、展开、问题填入及版本状态示例。品牌图标复用现有资源。

本轮未修改主程序、后端接口或数据库。示例行程沿用用户截图，费用分项仅用于设计。JS语法检查通过；桌面和390px手机实看、展开、跳转、填入及弹窗交互已检查，手机无横向溢出，控制台未报告警告或错误。完整边界记录在设计稿README。


## 旅行草稿确认稿接入（2026-09-20）

1. `frontend/tests/requirement-stream.mjs` 先增加起终点、人数、晚数、日期和历史出发地断言，旧版在“2 人同行”检查失败；补充日期已定/未定与旧历史缺失起点的检查。
2. `useRequirementConversation.ts` 从每轮已保存响应的result.extraction.origin取得起点，存入对应消息；`App.vue` 传给该消息的行程卡片，避免用当前需求覆盖旧草稿。
3. `components/ItineraryCard.vue` 用放大的起终点替换原宣传标题，展示天数/晚数、人数、日期；逐日连续展开、按当前卡片定位，预算移至侧边，地址和来源按需展开。沿用真实来源、地图入口、预算明细、天气/路线提问及撤销事件。
4. `components/itinerary.css` 管理卡片与容器响应式布局；`chat.css` 为含行程的对话留出双栏宽度，普通消息保持原有阅读宽度。窄屏将预算移至下方，完整日期单独一行。
5. 设计目录同步移除宣传标题、放大出行信息并标记已确认。08记录展示层调用关系；无后端或数据库变更。

验证：前端回归覆盖原有SSE、历史恢复、撤销失败/冲突/隔离及地图来源，新增显示合同通过；类型检查和生产构建通过。正式Vue组件在隔离样例中检查1440×1000和390×844布局、完整日期、路线填入、预算展开、撤销事件及多草稿按天跳转；手机无横向溢出。未在真实账号发起模型或撤销请求。
## M4-C 实施记录（2026-09-20）

先补独立审查，再补数据库与图状态，最后接回原 HTTP 和页面。当前小步记录以 08 第 12.17 节为架构依据，完整验收结论在本节末尾更新。

1. `services/itinerary/review.py`：重新构造最终草稿，检查日期、时间、预算、原文来源和修改范围，再通过独立模型请求审查意图；`graph.py` 只增加返工意见输入，原工具权限和额度不变。
2. `schemas/workflow.py` 定义等待/完成/取消及恢复选择；需求请求和响应增加可选字段，旧历史兼容。`models/workflow.py` 和 `0014_chat_workflows.py` 注册会话执行及官方检查点表，删除会话级联删除状态。
3. `services/chat/workflow.py` 在原聊天链外增加 `generate → review → publish`；`CandidateHistory` 截获最终保存，审查通过后调用原 `RequirementHistoryService.append`。返工最多两次；缺项或取舍进入 `interrupt`，新的明确回复通过 `Command` 接续同一执行。
4. HTTP 两种入口提供可重复创建的检索上下文。前端类型、会话状态和 App 展示等待卡、候选预览及采用/保留现状；补充条件仍在原输入框发送，刷新及断网重试沿用原编号与选择。
5. 新增独立审查与 PostgreSQL 工作流测试，前端新增 `workflow.mjs`。原 SSE 断线测试适配编排入口，迁移测试补齐新表清单。

恢复粒度是外层节点：中断中的只读模型/查询可能重复，候选检查点之后不会重跑生成。已提交消息重试直接读历史，不保证所有外部查询恰好一次。普通自然聊天仍走原回答业务，外层持久包装不使它变成工具 Agent。

本轮修补记录：独立复核发现接续失败后原编号可能被覆盖，已用真实PostgreSQL与Vue状态层复现后修复；同时提供丢失编号后的安全取消。真实DeepSeek发现审查JSON模式提示词缺少JSON字样，补充后从已保存候选恢复成功。外层图原本会将检查点隐式传入内部Agent，已用真实嵌套图测试复现并为行程/周边两处显式关闭继承，保证恢复边界与闭包数据一致。取消时恢复本次编排前需求，避免未采用候选留下条件变更。

### M4-C 最终验收结论

2026-09-20：M4-A/B/C核心范围完成。最终全量 **818 passed、0 skipped**，真实隔离PostgreSQL/Milvus，109.03秒。mypy 98文件、改动文件Ruff、前端四组Node回归、Vue类型检查及Vite构建通过。44条警告包括既有Starlette/MCP提示及内部Agent显式覆盖durability时的依赖提示；外层同步持久化不变。内部Agent关闭检查点后显式使用async，是为避免LangGraph 1.2.11继承sync模式时访问不存在的保存future，两个真实嵌套图回归覆盖此情况。

正式数据库已先备份到 `temp/backups/travelmind-before-m4c-20260920.dump`（3,205,740字节），再执行0014；Alembic check无差异。两次不同恢复边界实测：浏览器缺预算暂停→停服重启→补预算继续同run_id；候选已保存但Reviewer请求失败→停服重启→同请求3秒内仅完成审查和提交，attempts仍为1。初次模型请求出现超时，保留同编号后重试成功，不把失败样本算作一次成功。

真实DeepSeek＋Milvus/PostgreSQL＋百度地图＋浏览器：杭州两天v1，指定第二天11:00生成v2（7秒），撤销v3；第一天与4000元预算完全保留，v3完整计划和需求等于v1。HTTP重复原接续请求返回完全相同结果。加入未核实轮椅无障碍要求时暂停，点击保留现状后取消，恢复编排前需求，SQL确认仍只有3份行程；刷新恢复v3及取消结果。

私人Markdown上传/解析后，真实API参考附件18秒生成断桥残雪、雷峰塔两日草稿并通过Reviewer；地点事实采用本轮知识库查证来源，原附件识别快照保留。另一个带未核实硬条件的附件样本6.4秒进入持久等待，不生成草稿、不可强行采用。修复了附件回复重建RequirementResult后message_intent为空导致漏掉interrupt的问题，单测先红后绿。此前M4-B清晰图片首次OCR到行程证据继续有效，本轮不重测或外推复杂图与长PDF稳定率。

独立代码审查的问题已修复：接续失败/运行锁409保留原请求，禁止覆盖后续重试身份；丢失编号后仍能明确安全取消。集成测试还覆盖最多两次自动返工、choice采用、提交后断线幂等、跨会话/过期选择拒绝、并发锁、级联清理及完整迁移往返。

边界：恢复到外层节点，不保证外部模型/只读查询恰好一次；缺项与人工选择等待由下一次请求触发恢复，不新增后台常驻执行器。实际逐段路线、实时票价、跨会话长期偏好和复杂资料广泛质量评测不在本轮闭合范围。代码尚未提交或推送。

阶段清理：本轮3个专用会话及1个专用账号已移除，数据库复核执行记录级联清空，私人验收原件由受认证删除接口清理；浏览器验收页关闭，临时结果、凭据及已关闭日志已清理。保留迁移前备份和当前服务日志 `temp/logs/m4c-backend-accepted*.log`；服务存活/就绪均200，未清空temp或触碰用户历史。


## 2026-09-21：三模型选择与旅行体验优先

用户确认优化方向后，按三模型完整切换→行程事实与停止体验→主规划＋按需研究的顺序实施。四独立Agent旧计划已标记被替代，不再以增加角色数量验收。

1. `llm/contracts.py`、`providers.py`、`config.py`：限定三家配置，补原生工具响应及token/耗时，保留SDK错误脱敏；`.env.example`说明实际首选和备用行为。本机Kimi端点修正为官方`https://api.moonshot.cn/v1`，型号修正为`kimi-k2.6`，未改变密钥。
2. `llm/gateway.py`、`gateway_client.py`、`tool_model.py`：用户选择按请求共享；文本与工具都走健康网关，备用成功后本轮保持；原生工具消息编号和参数完整保留。正文开始后不换模拼接。
3. `schemas/requirement/chat.py`、`history.py`、`api/requirement/routes.py`：请求、历史和status携带首选/实际模型；旧消息缺省deepseek。相同消息编号换选择仍受幂等冲突保护。
4. `api/requirement/history.py`、`services/chat/events.py`、`main.py`：SSE线程登记停止信号，调用边界取消，线程实际结束后释放登记；鉴权和会话归属沿用原依赖。
5. `services/chat/workflow.py`、`services/requirement/history.py`：候选/检查点/最终JSON保留模型及研究摘要，原事务、有限返工和恢复路径不另建一套。
6. `frontend/src/api/requirements.ts`、`useRequirementConversation.ts`：冻结每次发送的模型、显示fallback、刷新恢复首选；停止确认后保留输入。完整回归发现普通失败丢失幂等编号，已修复并通过原附件回归。
7. `frontend/src/App.vue`、`chat.css`：输入框三模型选择、未配置禁用、停止按钮及回答实际模型标签；移动窄屏隐藏快捷键提示，为选择器留空间。
8. `schemas/itinerary.py`、`services/baidu.py`、`services/itinerary/routes.py`：保存逐段地图估时、查询时间和未知状态；坐标系统及秒/分钟转换明确；超过预留时间要求返工，局部修改只核实目标日。
9. `services/itinerary/graph.py`、`research.py`：主规划通过一次受控工具委派独立研究循环；返回工具核实产物后继续规划。主/子消息隔离、共用查询额度，不新增自主Reviewer。
10. `services/itinerary/review.py`、`rules.py`、`frontend/src/components/ItineraryCard.vue`：程序审核仍保护预算、日期与不变更日；路线证据不当成用户活动修改。卡片区分交通预留、地图估时和未知，保留演示预算说明。

调用顺序：前端冻结选择→HTTP建立请求级共享模型上下文→原工作流→主规划按需委派研究→地点/路线/时间校验→独立审核→原子保存→实际模型与路线状态展示。普通聊天仍走原自然回答路径。

### 本轮验收结果

- 自动检查：最终后端 **941 passed、0 skipped**（111.61秒；真实隔离PostgreSQL/Milvus；46条既有依赖提示）；mypy **105文件通过**，本轮改动Ruff通过；前端SSE/会话、workflow、附件、landing-auth、model-selection五组回归及Vue/Vite构建通过。
- 新增固定`backend/evals/routes.v1.json`六项：步行/公交/打车、秒转分钟、无路线/缺时长及坐标系不匹配。由`test_route_estimates.py`执行；未修改原requirements/documents固定题库。研究测试验证主任务实际委派、子消息隔离与工具产物返回；停止测试验证线程未结束不能宣称停止、499不保存，以及前端保留输入/换模新编号。
- 真实三家基础调用：DeepSeek `deepseek-flash`、Kimi `kimi-k2.6`、Qwen `qwen3.8-max`分别通过JSON、流式文本与原生工具小样本；各次确认实际使用所选提供方。Kimi使用[官方接入地址](https://platform.kimi.com/docs/get-api-key)，不再使用开发者网站作为API端点。
- 浏览器：专用测试账号完成Kimi自然聊天→刷新恢复Kimi；Qwen规划中停止→确认后保留完整原输入→换DeepSeek生成杭州两天v1→换Qwen仅把第二天改11:00得到v2→刷新恢复Qwen及v2。页面实际模型标签分别为Kimi/DeepSeek/Qwen；390px窄屏选择器和发送按钮无遮挡，视口已恢复。
- 路线真实链：v1规划查询地图并修正候选后成功。卡片显示预留75分钟、地图估时30分钟；持久JSON为百度5513米、30分钟及查询时间。HTTP重读确认v2第一天包含路线证据在内逐字段等于v1，预算也相同；停止请求未多写消息或草稿，最终仅三条完成消息。
- 独立研究真实链：另一个Kimi首选任务发生Kimi→DeepSeek接续，最终完成行程；响应与数据库重读均含research completed记录、独立task_id/parent_task_id、7次查询、6.934秒。两提供方均记入used_providers，未把备用结果冒充Kimi独立完成。该样本证明接续和研究委派实际发生，不代表故障降级率或规模化质量结论。
- 独立只读审查未发现可明确复现的P1/P2；提出原生工具缺token字段的非阻断问题，已先补失败测试再修复。全量检查另发现附件普通失败丢失原编号，修复后原回归通过。
- 正式服务已使用`backend/.env`重启到本轮实现；重启前确认无在途连接、资料任务和数据库advisory锁。无新增SQL迁移，0014结构不变；未提交或推送。本轮专用账号、会话与不再使用的验收临时文件按项目约定清理，运行日志继续保留。

边界：停止为调用边界取消，在途外部HTTP需返回或超时；进程内停止/健康状态不支持多worker共享。预算仍是演示单价，地图估时不是未来实时交通。尚未做大规模真实规划、复杂附件稳定率和端到端P95/费用评测，下一步以固定样本测量这些质量和成本，再决定是否增加Agent。
