# 三模型与旅行体验优先实施计划

用户于2026-09-21确认按优化建议实施。本计划替代同日`model-choice-and-four-agents`中的四Agent必交付要求。

## 已确认的范围和取舍

1. 优先完成DeepSeek/Kimi/Qwen选择，覆盖文本、JSON和原生工具，健康首选不随机替换，故障使用兼容备用。
2. 优先改进行程事实核实、局部修改、自然聊天、等待和失败体验。真实路线耗时与演示预算分开；缺实时来源不编造。
3. 采用主智能体（对话与规划）＋按需研究子智能体。独立审核沿用程序规则＋单次模型审核，不为数量增加完整循环。
4. 四Agent留作评测驱动的后续扩展，不是当前验收门槛。已有工作流暂停、检查点、两次返工上限和幂等事务继续保留。

## 实施和验证

- [x] 模型基础：首选优先、备用切换、原生工具、首段之后不换模、并发槽位释放与安全错误分类。
- [x] 输入框：三选一下拉框、未配置状态、发送冻结选择、历史首选/实际模型；停止确认、原样重试及换模新请求。
- [x] 行程与体验：逐段地图估时、修改日边界、六项固定路线质量集；耗时及可空token记录。预算仍为演示单价，未建立真实费用账单。
- [x] 两Agent：主规划按需委派独立研究上下文/工具循环；审核仍是独立步骤。真实研究任务和父子记录已验证。
- [x] 验证：941项后端全量、真实隔离PostgreSQL/Milvus、105文件mypy、改动Ruff、五组前端回归和构建、三家基础真实调用及浏览器验收；详见07。

## 执行记录

- Ruling：沿用当前codex/m4-a工作区，不建立丢失现有未提交M4/M5实现的新checkout；只修改本任务涉及文件，不提交或推送。
- Ruling：用户最新确认优先于旧四Agent规格；三模型先形成完整可用切片，随后推进事实质量和按需研究委派。
- 本轮功能已交付到本机运行服务，未提交或推送。固定路线集与真实短样本通过，不据此声称广泛旅行质量、故障降级率或P95达标；这些量化评测优先于继续扩展Agent数量。
