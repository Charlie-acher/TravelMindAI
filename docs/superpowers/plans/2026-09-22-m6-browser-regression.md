# M5状态核对与M6浏览器回归第一步

用户要求核对M5并继续开发。沿用03中的三模型范围和M6浏览器回归路线；本步不改界面设计、不扩大Agent数量。

## 范围和执行

- [x] 以当前源码、2026-09-21三家真实调用记录和新全量测试核对M5；不把历史M5-A/B待办当成当前缺项。
- [x] 在现有Vue工程加入Playwright，仅增加开发依赖。固定接口夹具运行真实页面，默认拦截所有API请求，未声明接口立即失败。
- [x] 桌面1440×1000和手机390×844验证登录、Qwen发送、过程合并及研究助手、刷新恢复、完整文件预览、原生状态浮层、Escape和409恢复。
- [x] 最终回归及只读审查；同步03/04/07/08和README操作命令。

## 验收边界

浏览器接口夹具不是实际模型/数据库/Compose全栈验收。停止确认和流中断已有协议回归，本步未模拟供应商真实耗时或SSE网络分块。M6仍保留真实全栈端到端场景及部署验收门槛。

Ruling：本步为已有工作台补自动验收，用户已授权继续路线内开发，直接实施；由main新建codex/m6-browser-regression分支，在当前目录验证，保留运行中的本机服务。没有产品行为变更，因此先验证现有行为能否满足断言，不人为破坏产品制造失败。

## 实测记录

- 后端992 passed、0 skipped，135.30秒；使用backend/.env提供连接配置，PostgreSQL临时schema、Milvus测试集合隔离。46条已有依赖提示。
- 首次桌面/手机共6项浏览器回归通过；随后加强409读取另一页面新消息及重发采用新版本号的检查，最终结果见07同日记录。
- 审查修正：夹具由路径后缀匹配改为完整路径和HTTP方法匹配，错误会话编号/方法不能模拟成功。最终6 passed（14.1秒），原6组前端脚本、120文件mypy和git diff --check通过。
