# 这是可公开的配置样本，不是真实密钥文件。
# 已有 .env 时不要直接覆盖它；按需把下面的字段添加进去即可。
# M0 不调用模型，不需要填写 API Key、数据库地址或 Redis 地址。

# 环境变量采用项目前缀，避免与其他程序的 APP_NAME 等变量冲突。
TRAVELMIND_APP_NAME=TravelMindAI

# 仅允许 development（本地开发）、test（测试）、production（正式部署）。
# 填写其他值时，配置校验会报错，防止拼写错误被静默使用。
TRAVELMIND_ENVIRONMENT=development

# 配置优先级：操作系统环境变量 > 显式指定的 .env 文件 > 代码默认值。
# M0 的 load_settings() 默认不自动找 .env；传入文件路径才会读取文件。
# 因此只 import travelmind 不会读密钥、连模型或启动任何服务。
