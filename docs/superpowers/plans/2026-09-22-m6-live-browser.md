# M6真实服务浏览器回归

用户同意继续M6。继固定接口浏览器回归后，本步直接使用现有本机8000服务及backend/.env创建专用账号，不重启或改写管理员现有会话。

- [x] 独立真实服务Playwright配置：4176预览端口、单用例、无重试、不保存包含登录凭据的trace。
- [x] 真实规划v1→指定日修改v2→刷新→对应版本文件/下载→双角色权限→旧revision拒绝；不拦截业务接口、不预填模型输出。
- [x] 后端启动命令默认不调用；--live才创建临时账号，核对运行服务身份，调用浏览器。成功清理本次账号，会话失败时保留避免删除在途任务；费用账目保留。
- [x] 定向清理边界测试、类型/静态检查、真实运行和已有浏览器回归。
- [x] 根据真实失败修复可复现问题，同步07/08、进度及使用说明。

Ruling：沿用codex/m6-browser-regression和已有未提交文件，新增内容与上一小步区分记录。当前Compose只有Milvus/etcd，PostgreSQL和应用未组成独立全栈，因此本步真实本机链通过也不代表Compose部署通过；部署仍是下一项明确待办。

验收记录：首次规划成功但Chromium读取已取消的SSE响应体失败，改为等待页面解锁并HTTP读回当前原话后真实两轮通过（50.1秒）。随后将文件验证升级为实际点击下载并修正密码fill失败脱敏，真实两轮再次通过（38.0秒；整个命令49.8秒）。没有把第一次脚本失败记成产品失败或成功验收。并行构建造成固定接口页面404已复现，两个模式改用独立temp/runtime构建目录后6项回归通过（14.7秒），同时真实模式目录首页/脚本HTTP 200；未因此再次重复付费模型查询。新清理边界1项、脚本Ruff/mypy和TypeScript检查通过，专用账号剩余0，首次24条费用记录仍保留。
