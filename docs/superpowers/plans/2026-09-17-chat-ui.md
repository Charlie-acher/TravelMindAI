# 已确认对话页接入计划

用户已确认 `designs/travelmind-chat-v1/` 并授权接入主程序及删除停用设计。

1. 补充会话重命名：PATCH 自己的会话标题，校验空白与长度，事务内检查归属；验证匿名、跨账号、保存和失败场景。
2. 将设计稿替换正式聊天布局：侧栏、新建空状态、底部账号、历史三点菜单、原有消息/景点/餐馆/行程及流式进度，管理员保留资料管理。私人附件尚未实现，入口明确告知不可上传，不将私人文件送到共享知识库。
3. 回归首页登录、会话状态、重命名和打包；浏览器检查桌面和手机。删除本轮替换的旧布局及品牌废弃预览，保留确认稿、品牌素材、业务数据和运行服务。同步07、08及设计说明。

只接入已确认方案，不新增身份切换、示例回复或模拟上传到正式页面。新建按钮清空本地对话，首次发送才创建服务端记录。
