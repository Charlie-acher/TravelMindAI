# M4-C 实施计划

> **For agentic workers:** 使用 superpowers:executing-plans，在当前任务依次实施并在末尾独立审查。

**Goal:** 完成独立审查、最多两次自动返工、人工暂停及 PostgreSQL 重启恢复。

**Architecture:** 外层 StateGraph 截获现有聊天保存结果，审查后调用原事务。官方 PostgresSaver 保存节点，interrupt/Command 恢复同一执行。

**Tech Stack:** 现有 Python/FastAPI/SQLAlchemy/LangGraph/Vue，增加官方 langgraph-checkpoint-postgres。

**Spec:** ../specs/2026-09-20-m4-c-design.md

## 共同约束

- 原普通聊天、附件、局部修改和撤销契约继续有效。
- 自动返工最多两次；不自动放宽硬条件，不声称真实票价或路线已核实。
- 正式配置仅 backend/.env；测试使用独立 PostgreSQL schema。
- 保留未提交的 M4-B 验收修复。当前工作区连续实施，不提交或推送。

## 审查重点

重复请求与不同选择、跨会话恢复、暂停后的新消息、提交后断线、会话删除和在途图竞争，分别列入恢复集成测试。

## 步骤

1. [x] 独立审查：新增 review.py 及 test_workflow_review.py；先验证预算篡改、日期/引用错误和模型输出不合规被拒绝，再实现程序重算与独立 JSON 模型审查。规划器接收审查反馈数据。
2. [x] 持久数据：新增 workflow.py 模型、0014 迁移和官方依赖；在临时 schema 迁移，校验外键级联和 Alembic metadata。完整字典同步 08。
3. [x] 编排：新增 chat/workflow.py 和工作流 DTO；拦截 append，图运行 generate/review/wait/publish；先测试两次返工、重建连接恢复、发布重放幂等，再接入两种 HTTP 入口。
4. [x] 页面：扩展 requirements.ts、useRequirementConversation.ts、App.vue；恢复待选择状态、继续补充/采用候选/保留现状，旧卡片失效，失败保留同一请求编号。
5. [x] 验证：完整 pytest、mypy、改动文件 Ruff、前端测试和构建；备份并执行正式迁移，真实模型/数据库/浏览器暂停、重启和恢复；独立代码审查后同步 03/04/07/08/09，清理本步临时验收数据。

各步执行 `backend/.venv/Scripts/python.exe -m pytest`；数据库 URL 从 `.env` 加载到测试环境变量，不输出。验证状态以最终记录为准。


## 执行记录

已按当前任务直接完成，保留M4-B未提交改动。独立审查与实际模型接入暴露的问题均完成复现、修补和定向回归；最终818项全量通过。前端四组回归/构建、mypy及Ruff通过；真实暂停/两处重启恢复/修改/撤销/取消及私人附件样本记录在07，字段字典与恢复边界在08第12.17节。

实现取舍：恢复边界为完整候选/审查/提交，内部只读工具循环不独立持久化；原节点内查询中断会重做。不存在后台自动恢复执行器，重试或用户继续才推进。采用固定官方3.1.2依赖与0014结构配套；继续当前codex/m4-a工作区，未提交或推送。
