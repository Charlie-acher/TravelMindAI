# 百度地图MCP接入计划与完成记录

核对日期：2026-09-17。最终范围按用户要求收敛为所有地图查询统一百度MCP，移除高德配置/客户端和百度直连HTTP分支；地图仅配置后端BAIDU_MAP_API_KEY。保留账号归属、周边筛选、SSE、幂等与原子保存。

实现使用LangChain原生MCPAdapter、FastMCP及AnyIO BlockingPortal同步桥接；BaiduMaps按请求惰性连接和复用，不手写协议、参数结构或模型循环。固定官方远端，工具发现与只读白名单取交集，不开放分享地图、IP定位、未知工具或额外授权。初始化10秒、调用15/20秒，每请求24次外部调用，地图Agent最多8轮决策；同轮多只读调用由框架顺序执行，提交/补问/分流单独选择。

1. [x] MCP适配与地图业务：统一百度地点核对、BD-09坐标和受控周边筛选；旧高德/GCJ-02锚点用于新查询时重新按名称定位，原历史不改写。缺配置或连接失败如实记录，不回退高德。
2. [x] 聊天与持久化：Agent可查询天气、地点、路线和路况，并引用本轮成功工具编号提交回答；周边仍核对评分/消费，mcp可空快照存入既有JSON，无新表或迁移。幂等与版本先于连接，失败不留半轮。
3. [x] 前端与文档：三类卡片共用百度marker链接，显式声明bd09ll/gcj02；历史评分保留来源。README、07逐文件说明及08第10.25调用链、图与JSON字典同步。
4. [x] 验证：最终后端565 passed、2 warnings、84.05秒，含真实隔离PostgreSQL/Milvus；mypy78文件、修改范围Ruff、前端build、Node SSE契约及diff检查通过。真实AK天气/地点/周边及DeepSeek HTTP/SSE四轮接续成功，最终公交路线和附近100元内餐饮返回200；浏览器恢复两轮并确认百度评分及链接，QA数据清理回查0。详见08第10.25。

已知边界：引用编号校验不等于逐句事实验证；依据精简并去掉图片、坐标路径等大字段，不是完整原始报文。消费不等于实时房价，圆形半径不等于步行时间，单独路线查询不等于逐日行程完整路线校验。外部百度落地页未打开验收，不宣称底图已展示；私人附件、Reviewer及持久恢复未扩展。
