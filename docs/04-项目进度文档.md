# TravelMindAI 项目进度文档

> 文档版本：v0.31（补充五项建议与智能体接入顺序，尚未实施）
> 盘点时间：2026-09-15（Asia/Shanghai）
> 证据范围：`E:\TravelMindAI` 当前文件、静态检查和本次命令输出  
> 关联：[PRD](./01-项目需求文档.md) · [技术设计](./02-技术设计文档.md) · [模块实施](./03-功能模块实施文档.md) · [参考文档对照](./05-参考文档实施取舍.md)

## 1. 当前结论

**2026-09-15口语问法修复：**距离模糊先追问可接受的步行时长或交通方式，简短回复接续上一轮地点；模型可单独返回无需事实引用的偏好追问。门票/地址缺失或校验失败按字段处理，保留有依据主体，正文同步清除失败详情；页面同时显示卡片和追问。没有新增表或迁移，步行路线耗时尚未计算。源码合同和本步验收见08第10.21节。

**2026-09-15追加方案，尚未实施：**用户接受五项补充：附件参考/必经/替换用途，结构差异摘要及撤销，以地点记录核对切片质量，管理员批量两标签/失败重试，以及新版就绪后替换旧文件。详情见09第12节。智能体接入顺序调整为A～C基础 → M4-A文本行程智能体 → D私人附件理解 → E/M4-B附件行程 → M4-C独立审查与持久恢复；批量管理和文件替换不阻塞M4-A。现有源码仍是固定AI业务链，未接StateGraph/checkpointer，不能标记智能体已完成。本次仅同步01/02/03/04/07/08/09，未修改代码或执行迁移。

**2026-09-15新增目标方案，尚未实施：**按用户要求规划管理员知识库与私人对话附件分流、users及会话归属、左侧历史/新建对话、去掉用户旅行需求面板、管理员城市/类别两标签及城市分组，移除资料页顶部大块搜索区。用户确认管理员仅管理知识库文件，不读取私人附件。方案见[09](./09-双角色与对话附件改造方案.md)，目标字段和迁移阶段见08第12节。此次仅修改01/02/03/04/05/07/08并新增09；未改应用源码、未建用户表、未执行迁移或验证新界面。以下测试数字均为此前开发记录。

**2026-09-15地点修复：**新增区县识别、已核验地图别名和行政镇中心匹配，完整地址优先使用高德，缺失时定向补查有原文依据的地址。每轮至多为三个已选景点分别补查缺失门票/地址。卡片隐藏免费与未知门票栏，只保留收费或部分收费；未知仍不代表免费。397项后端测试、66文件mypy、变更目标Ruff、前端构建和数据库结构一致性检查通过。截图问题和本步验证见08第10.20节。数据库仍为0007；旧聊天保留当时快照，新问答使用新流程。

**2026-09-15追加完成：**网页批量上传后自动切片和建立索引，每份显示进度与重试；后台同时最多两份。新资料预填可确定城市/来源及待审核，已有资料可补全空标签，人工内容保留。真实浏览器三份资料各20/20段完成，同批无效文件独立拒绝；细节与检查见08第10.19节。数据库仍为0007，既有问答质量限制不变。最终373项后端测试通过、0跳过，66文件mypy、目标Ruff、前端构建与alembic check通过；三份临时验收资料已清理，原有攻略保留。

**2026-09-15收尾：**三项剩余工程工作已补齐：城市/来源/审核/POI标签保存与检索前置筛选；后台解析/索引、暂停、进程中断识别和显式重试；29段33题真实固定集评测。本机已迁移0006/0007且ORM无差异。页面验证标签保存/组合搜索、无匹配时不退回全库；服务中断重启后显示失败、从页面重试后240段全部完成。仅本轮两份测试资料已清理，原有攻略保留。

**此前评测结论：**当时选定第二版提示词Recall@5=100%、MRR@5=0.9464、无证据5/5、注入2/2；严格整题忠实度32/33，q26仍把需自带野餐垫误写为场地提供。第三次只增加条件提示未改善，已撤回并保留完整试验报告。本步改变地址和门票补查提示词，未重跑33题；32/33不作为本次版本的实测分数。工程闭环和召回基线达标，不代表所有问答或全量知识库通过。题库及逐题核对由Agent完成，非独立人类盲测。见[三轮对比](../backend/evals/reports/m3-comparison-20260915.md)和08第10.17/10.18。当前codex/m3，未提交或推送。

以下为此前小步历史记录，其中的待办只对应当时。

最终检查：367项测试通过、0跳过（含真实PostgreSQL与Milvus），65文件mypy、本轮目标Ruff和前端构建通过；全app Ruff仍有trips.py/budget_service.py共3条原有长行，不称全仓lint清洁。迁移0007及ORM一致性再次核对通过。

**2026-09-15本步完成批量上传与查找：**资料页支持一次多选、逐份上传、逐项结果、暂停后续文件和未确认上传的单份重试；文件名关键词由数据库筛选后分页。351项后端测试、59文件mypy、目标Ruff和前端构建通过；真实浏览器覆盖损坏/重复/空文件、暂停与服务中断后重试。测试档案已清理，没有修改两套来源目录，也没有新增README。使用和景点正文写法见07顶部，当前契约见08第10.16节。

**M3仍在收尾：**来源/POI元数据过滤、后台处理中断恢复及固定题库的召回和事实忠实度评测尚未完成；文件名筛选不改变聊天检索范围，也不是来源权重。分支codex/m3，未提交或推送。下方为此前小步记录。

**2026-09-15本步完成重新解析：**失败资料可在原页面重读原文件，保留编号；文件缺失/损坏/被替换均不伪装成功。350项测试、59文件mypy、目标Ruff和前端构建通过。runtime只保留chinatravel-sandbox和lvbangpt两个目录，来源清单及内容哈希已核对，没有新增README。清理文件已移出项目，但自动审批阻止永久删除，临时隔离位置见07顶部。

LvBanGPT杭州攻略已实际导入并完成46/46段索引，限定资料查询返回5段带页码正文，页面可见；主参考仍为ChinaTravel-Sandbox。来源优先级尚未成为程序检索权重，不能把目录主次说成自动加权已实现。M3仍待后台任务恢复、来源/POI元数据过滤及固定题库的召回与事实忠实度评测。分支codex/m3，未提交或推送。

以下为此前小步的历史记录。

**2026-09-15资料删除小步完成：**新增DELETE接口和前端确认/取消/重试，资料先进入deleting并退出新检索，再清理所有模型版本中的对应向量、磁盘原文件及档案片段。清理中断保留待清理记录，刷新或重启后仍可重试；历史聊天快照不改写。本机已升级0005且ORM无差异；344项后端测试通过、0跳过，59文件mypy、目标Ruff和前端构建通过。真实Milvus跨版本隔离删除、页面文件占用失败/刷新/重试均已验收，只操作本步测试资料。详见08第10.14节，逐文件顺序见07。

**M3仍在进行中：**本步完成的是删除及删除重试；失败文件重新解析、后台处理中断恢复、来源/POI元数据与过滤、固定30题的Recall@5/MRR/延迟及事实忠实度评测仍需继续。内嵌地图、每日行程不是本次交付能力。分支codex/m3，未提交或推送。

以下为此前检索和问答小步的记录，其中当时的待办不代表最新状态。

**2026-09-15检索修正：**新地点提问不再混入旧话题，住宿提问不再追加景点门票搜索词；缺入住条件的住宿价格问题先补问，不让模型推断预算档次。RAG输出校验失败最多修正一次，仍失败则拒绝保存。334项自动测试通过，真实住宿补问已在原页面验证。更完整的贵州卡片验收与边界见08第10.13节。

**2026-09-15：M3景点卡片与外部证据已接入原聊天。** 知识库优先，Tavily补充网页证据，DeepSeek输出自然介绍和结构化景点；每个推荐景点进入高德定位流程，门票必须保留依据。卡片包含介绍、推荐原因、门票参考和位置状态，随整轮快照保存、刷新恢复。最新按用户要求：有景点卡片时只展示卡片，隐藏重复文字回答；没有卡片的普通对话保留文字。没有新增问答按钮、依赖或数据库迁移。

当前搜索和高德配置均已完成真实调用；苏堤返回真实POI编号和坐标，湖滨路步行街仍有别名匹配限制。最新票价尚未通过实时票务服务验收；高德复验详见08第10.12节。真实页面已展示两处杭州景点并刷新恢复；也记录过一次模型格式或依据校验失败，失败轮未保存，输出稳定性仍需评测。完整验证、限制与JSON字段见[08第10.11节](./08-架构设计说明.md)，开发顺序见[07](./07-各阶段开发说明.md)。M3仍有召回质量、事实忠实度和资料生命周期等待办；内嵌地图与每日行程尚未实现。当前分支codex/m3，未提交或推送。

以下为此前各小步的历史记录。

**2026-09-14 M3第五小步：原聊天入口默认RAG与后台证据保存。** 每轮先检索知识库，再让DeepSeek依据命中原文回答，引用由后端校验。资料不足明确说明，服务故障不降级为自由回答。资料页没有额外问答按钮；聊天按用户要求仅展示自然回答，不展示引用或来源套话；后台保留证据校验，回答与原文快照随现有JSON整轮保存、刷新恢复。需求收集仍由程序规则维护。

此前主聊天基础验收299项通过。本次增加景点介绍、默认门票说明及按需高德补查；补充杭州3处景点内容，已上传索引7段，累计2451段。用户未配置地图Key，真实高德查询待验收；人均消费不能当门票。最终312项测试通过、0跳过，45文件mypy、目标Ruff与前端构建通过。真实DeepSeek回答及地图未配置快照保存/刷新已验证；最新验证见08第10.9节。浏览器与真实模型验收见[08第10.8节](./08-架构设计说明.md)，逐文件顺序见[07](./07-各阶段开发说明.md)。没有新增依赖或迁移。当前分支`codex/m3`，未提交或推送。

以下为第四小步及更早历史验收。

**2026-09-14 M3第四小步：Milvus索引、续建和前端原文搜索完成。** 当前分支`codex/m3`，未提交或推送。本机Milvus 2.6.17已运行；杭州景点760段、餐馆922段、住宿762段共2444段已实际生成1024维向量并持久化。容器重启后数量与搜索均恢复。前端可分批建立索引、暂停/刷新续建及跨资料语义搜索，展示原文和出处；上传仍不自动调用模型。

全量283项测试通过、0跳过，含真实PostgreSQL临时schema和真实Milvus测试集合，1条既有依赖弃用提示；变更Ruff、41文件mypy和前端构建通过。浏览器验证首批暂停8段、切回聊天后停在16段、手动续建至760段和刷新搜索；重复正文挤占候选回归修复。只调用Embedding，尚未让DeepSeek根据资料生成答案。没有新增PostgreSQL迁移，向量字段字典与验收详见[08第7.8及10.7节](./08-架构设计说明.md)，逐文件顺序见[07](./07-各阶段开发说明.md)。

以下为第三小步及更早的历史验收。

**2026-09-14 M3第三小步：Embedding独立配置、客户端和试跑入口完成。** 当前分支`codex/m3`，未提交或推送。用户已上传杭州主资料并生成2444段；新增命令可只读预览少量片段，或在独立配置齐全后调用兼容向量服务，校验模型、数量、序号、维度与数字。用户已补充阿里云配置，text-embedding-v4真实短文本和3段资料调用均通过，返回1024维向量。

全量276项测试通过、0跳过，含真实PostgreSQL临时schema，1条既有依赖弃用提示；变更目标Ruff与38文件mypy通过。此前本机3段资料只读预览成功，无密钥请求模式正确拒绝；补齐配置后又完成2次真实调用。此次仅补版本标签和验收文档，应用源码未改，未重跑全量测试。没有新增依赖、表、迁移、Web接口或前端行为；真实Embedding已小批量接通，向量未写入Milvus。当前契约与验收详见[08第10.6节](./08-架构设计说明.md)，逐文件顺序见[07](./07-各阶段开发说明.md)。

以下为第二小步及更早的历史验收。

**2026-09-14 M3第二小步：正文切分、片段保存与前端预览完成。** 当前分支`codex/m3`。已有资料可按需生成片段，最多800个Unicode字符、同单元相邻长片段重叠100字符，保留页码/标题/字符位置；重复生成和刷新沿用同一批编号。增加片段表及GET/POST接口，Vue可切换原文和片段并分页。包含重复标题的切分结果限制200万字符，超限保留原资料并明确提示。

本地`0004_document_chunks (head)`迁移已应用，ORM比对无差异。最终全量248项通过、0跳过，1条既有依赖弃用提示；变更Python目标Ruff、36文件mypy和前端构建通过。浏览器验证已有PDF页码、长Markdown的55段翻页、刷新恢复、原文切换及失败资料无生成入口。逐文件顺序见[07](./07-各阶段开发说明.md)，字典和契约见[08](./08-架构设计说明.md)。本步未提交或推送。

以下为第一小步历史验收。

**2026-09-14 M3第一小步：资料上传、读取和现有前端接入完成。** 当前分支`codex/m3`，支持PDF/DOCX/TXT/Markdown，保存原文件与读取结果，提供列表、正文预览、重复内容去重和失败原因。`0003_documents`已应用于本地PostgreSQL。全量240项测试通过、0跳过，1条既有依赖弃用提示；变更Python目标Ruff、mypy及前端构建通过。浏览器已验证上传、页码/标题/表格、去重、刷新读取、失败原因及页签保留输入。逐文件顺序见[07](./07-各阶段开发说明.md)，数据库字典和调用链见[08](./08-架构设计说明.md)。

M3仍未完成：来源与POI元数据、召回与引用含义评测，以及后台处理/删除等完整生命周期。当前Web仍是本地单用户资料库，上传不会自动调用模型；以下M2记录属于历史验收。

**2026-09-11 M2第六小步：首次意图校正完成，M2按当前完成标准验收。** 无previous的modify_trip在服务端校正为plan_trip；无档案的删除/清空先拦截，已有需求的修改与非规划意图保持原行为。模型提取、程序校验/合并、集中追问、对话保存和Vue刷新续聊链路已具备。

全量216项测试通过、0跳过，含真实PostgreSQL；1条既有依赖弃用提示。变更文件Ruff及38文件mypy通过。题库及提示词不变，真实DeepSeek评测30/30组、36/36轮通过，36次调用、0次修复。四项字段132/132、日期12/12、最终业务意图36/36；成绩包含程序校正，不能称为原始模型分类准确率或独立盲测结果。报告见[本次成绩单](../backend/evals/reports/m2-intent-fix-20260911.md)。

当前M2不含交通时间建模和每日行程生成。M3资料上传读取、向量检索和主聊天RAG已于2026-09-14进入正式工程；继续维持前端联通和逐文件中文说明。以下第五小步及更早内容是历史验收记录。

**2026-09-11 M2第五小步：首轮日期补算与超范围提示已修复。** 10月1日开始三天按包含首日算到10月3日；首轮开始日加天数复用已有日期协调。提示词明确超范围日期不能继续组成非法跨度，HTTP回复给出当前支持范围并保留已有需求。早上9点返程仍属于旅行的最后一个自然日；具体可游玩时段和交通安排留给后续行程功能，当前未新增交通时间字段。

验证：214项测试通过、0跳过，包含真实PostgreSQL；1条既有依赖弃用提示。变更文件Ruff及38文件mypy通过。相同题库真实评测29/30组、35/36轮通过，36次调用、0次修复，日期12/12；原M2-16及M2-22均通过。本次M2-20出现plan_trip/modify_trip意图误判，其他字段正确，保留失败报告，M2继续进行中。报告见[日期修复成绩单](../backend/evals/reports/m2-date-fix-20260911.md)。前端通过原接口获取日期和支持范围回复，未新增数据库表或改动页面源码。

以下保留此前各小步的验收记录，其中当时的待办不代表当前状态。

**2026-09-11 M2第四小步：30组36轮固定样例评测已完成。** 新增人工标注题库、评分工具、显式真实调用命令和8项评分/CLI测试。脚本复用网页的extract_requirements及合并校验，不写会话库、不新增HTTP或前端状态。

真实DeepSeek结果：28/30组、34/36轮通过；35/36轮有效返回，38次请求，2轮修复。四项必要字段（目的地/天数/人数/预算）132/132，其中已提供值115/115、应未知值17/17；单独日期字段11/12，意图35/36。时间2026-09-11 06:24:55至06:26:10 UTC，题库和提示词哈希保存在报告中。这是人工开发集，不是独立盲测准确率。

**M2继续标记进行中。** 已定位两项差距：首次“明天出发三天”未推导结束日；6天超范围输入仍返回通用校验失败，没有正常反馈不支持。四项字段达到90%不掩盖这两项失败；下一小步修复后用同题库生成新报告，不修改或覆盖本次基线。

本小步验证：全量205项测试通过、0跳过、1条既有依赖弃用提示；包含真实PostgreSQL临时schema回归。新增8项评分与CLI测试，变更文件Ruff通过，mypy共38文件通过，compileall通过。前端仍连接本地服务，真实发送银川到杭州3天2人5000元并保存成功。

报告：[基线成绩单](../backend/evals/reports/m2-baseline-20260911.md) · [逐字段JSON](../backend/evals/reports/m2-baseline-20260911.json)。开发顺序和命令见[07](07-各阶段开发说明.md)，离线评测与在线路径关系见[08](08-架构设计说明.md)。

评测后按用户反馈改进错误提示：无previous时删除/清空内容直接提醒尚无旅行档案、引导先说目的地；已有需求失败时明确原需求已保留。增加3项HTTP回归测试（无档案、已有档案、空删除列表），不更改数据库或前端契约。原30组评测报告保留为此次文案调整前的基线。文案与空删除边界修改后的最终全量208项测试通过（含真实PostgreSQL），Ruff与38文件mypy通过；后端已重启，网页已实测新提醒。

以下保留前几小步的验收记录。

**2026-09-11 M2第三小步：对话保存、刷新恢复与前后端联通。** 新增requirement_turns表及0002迁移、事务存储服务、会话对话GET/POST接口。Vue使用sessionStorage保存会话编号和待确认发送，服务器读取旧需求，支持刷新/后端重启后续聊。版本检查拒绝旧页面覆盖；已保存消息重试不重复入库、不重新调用模型。模型失败不落库，存储故障整轮回滚。

真实前端验证：银川→杭州3天2人5000元，刷新后保留；续聊改为3人；后端断开时保留卡片并禁止发送，重启后重新读取恢复；新会话只说苏州，旧人数/预算/出发地不带入。页面明示2～5天、1～8人范围。本次没有修改真实.env或扩大业务范围。

最终验收：全量197项测试通过、0跳过、1条既有依赖弃用提示，其中包含真实PostgreSQL临时schema测试。本次新增11项测试；变更Python文件Ruff通过，mypy检查36个源码/脚本/迁移文件通过，compileall及git diff --check通过，Vue类型检查与生产构建通过。正式本地数据库已升级到0002_requirement_turns，alembic check无差异。

完整逐文件顺序、双配置文件启动命令和验收步骤见[各阶段开发说明](07-各阶段开发说明.md)。当时M2仍待30条标注集及准确率评测；登录、历史会话列表、跨设备找回和行程生成不在本小步。

以下为各次历史验收记录，其当时未实现项目不代表当前状态。

**2026-09-10 M2第二小步：多轮需求合并与Vue对话验收已实现。** 前端采用Vue 3、TypeScript、Vite和Arco Design Vue，在M2提前提供对话页；后端新增`GET /api/v1/requirements/status`和`POST /api/v1/requirements/messages`。当前运行入口、逐文件说明统一见 [各阶段开发说明](./07-各阶段开发说明.md)。

支持未提及保留、明确覆盖、列表去重、指定条目删除、明确清空、日期/天数联动；合并后再次校验，输出和合并错误共享一次修复额度。页面展示需求、集中追问、本轮变化和推导依据。previous由当前页面传回，各页面独立，刷新不恢复；本步没有把聊天消息写入数据库。

当前全量后端测试152通过、34数据库项跳过、1条既有依赖弃用提示；本步新增28项测试。Vue类型检查与生产构建通过，浏览器真实四轮对话、断连保留/重试、重置隔离及回车操作已验证。单模型访问沿用工作区同期接入的LangChain ChatOpenAI。M2仍缺消息持久化和30条数据集/准确率验收；M6继续负责完整前端工作台，不重复创建Vue工程。以下为各阶段历史记录。

2026-09-10增量：按用户要求将`llm/client.py`改为LangChain的`ChatOpenAI`，通过显式DeepSeek地址调用；保留`generate_json`业务接口、中文异常、网络超时、JSON模式和服务端一次修复。移除手写HTTP请求及choices解析；当前没有扩展多模型路由。依赖锁定langchain-openai 1.6.2、openai 2.54.0，未保留langchain-deepseek。全量124通过、34数据库测试跳过；真实DeepSeek复核完整需求和缺项两条输入。详细写法见下方逐文件讲解。下面2026-09-09数据属于此前验收记录。

**2026-09-09 M2第一小步已实现：单条自然语言 → DeepSeek → Pydantic校验 → 结构化旅行需求与集中追问。** 入口为 `backend/scripts/demo_requirement.py`，详细开发顺序、字段解释、命令与边界见 [M2逐文件讲解](./07-各阶段开发说明.md)。

本次新增需求schema、集中提示词、抽取服务、单模型HTTP客户端和命令行入口；保留原消息与参考日期，未知预算不填默认值，首尾日期可推导天数，输出无效时最多修复一次。配置兼容显式文件中的DS_API_KEY，httpx提升为正式依赖；没有修改真实.env、数据库表或现有HTTP接口。

验收：新增52项离线测试，全量123通过、34项数据库测试因未配置测试地址跳过。变更Python文件Ruff通过，mypy共27文件通过，compileall通过，uv按锁文件同步成功。全量Ruff仍有原来3处长行，pytest仍有2条原有依赖弃用提示。真实deepseek-v4-pro完成4个样例检查：完整需求、缺项、相对日期/人均预算、要求编造字段的输入。4个样例不能代表必要字段准确率达到90%。

**M0、M1保持已完成，M2进行中。** 本小步不含多轮需求合并、消息HTTP接口/入库、模型就绪状态集成、30条标注集与准确率验收；其他意图仅识别，不执行对应流程。下文M1描述为此前验收记录。

2026-09-09 目录重构：正式包迁到 `backend/app/`，API 直接放在 `app/api/`，不保留版本号目录；业务代码集中到 `services/`，DTO 拆到 `schemas/`，ORM 位于 `models/trip.py`，独立命令放在 `scripts/`。TripService 替代原存储类名。下文历史路径已按新位置映射，完整目录见 [分层约定](06-项目分层与审阅.md)。

迁移前后同一套105项测试通过；新布局共21个应用、脚本及迁移文件通过 mypy；Alembic 仍为 `0001_business_tables (head)`，检查没有结构变化。数据库、HTTP URL、请求响应、依赖锁定和预算规则保持一致。 默认不配置测试数据库时71通过、34跳过；临时测试 schema 已全部清理。新旧服务的 OpenAPI 文档完全一致，新入口可重读原有草稿并返回2442元预算。预算计算和 ORM 模型的 AST 与迁移前一致，wheel 打包包含 app/scripts，学习脚本及 uv.lock 未变。仍保留迁移前已有的3处长行提示和2条依赖弃用提示。

**此前M1结论：预算计算、PostgreSQL迁移与事务存储、会话/草稿HTTP接口及数据库就绪检查已验收。当前M2状态见本节开头。**

本小步（2026-09-09）：新增 `schemas/trip.py`、`api/trips.py` 和 `tests/test_trips_api.py`，共享真实数据库测试夹具至 `tests/conftest.py`，扩展应用生命周期与导入安全测试。HTTP 支持创建/读取会话、保存预算草稿、读取最新/指定版本。预算由服务端计算，客户端不能直接提交金额结果、状态或版本号；目前逐日活动为 `days: []`，尚未自动规划行程。

验收：105 项测试通过，其中34项使用真实 PostgreSQL；未配置测试数据库时71通过、34跳过。mypy 检查18个源码及迁移文件通过。全量 Ruff 仅剩预算文件第106行既有 E501，测试有两条既有依赖弃用提示。函数说明继续使用 def/装饰器上方的三引号字符串，说明与函数之间统一空一行。

实际 HTTP 创建了名为“M1 HTTP 验收”的示例会话：`e11bb90b-dcbd-4d4f-8bbc-4e71533d3159`，保存并重读第1版草稿成功；两人三天经济档合计2442元，预算5000元时余额2558元。该示例保留在正式本地库，自动化数据库测试使用独立临时 schema。

应用通过 lifespan 管理连接池，退出时释放连接。不配置数据库时预算接口仍可使用，存储接口返回503，就绪检查标记数据库为 disabled；配置数据库后，就绪检查会验证业务表及列，数据库不可用或缺迁移时返回503。暂未提供归属鉴权、消息保存、完整旅行需求、行程确认和导出。下文保留此前各小步的历史验收记录。

上一小步（2026-09-09）：新增 `services/trip_service.py`、`../scripts/demo_trip.py` 和 `tests/test_trip_service.py`，扩展导入安全测试。提供创建/读取会话、原子保存需求及行程快照、最新/历史版本读取；同会话使用行锁串行分配版本，数据写入和会话修改时间一起提交/回滚。返回对象脱离 ORM Session，不依赖活动连接。示例复用预算模块，并支持另一进程通过 session_id 只读恢复。

当时90项测试通过（原79项+11项存储测试），其中24项真实 PostgreSQL 测试；默认无测试数据库地址时66通过、24跳过。验证跨连接/跨进程重读、会话隔离、历史版本保留、失败回滚、不占用失败版本、并发版本唯一及输入快照隔离。新增文件 Ruff 与格式检查通过，源码及迁移共16个文件通过 mypy，compileall通过；`alembic check` 无结构差异。全量 Ruff 仍有预算文件第105行既有 E501，测试仍有两项既有依赖弃用提示。测试只写临时schema并清理，正式业务表没有新增演示数据。

当时范围：尚未增加HTTP存储路由、归属鉴权、完整旅行领域校验或自动生成行程；`/health/ready` 仍不检查数据库。每次创建会话是独立事务，每次保存草稿则原子保存两份快照；没有把演示脚本的全部操作声明为一个事务。下文保留此前各小步的验收记录。

2026-09-09 增量：新增 `models/trip.py`、`migrations/env.py`、`migrations/versions/0001_business_tables.py`、迁移模板和 `test_schema.py`。Alembic 1.19.2 配置放在 `pyproject.toml`，解决 Windows INI 本地编码读取中文注释的问题。正式库由空表状态升级到 `0001_business_tables`，包含 `sessions`、`travel_requests`、`itineraries` 和 `alembic_version`；未写业务示例数据。

表结构小步验收：当时79项测试通过，其中13项使用真实 PostgreSQL 临时 schema，覆盖首次升级、重复升级、回滚后再升级、ORM读写、金额字符串、版本/状态/JSON/外键约束及跨会话引用拒绝。`alembic current` 为 head，`alembic check` 无结构差异；临时目录中验证了迁移模板生成的 Python 文件可解析。

表结构小步也将正式源码和测试中42处函数说明移到函数/装饰器上方，保持三引号字符串形式；没有剩余函数体内 docstring。类和模块说明、行内 `#` 注释保留。此格式不再提供函数 `__doc__`。预算与健康接口逻辑不变，`/health/ready` 仍不探测数据库。后续实现会话及草稿的事务化保存/读取，再完成 M1。

上一小步（2026-09-08）：用户已创建 PostgreSQL 17 容器 `travelmind-postgres`，本机端口 `127.0.0.1:5432`；Python 通过 psycopg 实际执行只读 SQL，确认数据库和用户均为 `travelmind`。新增 `database.py`、包声明、数据库测试与 `backend/.env.example`，扩展 Settings；本机 `backend/.env` 已配置且被 Git 忽略，未改根目录 `.env`。

上一小步共66项测试通过（M1-A原55项、配置新增2项、连接模块新增9项），当时只验收连接基础。SQLAlchemy 锁定2.0.52、psycopg/psycopg-binary锁定3.3.5；本次增加 Alembic，不升级全局学习环境。

下面 M1-A 的55项计数为前阶段记录；当前操作命令和文件阅读顺序见 [README](../README.md)。

开发者已经了解 Python、LangChain、LangGraph，具备 Java/Vue 前后端分离开发经验。接下来直接按工程里程碑交付，不新增基础学习模块。

当前 9 个实验脚本已由用户整理到 `old_learn/`，覆盖模型调用、Prompt、结构化输出、工具调用、Redis 历史、LangGraph 和文本切分；本次不修改或运行它们。此前的 AST 语法检查仅证明语法，不证明远程服务接通。

M1-A 已增加预算领域函数、请求/响应 DTO、FastAPI 路由、应用工厂、错误处理和健康检查，当时55个测试通过，实际 HTTP 与 Swagger 提交验证成功。此后 M1-B 连接基础将测试扩展为66项，当前完整 M1 为105项。手写文件提供详细中文注释，每开发2–3个文件说明作用、顺序和验证结果。具体阅读顺序见 [README](../README.md)。

## 2. 当前工作区快照

### 2.1 文件证据

下表中的实验脚本均位于 `old_learn/`。

| 文件/目录 | 本次静态观察 | 尚未证明 |
|---|---|---|
| `LCEL.py` | Prompt、模型、输出解析器组成调用链 | 产品服务封装与异常处理 |
| `model_io.py` | 同步/异步/流式模型调用练习 | 多模型网关与正式 SSE 服务 |
| `prompt_templates.py` | PromptTemplate / ChatPromptTemplate 实验 | 旅行领域提示与抽取评测 |
| `StructuredOutput_TypedDict.py` | TypedDict/Pydantic 结构化输出示例 | TravelRequest schema 与业务规则 |
| `ToolCalling.py` | bind_tools、天气函数和工具调用链 | 当前真实天气可用、完整错误与日期边界 |
| `AgentSmartSelect.py` | create_agent 与天气工具调用示例 | 旅行编排、可恢复会话及端到端验收 |
| `RedisChatMemory.py` | RedisChatMessageHistory 与 RunnableWithMessageHistory | 当前 Redis 连接成功、持久化策略与图状态恢复 |
| `LangGraphHello.py` | 单节点 StateGraph，节点返回消息更新字典 | checkpointer、条件路由、interrupt、多 Agent 协作 |
| `RecursiveTextSplitter.py` | RecursiveCharacterTextSplitter 切分实验 | PDF/DOCX 解析、embedding、Milvus 索引和 RAG |
| `.env` | 文件存在，本次未读取内容 | 凭据有效或任何提供方可用 |
| `AGENTS.md` | 文件为空；执行遵循会话提供的指令 | 仓库已持久化同一规则 |
| `backend/app/` | 预算/存储、单模型抽取、多轮合并、集中追问、对话HTTP | 模型规划、归属鉴权 |
| `backend/tests/` | 含多轮合并、对话持久化、事务/迁移及HTTP测试；最终计数见当前结论 | 独立盲测及更大规模真实用户评测 |
| `frontend/` | Vue 3 + Arco简易对话页，真实API、需求卡片、变化高亮、错误提示与重置 | 历史会话列表、行程工作台和完整E2E套件 |
| `backend/migrations/` | 首次建表、临时schema回滚/再升级、模板验收 | 后续表结构版本迁移 |
| `backend/pyproject.toml`、`uv.lock` | 最小依赖和工具配置，独立环境重装通过 | 全部后续依赖兼容 |
| `README.md` | 安装、预算服务启动、Swagger提交、计价规则与逐批文件说明 | 完整旅行产品可用 |
| `docs/` | 进度更新为M2第三小步，统一维护07逐文件讲解 | 完整M2–M7已完成 |

### 2.2 工具环境快照

下表中 LangChain/LangGraph/FastAPI 等为本轮开始时的全局包快照，不是正式后端的运行依赖。M0 独立环境的精确依赖以 `backend/uv.lock` 为准。

| 项目 | 本次观察 |
|---|---|
| Python | `E:\Python 3.11.2\python.exe`，3.11.2 |
| LangChain | 1.3.14 |
| LangGraph | 1.2.11 |
| langchain-core | 1.5.3 |
| FastAPI | 0.136.3；安装了包但项目没有应用入口 |
| Pydantic | 2.13.4 |
| langchain-redis | 0.2.5 |
| uv | 已通过 pip 用户级安装 0.12.10，使用 `python -m uv` |
| Git | 已初始化本地 `m0-engineering` 分支，无提交、无远程地址 |

M0 项目环境：`backend/.venv/Scripts/python.exe`，Python 3.11.2；pydantic-settings 2.15.0、Pydantic 2.13.5、pytest 9.1.1、ruff 0.16.6、mypy 1.20.2。未把全局 LangChain、LangGraph 或 FastAPI 安装到 M0 环境。

M1-A 在项目环境增量安装 FastAPI 0.141.1、Uvicorn 0.52.4、httpx 0.28.1，及 Starlette 1.6.0 等依赖；`uv.lock` 记录30个包（含本地项目）。未升级原有全局学习环境中的包。

### 2.3 当前缺失的产品产物

- 完整前端行程工作台与SSE（Vue对话页与资料页已实现）；
- 历史会话列表与跨设备找回（当前标签页刷新恢复已实现）；
- 归属鉴权、行程确认与导出（会话/草稿HTTP、对话保存及版本存储已具备）；
- 带状态恢复、条件边、返工和人工确认的旅行工作流；
- 全量库检索质量、事实忠实度与来源权重（解析、向量化、Milvus及可追溯RAG已实现）；
- 四提供方统一适配器、路由、熔断与降级；
- 全应用部署与综合验收（现有Vue、自动测试、M2/M3评测与Milvus Compose已落地）。

根目录的 `__pycache__`、`.pytest_cache` 等缓存不能作为当前产品测试通过的证据。

## 3. 里程碑状态

| 里程碑 | 状态 | 当前证据 | 下一验收 |
|---|---|---|---|
| 文档基线 | 已更新 v0.2 | 五份文档，含飞书对照及工程决策 | 后续随实现维护 |
| M0 工程基线 | **已完成** | 独立重装、6 测试、ruff/mypy/compileall、Git 忽略和新增文件凭据模式检查通过 | 进入 M1-A |
| M1 FastAPI + 预算 | **已完成** | 105测试、预算/会话/草稿HTTP、PostgreSQL迁移/事务/历史版本、数据库就绪检查 | 进入 M2 单模型需求抽取 |
| M2 单模型需求抽取 | **已完成（当前范围）** | 216测试、抽取/修复/追问、持久化与Vue续聊，30组36轮开发集全通过 | 进入M3；后续持续增加实际失败样例，非独立盲测结论 |
| M3 RAG | **工程闭环与召回基线达标** | 生命周期、元数据、后台恢复、真实33题Recall@5=100%，全文忠实32/33 | 已知条件丢失、复杂问题及独立标注，不外推全库质量 |
| M4 LangGraph / 记忆 | 局部实验 | 单节点图、create_agent、Redis 历史示例 | 旅行图、返工、interrupt、checkpoint 恢复 |
| M5 四模型网关 | 未开始 | 单模型调用实验不能计为网关 | 适配器契约、真实 smoke、故障降级 |
| M6 Vue 全栈 | 对话/资料页已提前落地 | 需求卡片、资料生命周期、后台状态及真实浏览器验证 | 完整工作台、SSE与自动浏览器回归 |
| M7 质量与部署 | 部分基础已具备 | M2/M3评测和本地Milvus Compose | 六个PRD场景、全应用部署、新环境启动 |

M0–M2按此前范围完成，M3工程闭环和召回基线已交付，但仍有明确问答质量限制；M4–M7不因此算作完成。学习脚本不计为正式工程验收。

## 4. 本次变更与验证

### 2026-09-08：M1-A 预算计算与 API

开发顺序：领域测试→领域计算→API依赖与测试→DTO→路由→应用装配→实际HTTP与Swagger→文档。每批2–3个文件说明用途；源码包含中文注释与Java概念对应。

新增：`services/__init__.py`、`services/budget_service.py`、`schemas/budget.py`、`api/__init__.py`（原版本子包已在目录重构时移除）、`api/budget.py`、`main.py`、`tests/test_budget.py`、`tests/test_budget_api.py`。更新依赖与锁文件、导入安全测试、README、实施和进度文档。

预算输入2–5天、1–8人、正数人民币预算（最多两位小数）和economy/comfort住宿档位。默认每间2人、向上取整房间数、晚数=天数−1。日期可不填，填写则要求首尾日包含在days中。所有价格是 `demo-cny-v1` 固定演示假设；2人3天经济档分类小计2220、预留金222、合计2442元，预算5000时余额2558元。

已执行并通过：

- 测试先行：领域测试先因缺少领域包失败，API测试先因缺少main入口失败；实现后55测试通过；
- `python -m uv run --locked ruff check .`：无错误；
- `python -m uv run --locked ruff format --check .`：13个Python文件格式正确；
- `python -m uv run --locked mypy app scripts`：9个源文件无错误；
- `python -m uv run --locked python -m compileall -q app scripts`：通过；
- `python -m uv run --locked python -X utf8 -m pytest -q`：55 passed，2条依赖弃用提示；
- 实际启动 `uvicorn app.main:create_app --factory --host 127.0.0.1 --port 8000`；
- HTTP检查：live/ready/docs/openapi均200；预算返回2442.00/2558.00，请求头与体编号一致；days=1返回422；
- 浏览器Swagger实际执行默认JSON，Server response显示200与正确预算；
- `old_learn/` 文件按本轮开始时的SHA-256快照检查，无内容变化。

两条提示来自 Starlette 对 httpx 的弃用，以及 AnyIO BlockingPortal 旧别名；当前测试可用，没有过滤警告或以此推断未来版本兼容。后续升级测试依赖时单独处理。

边界：只实现预算与健康接口，不调用模型/天气，不保存会话或预算结果，不引入数据库。校验/领域/HTTP错误已统一；未捕获的意外程序错误仍使用框架默认500行为。Swagger使用默认CDN资源，离线环境的页面可用性尚未验收。当前分支仍无提交、无远程地址。

### 2026-09-08：M0 编码与验收

开发顺序：配置与依赖（3 文件）→ 测试（2 文件）→ 正式包与配置实现（2 文件）→ README/实施/进度（3 文件）。`uv.lock` 为工具自动生成，不手工修改。

新增工程文件：`.gitignore`、`.env.example`、`backend/pyproject.toml`、`backend/uv.lock`、`backend/app/__init__.py`、`backend/app/config.py`、`backend/tests/test_config.py`、`backend/tests/test_import_safety.py`、`README.md`。

测试先行记录：实现前配置测试因缺少 travelmind 包收集失败，独立进程导入测试失败；创建包和配置实现后，6 个测试全部通过。

在 `E:\TravelMindAI\backend` 执行：

| 命令/检查 | 结果 |
|---|---|
| `python -m uv sync --locked --link-mode copy` | 按锁文件安装成功 |
| `python -m uv run --locked python -X utf8 -m pytest` | 6 passed |
| `python -m uv run --locked ruff check .` | 无错误 |
| `python -m uv run --locked ruff format --check .` | 4 个 Python 文件格式符合要求 |
| `python -m uv run --locked mypy app scripts` | 2 个源文件无类型错误 |
| `python -m uv run --locked python -m compileall -q app scripts` | 语法编译通过 |
| 独立临时目录安装 + pytest + load_settings | 重新创建环境，6 passed，默认配置正确 |
| Git 忽略检查 | `.env`、.venv、上传目录被忽略，`.env.example` 可跟踪 |
| 新增手写工程文件常见凭据模式扫描 | 未发现匹配；非完整安全审计 |
| 原有 9 个脚本 SHA-256 比较 | 内容均未变化 |

独立重装目录为 `C:\Users\admin\AppData\Local\Temp\travelmind-m0-evpm67ah\backend`。只复制源码、测试、pyproject 和锁文件，未复制 `.env` 与虚拟环境；使用同一机器的基础 Python 和依赖缓存，不能等同于另一台机器验收。

修正过的验证问题：起初从根目录运行检查扫到了旧实验脚本，已改为 backend 工作目录；mypy 未识别 BaseSettings 的 `_env_file`，根据已安装插件实现和官方文档启用 `pydantic.mypy` 后通过。没有以关闭类型检查或修改旧实验代码来规避错误。

无模型、天气、Redis、数据库请求。当前没有提交任何文件；首次提交前仍需单独审查旧实验脚本与用户材料，不能把新增文件的凭据模式检查当作整个目录的安全保证。

### 2026-09-08：对照飞书更新项目文档

以下为编码开始前的文档阶段记录；其中环境缺失描述仅对应当时快照。

变更文件：

- `docs/01-PRD.md`：补修订依据、景点筛选、本地推荐、路线调整及第六个验收场景；
- `docs/02-TECHNICAL-DESIGN.md`：补选型取舍、预算端点、Embedding 独立配置、工具有效性、生命周期、POI 与行程数据边界；
- `docs/03-MODULE-IMPLEMENTATION.md`：直接按产品工程推进，细化 M0、M1-A/M1-B，补中断索引处理与路线测试；
- `docs/04-PROGRESS.md`：以当前 E 盘、9 个脚本和真实命令结果替换过时快照；
- `docs/05-REFERENCE-ALIGNMENT.md`：新增章节覆盖、技术清单差异和样例代码缺口。

本次通过 `ast.parse` 逐个解析根目录 `.py` 源码，输出 9 个 `SYNTAX_OK`，退出码 0。仅解析源码，没有导入执行，没有网络调用；不等同于运行测试通过。可复核命令：

```powershell
Set-Location E:\TravelMindAI
python -c "import ast,pathlib; files=sorted(pathlib.Path('.').glob('*.py')); [ast.parse(p.read_text(encoding='utf-8-sig'),filename=str(p)) for p in files]; print('SYNTAX_OK',len(files))"
```

同时核对了 `git status --short`、文件清单、`Get-Command python,uv,git` 与包元数据：目录不是 Git 仓库；uv 不在 PATH；解释器和版本见 §2.2。文档编辑完成后检查五份文件的本地链接、代码围栏及 Python 文件 SHA-256，确认无断链、无未闭合代码围栏，9 个脚本内容未变化。

文档阶段未运行 pytest、ruff、mypy；后续 M0 编码已运行并验证，结果见本节前部。后端 Web 启动、前端构建、数据库/向量库和真实模型/天气 smoke 仍未运行。

## 5. 历史记录与当前边界

2026-08-17 的 v0.1 文档记录了 `D:\TravelMindAI` 当时只有 4 个脚本，并完成四份规划文档；该描述是历史快照，不能继续当作当前目录的状态。

原进度文档还保留过早期预算模块/13 个测试及一次模型调用的历史记录。当前目录没有对应预算模块和测试，因此本次不复用历史“通过”状态；M1 需要在当前正式工程重新实现或恢复并验证。

飞书的示例源码未在本地运行，静态查出的风险不扩展为整个参考项目的运行结论。三个嵌入图块的细节尚未核实，不影响基于其文字与代码对照本次需求。

## 6. 后续开发入口

当前优先按09方案推进A账号与归属 → B历史聊天 → C管理员资料基础 → M4-A文本行程智能体 → D私人附件理解 → E/M4-B附件更新草稿 → M4-C独立审查与持久恢复。批量操作/文件替换不阻塞M4-A。M3条件丢失与扩写质量问题仍保留待办，切片改进先做样本评测，不能通过修改固定题库或放宽打分掩盖。继续按2–3个文件一批说明、先测试后实现，并同步07/08；M2不再作为待启动阶段。

当前正式后端入口是 `backend/app/main.py:create_app`，在PowerShell运行：

```powershell
Set-Location E:\TravelMindAI\backend
python -m uv sync --locked
python -m uv run --locked uvicorn app.main:create_app --factory --env-file .env --reload --host 127.0.0.1 --port 8000
```

打开 [Swagger](http://127.0.0.1:8000/docs)，按 README 创建会话、保存并读取草稿，也可单独执行预算接口。此入口已验证可用；不要把飞书的 `app.main:app` 当成本地入口。若端口已有本阶段服务在运行，不必重复启动。

实施时需核实：

- M1：预算计价单位、测试夹具和依赖增量；保持 `.env` 忽略规则；
- M2：选择已有配置的单一提供方，核实模型标识及结构化输出能力；
- M3：验证 Windows 的 Milvus 部署环境、Embedding 供应方和语料来源；
- M5：逐个核验提供方，不把未配置适配器记为已接通；
- P1：天气/地图数据源及账号配额，接入前保持“待核实”；
- 当前A步：认证与数据隔离优先落地；完成浏览器隐藏入口不等于完成后端授权。

## 7. 后续记录模板

```text
日期：
模块/验收项：
变更文件：
执行命令：
结果：通过 / 失败 / 未运行
验证层级：静态 / 单元 / 集成 / 真实 API / E2E
外部依赖：fake / 本地服务 / 真实 provider
已证明：
未证明：
遗留问题：
下一步：
```
