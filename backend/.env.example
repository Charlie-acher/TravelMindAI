# 正式应用统一使用此模板：复制为同目录的 .env，再填写本机配置。
# backend/.env 被 Git 忽略；真实密钥不能提交。
TRAVELMIND_APP_NAME=TravelMindAI
TRAVELMIND_ENVIRONMENT=development

# PostgreSQL 容器的 POSTGRES_PASSWORD 必须与下面的密码一致。
# 连接格式：postgresql+psycopg://用户名:密码@主机:端口/数据库名
# 若密码含 @、:、/、% 等 URL 特殊字符，需要先进行 URL 编码。
TRAVELMIND_DATABASE_URL=postgresql+psycopg://travelmind:REPLACE_WITH_YOUR_PASSWORD@127.0.0.1:5432/travelmind

# 配置文件不会自动读取，检查命令通过 --env-file .env 显式指定它。
# 操作系统中同名环境变量优先于文件；SecretStr 脱敏不能替代对文件的保管。

# M2单模型需求抽取。密钥留空不影响已有预算服务，抽取命令会提示模型未配置。
# 统一使用正式变量名；DS_API_KEY仅保留为旧环境的兼容别名。
TRAVELMIND_DEEPSEEK_API_KEY=
TRAVELMIND_DEEPSEEK_MODEL=deepseek-flash
TRAVELMIND_DEEPSEEK_TIMEOUT_SECONDS=30
TRAVELMIND_DEEPSEEK_BASE_URL=https://api.deepseek.com

# 另两家模型适配器；完整配置后出现在输入框，也作为首选故障时的备用。
# 只填写已开通的提供方；地址、模型、密钥必须来自同一地区/工作空间。
# 这些密钥不会借用下面的视觉或Embedding密钥。留空密钥者排除出候选池。
# Qwen选择支持enable_thinking=false的文本模型，Kimi选择可关闭思考的K2型号。
# TRAVELMIND_MODEL_PROVIDERS__QWEN__BASE_URL=https://YOUR_WORKSPACE.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
# TRAVELMIND_MODEL_PROVIDERS__QWEN__MODEL=YOUR_TEXT_MODEL
# TRAVELMIND_MODEL_PROVIDERS__QWEN__API_KEY=
# TRAVELMIND_MODEL_PROVIDERS__QWEN__TIMEOUT_SECONDS=30
# TRAVELMIND_MODEL_PROVIDERS__KIMI__BASE_URL=https://api.moonshot.cn/v1
# TRAVELMIND_MODEL_PROVIDERS__KIMI__MODEL=YOUR_K2_MODEL
# TRAVELMIND_MODEL_PROVIDERS__KIMI__API_KEY=
# TRAVELMIND_MODEL_PROVIDERS__KIMI__TIMEOUT_SECONDS=30

# 页面请求按用户首选排序，其后固定DeepSeek、Kimi、Qwen去重；不做随机分流。
# 文本、JSON、规划/研究/周边原生工具共用健康状态和并发限制。
# ROUTES仅用于未传用户选择的旧内部调用；不控制页面显式选择。
TRAVELMIND_MODEL_GATEWAY__MAX_CONCURRENT=4
TRAVELMIND_MODEL_GATEWAY__WINDOW_SIZE=10
TRAVELMIND_MODEL_GATEWAY__FAILURE_THRESHOLD=3
TRAVELMIND_MODEL_GATEWAY__COOLDOWN_SECONDS=30
TRAVELMIND_MODEL_GATEWAY__RETRY_DELAY_SECONDS=0.25
TRAVELMIND_MODEL_GATEWAY__MAX_RETRY_AFTER_SECONDS=5
# 上述限制是每进程、每提供方；多worker不共享计数，重启清除内存健康状态。

# 私人图片使用一个百炼Qwen视觉模型；显式填入同工作空间的兼容地址与密钥。
# 可以使用同一百炼账号，但不会自动借用Embedding密钥。
# TRAVELMIND_VISION_BASE_URL=https://YOUR_WORKSPACE.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
# TRAVELMIND_VISION_API_KEY=REPLACE_WITH_YOUR_KEY
TRAVELMIND_VISION_MODEL=qwen3-vl-plus

# 私人聊天附件的本机MinerU，部署命令见README；留空使用旧解析。
TRAVELMIND_MINERU_BASE_URL=http://127.0.0.1:8010
TRAVELMIND_MINERU_TIMEOUT_SECONDS=600
TRAVELMIND_VISION_TIMEOUT_SECONDS=60

# 原文件目录：不填时使用项目根目录temp/uploads；阶段清理时必须保留。
# 仅改保存位置，不开启模型读取、OCR或向量索引；原文目录不得提交到Git。
# TRAVELMIND_DOCUMENT_UPLOAD_DIR=E:/TravelMindAI/temp/uploads
# M3向量服务：以下为阿里云百炼text-embedding-v4示例
TRAVELMIND_EMBEDDING_PROVIDER=aliyun
TRAVELMIND_EMBEDDING_BASE_URL=https://ws-19x4uyg6j1ouw0hr.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
TRAVELMIND_EMBEDDING_MODEL=text-embedding-v4
TRAVELMIND_EMBEDDING_API_KEY=
TRAVELMIND_EMBEDDING_DIMENSIONS=1024
# TRAVELMIND_EMBEDDING_VERSION=aliyun-text-embedding-v4-1024-v1
TRAVELMIND_EMBEDDING_TIMEOUT_SECONDS=30

# M3本机Milvus：先在项目根目录运行docker compose -f deploy/milvus/compose.yaml up -d。
# 取消注释并重启后端才启用；Web索引与搜索使用上面独立的Embedding配置。
# TRAVELMIND_MILVUS_URL=http://127.0.0.1:19530
# TRAVELMIND_MILVUS_TIMEOUT_SECONDS=30

# 百度地图官方MCP服务端AK；按控制台要求设置服务端IP白名单。
# 地图消费金额仅作人均消费参考，不能据此确认门票或免费。
BAIDU_MAP_API_KEY=

# 网页搜索独立Key：未填写时保留知识库回答，明确提示尚未联网核验。
TRAVELMIND_TAVILY_API_KEY=
# JSON数组：可加入经过核实的景区官网域名；默认只搜索政府公开网站。
TRAVELMIND_TRAVEL_WEB_DOMAINS='["gov.cn"]'
