# 聊天验收修正计划

1. SSE：保留原 JSON 接口，新增带鉴权的 POST 流接口。实际业务步骤立即推送；模型使用真实流，公开回答草稿与最终校验结果分开，禁止展示内部推理、原始 JSON 或密钥。保存成功后才发送 done。验证断流、错误、重试、归属隔离。
2. 自然引导：首次给出目的地时检索城市简介，再以口语补问尚未提供的人数、天数和总预算；普通条件登记不再附加无关“无法确认”。验证混合知识问题仍保留证据不足提示。
3. 附近餐饮：精确定位景点后调用高德周边，半径 2000 米，只推荐实际评分 4.0–5.0 的结果；无评分不补造。短口味追问继承上轮范围。验证筛分、异常、指代与真实服务。
4. 前端消费 SSE，展示处理步骤、回答草稿和最终餐厅卡片；通过构建及浏览器验收，同步 07/08 和调用图。

决定：餐饮使用专门的地图查询分支，避免无关知识库资料阻断地图结果；其余问答保留原 RAG 约束。结构化快照新增可空餐饮字段，无需数据库迁移。浏览器断开后已经开始的操作继续校验保存，刷新按原消息编号恢复，不自动重复提交。

状态：完成。真实 PostgreSQL/Milvus 全回归 479 项通过，mypy 71 文件、定向 Ruff、前端构建与 SSE 契约测试通过。浏览器验证首问登录续接、杭州简介引导、真实周边餐厅与火锅偏好接续、刷新恢复卡片。单次真实 SSE 观测：0.062 秒首进度，10.078 秒首草稿，3 次增量草稿，10.625 秒保存完成；不是耗时承诺。

复核修正：正常停服先等待本应用仍运行的聊天任务，再释放数据库；公开草稿要求完整引用字段先于正文，违反顺序等待最终校验，草稿撤回即时发送；餐饮按高德原始名称、类别或标签二次核对口味，不把关键词召回误当匹配保证。临时验收账号及其四个会话、七轮消息已定向清理。
