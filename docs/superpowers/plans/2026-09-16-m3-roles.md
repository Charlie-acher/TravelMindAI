# M3 双角色与历史会话实施计划

**目标：** 落地09方案的A、B、C，保持现有聊天、锁、幂等和共享检索能力。
**依据：** `docs/09-双角色与对话附件改造方案.md`，用户已要求补齐文档新增开发。
**实现：** 复用FastAPI、SQLAlchemy、Vue；服务端随机登录态；新增成熟密码哈希库；不引入权限平台。

## 执行边界

- 当前`codex/m3`干净工作区直接继续，避免切换用户已有开发位置；不提交、不推送。
- A/B/C已获本次开发授权；D、M4及批量/替换增强仍属后续阶段。
- 用户后续明确要求删除32条旧会话：先备份再清理，以0010迁移强制user_id非空；其他环境仍须在0009显式认领或获得授权后清理，不能猜测归属。
- 知识库固定knowledge-base；迁移旧向量必须保留编号和向量，读回核对后再改PostgreSQL。存在旧范围时阻止新服务资料访问。
- 简洁中文注释，07记录文件顺序，08同步实际模型、接口、调用链和完整数据字典。

## 任务1：账号、会话和迁移（主任务）

- [x] 先写未登录、普通用户管理拒绝、跨账号会话404测试，观察失败。
- [x] users/auth_sessions、0008迁移、密码哈希、登录/退出/me、来源校验及登录频率限制。
- [x] 为所有会话/草稿/历史接口加归属检查，无状态模型接口要求登录。
- [x] GET /sessions用updated_at/id游标分页；首轮成功生成标题。
- [x] 部署命令创建账号、显式认领历史、检查后收紧约束；知识库范围迁移。
- [x] PostgreSQL临时schema集成测试及旧测试适配。

## 任务2：管理员资料基础（独立子任务）

- [x] city/category契约、类别推断、uploaded_by、0009迁移；旧标签列保留但退出业务。
- [x] 文件名/两标签筛选、城市规范化、全量分组计数及城市内分页。
- [x] 注册/admin/documents及旧路径统一管理保护，上传限制覆盖两路径。
- [x] 固定knowledge-base用于资料、后台任务、检索，避免用户ID替代知识范围。
- [x] 新增有意义回归测试并适配旧元数据测试。

## 任务3：角色页面与历史（任务1/2合同固定后子任务）

- [x] 登录页面、按角色导航、退出清缓存；所有写请求携带X-Requested-With。
- [x] 左侧服务端历史、新建/切换/加载更多、失败重试、首轮更新标题，发送时禁切换。
- [x] 移除固定需求面板，后台条件保持。
- [x] 管理页城市折叠、两标签、紧凑多文件上传、默认切片、原文切换。
- [x] 前端构建和浏览器检查。

## 任务4：验收与文档

- [x] 后端测试、mypy、针对修改文件的Ruff、前端构建。
- [x] 新迁移真实PostgreSQL演练；真实数据操作前备份与检查，不能伪造历史归属。
- [x] 代码审查，修复隔离/分页/异步回写等风险。
- [x] 同步07/08/09及阶段说明，明确源码/测试/实际运行的证据层次。

## 交付核对（2026-09-16）

已按用户补充要求开放普通用户自助注册，禁止注册管理员；32条旧会话已备份并删除，0010强制会话账号归属。实际库保留32资料、24101片段，22685向量改范围后读回一致。临时验收账号及2条新测试会话已清理。

全量431项测试、mypy 68文件、修改范围Ruff、前端构建及Alembic check通过；浏览器验证登录、私有历史、真实知识库回答、刷新恢复、管理员资料目录与片段/原文预览。注册为真实HTTP验收及页面入口检查。文档07/08/09记录现行能力与后续阶段，未提交或推送Git。
