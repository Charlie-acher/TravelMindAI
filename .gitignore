# Git 只记录源代码和可共享的配置，不记录个人密钥。
# 下面的规则也匹配子目录内同名的文件；最后一条 ! 表示允许提交示例。
.env
.env.*
!.env.example

# 虚拟环境相当于本项目独立的依赖目录，可以通过 uv.lock 重新安装。
.venv/
venv/

# Python 运行、测试和静态检查自动产生的缓存，无需人工维护。
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# 打包结果可以重新生成；src 中的源代码仍然会被跟踪。
dist/
build/
*.egg-info/

# 编辑器的个人设置、日志及本地工作目录不属于共享工程。
.idea/
.vscode/
*.log
.worktrees/

# 日志、检测报告、验收临时文件及本机上传原文统一存放，不提交Git。
/temp/
# 数据集只保存在本机；data目录只允许chinatravel和lvbangpt。
/data/
/backend/data/
/backend/evals/reports/

# 固定评测输入 backend/evals/*.json 需要提交；只有生成的报告应被忽略。
# 本地数据库及其事务日志不提交。
*.db
*.db-shm
*.db-wal

# 后续 Vue 项目的依赖通过包管理器安装，不提交 node_modules。
node_modules/
