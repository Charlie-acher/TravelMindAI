# 项目文档同步约定

- `docs/08-架构设计说明.md` 是当前源码架构的持续维护文档。后续开发涉及模块职责、接口契约、调用链、模型接入、数据库或会话状态变化时，在该小步交付前同步更新对应章节、架构图与核对日期。
- 明确区分源码已实现、测试通过和真实运行验收；不得把计划能力或尚未执行的迁移写成已运行事实。替换旧流程时改写旧描述，避免前后矛盾。
- `docs/07-各阶段开发说明.md` 继续记录开发顺序和逐文件功能；08文件说明整体架构与模块关系。
- 后续新增或修改数据库表时，在08文件同步维护逐字段数据字典（类型、含义、是否允许空值、默认值）、主外键、唯一/检查约束、索引、表关系与迁移版本；用户提供的参考表单列为待落地设计，不混写为当前已建表。

# 代码注释约定

- 后续新增或修改代码时，沿用简洁、通俗的中文注释，先阅读同层原有文件的写法。
- Python文件开头用三引号说明“哪一层：负责什么”，必要时再用一句话说明谁调用它、它调用谁。例如：`HTTP接口层：接收前端请求，调用业务服务并返回结果。`
- 类的说明放在类体开头，使用“什么类：保存或处理什么”的写法。例如：`资料业务服务类：按资料归属处理上传、查重、保存和读取。` 所属层在文件开头交代，不必在每个类中重复。
- 函数和方法的说明用独立三引号放在`def`或装饰器上方，使用“什么函数：负责什么”的写法；初始化方法、内部函数、中间件和异常处理函数也一样。例如：`资料详情查询函数：按资料编号读取已保存的正文。`
- 字段和关键步骤使用简短的`#`注释，说明含义、单位、执行原因或重要限制。优先使用日常用语，必要术语附带解释；不逐行翻译显而易见的代码，不在注释里堆开发过程和验收记录。
- 只调整注释时保持程序逻辑、接口和数据结构不变；通过代码结构对比和必要静态检查确认。
- 每完成两三个文件，用通俗的话说明开发顺序、各文件作用和调用关系；逐文件说明维护在07文档。
